---
plan: plans/in-progress/2026-04-17-deployment-pipeline.md
checked_at: 2026-04-20T15:57:47Z
auditor: orianna
check_version: 2
claude_cli: present
block_findings: 7
warn_findings: 0
info_findings: 4
---

## Block findings

1. **Step A — Frontmatter:** `status:` field is `in-progress`; expected `proposed` for proposed→approved gate. | **Severity:** block
2. **Step A — Frontmatter:** missing required frontmatter field: `created:` (plan has a non-canonical `date:` field instead). | **Severity:** block
3. **Step A — Frontmatter:** missing required frontmatter field: `tags:`. | **Severity:** block
4. **Step B — Gating question:** Section `## 11. Open questions for Duong` contains 18 unresolved gating bullets (items 1–18) each phrased as a question (standalone `?` at end of sentence in bullet). Recommendations are advisory and do not resolve the gate; plan cannot be approved with open gating questions. | **Severity:** block
5. **Step C — Claim:** `scripts/deploy.sh` (non-suppressed occurrences at §4 contracts "Calls `scripts/deploy.sh <project> [<surface>] --ref <ref>`", §4 note "renamed in P1.1 from the previous `scripts/deploy.sh`", §7 fenced block lines `scripts/deploy.sh <staging-project> ...` and `scripts/deploy.sh myapps-b31ea ...`). | **Anchor:** `test -e scripts/deploy.sh` | **Result:** not found | **Severity:** block
6. **Step C — Claim (fenced code, unsuppressed past marker line):** `scripts/deploy/_lib.sh`, `scripts/deploy/project.sh`, `scripts/deploy/functions.sh`, `scripts/deploy/storage-rules.sh`, `scripts/deploy/smoke.sh`, `scripts/deploy/revert.sh`, `scripts/test-functions.sh`, `scripts/test-storage-rules.sh`, `scripts/test-all.sh` inside §4 fenced block. The `<!-- orianna: ok -->` marker on the first line of the fence suppresses only the immediately following line per contract §8; subsequent path tokens inside the fence remain extractable. | **Anchor:** `test -e` against each path | **Result:** all not found | **Severity:** block
7. **Step D — Sibling:** `plans/in-progress/2026-04-17-deployment-pipeline-tasks.md` exists; must be inlined under `## Tasks` in the plan body and the sibling deleted per ADR §D3 one-plan-one-file rule. | **Severity:** block

## Warn findings

None.

## Info findings

1. **Step C — Claim (author-suppressed):** `plans/approved/2026-04-13-deployment-pipeline-architecture.md` (lines 12 and 450) — both occurrences carry `<!-- orianna: ok -->`. Anchor `test -e plans/approved/2026-04-13-deployment-pipeline-architecture.md` → not found, but suppression marker authorizes the reference (explicitly noted as demoted per orianna-gated-plan-lifecycle T9.1).
2. **Step C — Claim (unknown prefix):** bare workflow filenames `release.yml`, `preview.yml`, `test.yml`, `deploy.yml`, `release-please.yml` appear without a `.github/workflows/` prefix in several prose spans (e.g. §1 "existing `scripts/composite-deploy.sh` + `release.yml`/`preview.yml` path", §9 Phase 2 "`test.yml`, `deploy.yml`, `release-please.yml`, `release.yml` workflows"). Unknown path prefix; add to contract if load-bearing. Fully-qualified `.github/workflows/*` forms are suppressed or resolved elsewhere.
3. **Step C — Claim:** `scripts/composite-deploy.sh` | **Anchor:** `test -e scripts/composite-deploy.sh` | **Result:** found — clean pass.
4. **Step C — Claim:** `scripts/deploy-discord-relay-vps.sh`, `tools/decrypt.sh`, `tools/encrypt.html`, `assessments/2026-04-17-deploy-script-audit.md`, `plans/proposed/2026-04-08-autonomous-delivery-pipeline.md`, `architecture/key-scripts.md`, `apps/myapps/functions/` (cross-repo), `apps/myapps/firebase.json` (cross-repo), `apps/myapps/functions/package.json` (cross-repo), `.github/workflows/release.yml` (cross-repo), `.github/workflows/preview.yml` (cross-repo) — all resolve cleanly; anchors confirmed.
