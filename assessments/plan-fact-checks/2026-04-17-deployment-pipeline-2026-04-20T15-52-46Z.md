---
plan: plans/in-progress/2026-04-17-deployment-pipeline.md
checked_at: 2026-04-20T15:52:46Z
auditor: orianna
check_version: 2
claude_cli: present
block_findings: 23
warn_findings: 0
info_findings: 15
---

## Block findings

1. **Step A — Frontmatter:** status field is `in-progress`; expected `proposed` for proposed→approved gate | **Severity:** block
2. **Step A — Frontmatter:** missing required frontmatter field: `created:` (plan uses `date: 2026-04-17` instead) | **Severity:** block
3. **Step A — Frontmatter:** missing required frontmatter field: `tags:` | **Severity:** block
4. **Step B — Gating question:** unresolved `?` in `## 11. Open questions for Duong` — item 1 "Vitest or Jest for Cloud Functions tests?" | **Severity:** block
5. **Step B — Gating question:** unresolved `?` in `## 11. Open questions for Duong` — item 2 "Encrypted dotenv vs. Firebase Functions 'secret params' (`defineSecret`)." | **Severity:** block
6. **Step B — Gating question:** unresolved `?` in `## 11. Open questions for Duong` — item 3 "Where does the encrypted env file live ...?" | **Severity:** block
7. **Step B — Gating question:** unresolved `?` in `## 11. Open questions for Duong` — item 4 "Deploy from `main` only, or any branch with `--allow-branch`?" | **Severity:** block
8. **Step B — Gating question:** unresolved `?` in `## 11. Open questions for Duong` — item 5 "Audit log retention." | **Severity:** block
9. **Step B — Gating question:** unresolved `?` in `## 11. Open questions for Duong` — item 6 "Firebase CLI auth for local deploys ...?" | **Severity:** block
10. **Step B — Gating question:** unresolved `?` in `## 11. Open questions for Duong` — item 7 "`firebase-functions-test` offline mode vs emulator-backed integration ...?" | **Severity:** block
11. **Step B — Gating question:** unresolved `?` in `## 11. Open questions for Duong` — item 8 "Reconcile `scripts/deploy.sh` and `scripts/composite-deploy.sh` up-front, or retire later?" | **Severity:** block
12. **Step B — Gating question:** unresolved `?` in `## 11. Open questions for Duong` — item 9 "Release-please bot commits on `main` — acceptable?" | **Severity:** block
13. **Step B — Gating question:** unresolved `?` in `## 11. Open questions for Duong` — item 10 "First Bee version — `0.1.0` or `1.0.0`?" | **Severity:** block
14. **Step B — Gating question:** unresolved `?` in `## 11. Open questions for Duong` — item 11 "Release PR auto-merge on green CI, or manual merge by Duong?" | **Severity:** block
15. **Step B — Gating question:** unresolved `?` in `## 11. Open questions for Duong` — item 12 "Staging project ID — create new `myapps-b31ea-staging`, or reuse ...?" | **Severity:** block
16. **Step B — Gating question:** unresolved `?` in `## 11. Open questions for Duong` — item 13 "Continuous staging on every `main` push, OR staging only on release tag?" | **Severity:** block
17. **Step B — Gating question:** unresolved `?` in `## 11. Open questions for Duong` — item 14 "Smoke test scope — `/version` only, or include an auth'd read?" | **Severity:** block
18. **Step B — Gating question:** unresolved `?` in `## 11. Open questions for Duong` — item 15 "Prod deploy approver — Duong only, or any push-rights agent?" | **Severity:** block
19. **Step B — Gating question:** unresolved `?` in `## 11. Open questions for Duong` — item 16 "Shared packages versioning (when they exist) — lockstep or independent?" | **Severity:** block
20. **Step B — Gating question:** unresolved `?` in `## 11. Open questions for Duong` — item 17 "Service-account IAM roles — ...?" | **Severity:** block
21. **Step B — Gating question:** unresolved `?` in `## 11. Open questions for Duong` — item 18 "`auto_revert` default true or false?" | **Severity:** block
22. **Step C — Claim:** integration `release-please` (and `googleapis/release-please-action@v4` in §6) | **Anchor:** not in allowlist §1; not in allowlist §2; no file/line anchor in plan body for the configured integration. Per strict default (contract §4), C1 integration name unverifiable → block | **Severity:** block
23. **Step D — Sibling:** `plans/in-progress/2026-04-17-deployment-pipeline-tasks.md` exists; must be inlined into this plan under `## Tasks` and the sibling deleted, per ADR §D3 one-plan-one-file rule | **Severity:** block

## Warn findings

None.

## Info findings

1. **Step C — Path (this repo):** `assessments/2026-04-17-deploy-script-audit.md` | **Anchor:** `test -e` → exists | **Severity:** info (clean pass)
2. **Step C — Path (this repo):** `scripts/composite-deploy.sh` | **Anchor:** `test -e` → exists | **Severity:** info
3. **Step C — Path (this repo):** `scripts/deploy-discord-relay-vps.sh` | **Anchor:** `test -e` → exists | **Severity:** info
4. **Step C — Path (this repo):** `architecture/key-scripts.md` | **Anchor:** `test -e` → exists | **Severity:** info
5. **Step C — Path (this repo):** `tools/decrypt.sh` | **Anchor:** `test -e` → exists | **Severity:** info
6. **Step C — Path (this repo):** `tools/encrypt.html` | **Anchor:** `test -e` → exists | **Severity:** info
7. **Step C — Path (this repo):** `plans/proposed/2026-04-08-autonomous-delivery-pipeline.md` | **Anchor:** `test -e` → exists | **Severity:** info
8. **Step C — Path (this repo):** `CLAUDE.md` | **Anchor:** `test -e` → exists | **Severity:** info
9. **Step C — Path (strawberry-app):** `apps/myapps/functions/` | **Anchor:** `test -e ~/Documents/Personal/strawberry-app/apps/myapps/functions` → exists | **Severity:** info
10. **Step C — Path (strawberry-app):** `apps/myapps/firebase.json` | **Anchor:** `test -e` → exists | **Severity:** info
11. **Step C — Path (strawberry-app):** `apps/myapps/functions/package.json` | **Anchor:** `test -e` → exists | **Severity:** info
12. **Step C — Path (strawberry-app):** `packages/shared/**` (§1a.6) | marked future-state ("When ... lands"); contract §2 speculative-statement exemption → info (not flagged) | **Severity:** info
13. **Step C — Suppressed:** numerous backtick tokens on lines ending with `<!-- orianna: ok -->` (e.g. §1 lines 11/12, §2 line 60, §3 lines 107–109, §4 lines 124, 141, 143, 160, §5 lines 172, 173, 175, §7a lines 308, 315, 319, §8 line 371, §9 lines 382, 386, 389, 402, 403, §11 line 434, Cross-references line 450) — author-suppressed; logged and passed | **Severity:** info
14. **Step C — Integration (allowlisted, §1):** Firebase, GitHub Actions, Cloud Run, GCE, Discord, Node.js, TypeScript, Vitest, Playwright, age, npm, pnpm — bare vendor names pass | **Severity:** info
15. **Step C — Unknown path prefix:** `logs/deploy-audit.jsonl` (§8) — `logs/` prefix not in contract §5 routing table; add to contract if load-bearing | **Severity:** info

