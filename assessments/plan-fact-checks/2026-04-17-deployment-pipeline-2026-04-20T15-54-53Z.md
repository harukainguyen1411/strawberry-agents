---
plan: plans/in-progress/2026-04-17-deployment-pipeline.md
checked_at: 2026-04-20T15:54:53Z
auditor: orianna
check_version: 2
claude_cli: present
block_findings: 22
warn_findings: 0
info_findings: 4
---

## Block findings

1. **Step A — Frontmatter:** `status:` field is `in-progress`; expected `proposed` for proposed→approved gate | **Severity:** block
2. **Step A — Frontmatter:** missing required frontmatter field: `created:` (plan uses `date:` instead) | **Severity:** block
3. **Step A — Frontmatter:** missing required frontmatter field: `tags:` | **Severity:** block
4. **Step B — Gating question:** unresolved `?` in `## 11. Open questions for Duong` — Q1 "Vitest or Jest for Cloud Functions tests?" | **Severity:** block
5. **Step B — Gating question:** unresolved `?` in `## 11. Open questions for Duong` — Q2 "Encrypted dotenv vs. Firebase Functions 'secret params'?" | **Severity:** block
6. **Step B — Gating question:** unresolved `?` in `## 11. Open questions for Duong` — Q3 "Where does the encrypted env file live?" | **Severity:** block
7. **Step B — Gating question:** unresolved `?` in `## 11. Open questions for Duong` — Q4 "Deploy from `main` only, or any branch?" | **Severity:** block
8. **Step B — Gating question:** unresolved `?` in `## 11. Open questions for Duong` — Q5 "Audit log retention?" | **Severity:** block
9. **Step B — Gating question:** unresolved `?` in `## 11. Open questions for Duong` — Q6 "Firebase CLI auth for local deploys?" | **Severity:** block
10. **Step B — Gating question:** unresolved `?` in `## 11. Open questions for Duong` — Q7 "`firebase-functions-test` offline vs emulator-backed?" | **Severity:** block
11. **Step B — Gating question:** unresolved `?` in `## 11. Open questions for Duong` — Q8 "Reconcile `scripts/deploy.sh` and `scripts/composite-deploy.sh` up-front?" | **Severity:** block
12. **Step B — Gating question:** unresolved `?` in `## 11. Open questions for Duong` — Q9 "Release-please bot commits on `main` acceptable?" | **Severity:** block
13. **Step B — Gating question:** unresolved `?` in `## 11. Open questions for Duong` — Q10 "First Bee version — `0.1.0` or `1.0.0`?" | **Severity:** block
14. **Step B — Gating question:** unresolved `?` in `## 11. Open questions for Duong` — Q11 "Release PR auto-merge on green CI, or manual merge?" | **Severity:** block
15. **Step B — Gating question:** unresolved `?` in `## 11. Open questions for Duong` — Q12 "Staging project ID — create new or reuse?" | **Severity:** block
16. **Step B — Gating question:** unresolved `?` in `## 11. Open questions for Duong` — Q13 "Continuous staging on every `main` push, OR staging only on release tag?" | **Severity:** block
17. **Step B — Gating question:** unresolved `?` in `## 11. Open questions for Duong` — Q14 "Smoke test scope — `/version` only or include auth'd read?" | **Severity:** block
18. **Step B — Gating question:** unresolved `?` in `## 11. Open questions for Duong` — Q15 "Prod deploy approver — Duong only, or any push-rights agent?" | **Severity:** block
19. **Step B — Gating question:** unresolved `?` in `## 11. Open questions for Duong` — Q16 "Shared packages versioning — lockstep or independent?" | **Severity:** block
20. **Step B — Gating question:** unresolved `?` in `## 11. Open questions for Duong` — Q17 "Service-account IAM roles — reactive widening?" | **Severity:** block
21. **Step B — Gating question:** unresolved `?` in `## 11. Open questions for Duong` — Q18 "`auto_revert` default true or false?" | **Severity:** block
22. **Step D — Sibling:** `plans/in-progress/2026-04-17-deployment-pipeline-tasks.md` exists; must be inlined under `## Tasks` and sibling deleted per ADR §D3 one-plan-one-file rule | **Severity:** block

## Warn findings

None.

## Info findings

1. **Step C — Claim:** `turbo.json` | **Anchor:** no routing prefix (bare filename with recognized extension) | **Result:** unknown path prefix; add to contract if load-bearing | **Severity:** info
2. **Step C — Claim:** `logs/deploy-audit.jsonl` | **Anchor:** unknown routing prefix `logs/` | **Result:** unknown path prefix; add to contract if load-bearing | **Severity:** info
3. **Step C — Claim:** `secrets/env/<project>.env.age` (and siblings in §2 layout table) | **Anchor:** unknown routing prefix `secrets/` (this repo does have `secrets/` gitignored; prefix not in contract §5) | **Result:** unknown path prefix; add to contract if load-bearing | **Severity:** info
4. **Step C — Claim (author-suppressed):** multiple `<!-- orianna: ok -->` markers on lines referencing planned/not-yet-built paths (`.github/workflows/test.yml`, `.github/workflows/deploy.yml`, `.github/workflows/release-please.yml`, `scripts/test-*.sh`, `scripts/deploy/*`, `tools/edit-env.sh`, `apps/myapps/functions/.env.myapps-b31ea`, `plans/approved/2026-04-13-deployment-pipeline-architecture.md`, `scripts/vps/`) | **Result:** author-suppressed per §8 | **Severity:** info
