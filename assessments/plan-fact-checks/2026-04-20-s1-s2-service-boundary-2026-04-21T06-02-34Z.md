---
plan: plans/proposed/work/2026-04-20-s1-s2-service-boundary.md
checked_at: 2026-04-21T06:02:34Z
auditor: orianna
check_version: 3
claude_cli: present
block_findings: 2
warn_findings: 0
info_findings: 3
external_calls_used: 0
---

## Block findings

1. **Step C — Claim:** `` `git rm tools/demo-studio-v3/sample-config.json` `` (line 679, BD.D.2 task body) | **Anchor:** `test -e ~/Documents/Work/mmp/workspace/tools/demo-studio-v3/sample-config.json` | **Result:** not found. Plan declares `concern: work`; default resolution root is `~/Documents/Work/mmp/workspace/`, and bare `tools/` is NOT on the opt-back list (contract §5a). The file actually lives at `~/Documents/Work/mmp/workspace/company-os/tools/demo-studio-v3/sample-config.json` (confirmed present). Every other `tools/demo-studio-v3/...` reference in the plan carries `<!-- orianna: ok -->` with a "company-os file under missmp/company-os/..." note; this shell-command token on line 679 is the sole instance missing the suppressor. Fix: add `<!-- orianna: ok -->` to line 679 consistent with the rest of the plan. | **Severity:** block

2. **Step C — Claim:** `` `plans/approved/work/2026-04-20-s1-s2-service-boundary.md §3.2` `` (line 528, BD.B.1 Acceptance) | **Anchor:** `test -e plans/approved/work/2026-04-20-s1-s2-service-boundary.md` | **Result:** not found — this plan is currently at `plans/proposed/work/2026-04-20-s1-s2-service-boundary.md`; `plans/approved/work/` does not yet contain it. This is a self-reference to the plan's post-promotion location, idiomatic for a signed ADR but technically unverifiable at the moment of the proposed→approved gate. Strict default (contract §4) is block. Fix: either suppress with `<!-- orianna: ok — self-reference to post-promotion location -->` or rewrite the test's reference to use the basename / relative anchor. | **Severity:** block

## Warn findings

None.

## Info findings

1. **Step C — HTTP routes:** numerous backtick tokens that begin with an HTTP verb (`GET /sessions`, `GET /session/{id}/status`, `POST /session/new`, `POST /build {sessionId}`, `GET /v1/config/{sessionId}`, `GET /v1/config/{sessionId}/versions`, `GET /v1/schema`, `POST /v1/config`, `POST /v1/configs:batch-get`, etc.) and bare route paths (`/history`, `/logs`, `/preview`, `/session/new`, `/session/{id}`, `/sessions`) appear throughout §2–§5 and the task sections. These are HTTP surface descriptions (S1/S2/S3 API design), not filesystem paths; they happen to contain `/` and satisfy the path-shape heuristic but are clearly HTTP route names in context. Not flagged. | **Severity:** info

2. **Step C — Code expressions:** backtick tokens that are Python expressions (e.g. `` `session.get("config", {}).get("brand"/"insuranceLine"/"market")` `` on lines 96, 916) are code samples illustrating call-site shapes, not filesystem paths. Not flagged. | **Severity:** info

3. **Step C — Suppression coverage:** the plan carries 121 `<!-- orianna: ok -->` suppressors; the overwhelming majority are justified author annotations ("company-os file under missmp/company-os/tools/demo-studio-v3/", "cross-repo reference spec", "grep-gate pattern string", etc.). Author intent is clear and consistent; the two block findings above are almost certainly oversights of the same suppressor pattern. | **Severity:** info

## External claims

None. Step E trigger heuristic did not fire: no URLs, no RFC/spec citations, no pinned library versions, no bare factual assertions about external SDK/library behaviour. Named vendors referenced in prose (Anthropic managed agent, Firestore, Cloud Run, Slack) are either on Section 1 of the allowlist or are generic architectural context without version/symbol claims requiring verification.
