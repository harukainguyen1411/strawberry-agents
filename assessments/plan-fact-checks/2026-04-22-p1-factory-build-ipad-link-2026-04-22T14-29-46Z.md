---
plan: plans/proposed/work/2026-04-22-p1-factory-build-ipad-link.md
checked_at: 2026-04-22T14:29:46Z
auditor: orianna
check_version: 3
claude_cli: present
block_findings: 0
warn_findings: 2
info_findings: 18
external_calls_used: 0
---

## Block findings

None.

## Warn findings

1. **Step B — Marker:** literal `TODO(BD.F.2)` referenced in Context section line 45 (backtick span describing an existing code marker in `factory_bridge_v2.py`) | **Severity:** warn (not in a gating-question section; describing extant code, not an unresolved plan decision)
2. **Step B — Marker:** word "TODO" in §D3.4 line 248 ("mark as a known TODO in code comments, not a blocker") | **Severity:** warn (prose in design-rationale section, not a gating section; explicitly framed as a follow-up note)

## Info findings

1. **Step A — Frontmatter:** `owner: swain` present; OK.
2. **Step C — Suppression:** lines 31–36 contain six author suppression markers (`<!-- orianna: ok -->`) covering all `company-os/tools/...` work-monorepo paths, HTTP route tokens, Firestore collection paths, Python stdlib identifiers, MIME tokens, and Wallet Studio URL templates. All claims on suppressed lines (and the lines that follow standalone markers) are logged here as author-suppressed.
3. **Step C — Author-suppressed (line-trailing markers):** every load-bearing reference to `company-os/tools/demo-studio-v3/...`, `company-os/tools/demo-factory/...`, and `company-os/tools/demo-config-mgmt/...` carries an inline `<!-- orianna: ok -- work-workspace path, not strawberry-agents -->` trailer. Suppressed; no filesystem check attempted. (Lines 42, 44–54, 65, 79, 102, 122, 126, 128, 129, 133, 134, 142, 277, 307, 311, 341, 342, 343, 344, 347, 363, 365–368, 371, 372, 380, 382.)
4. **Step C — Claim:** `factory.py` (line 75, 162) | **Category:** C2b (path-shaped, no internal prefix) | **Severity:** info (non-internal-prefix path token; C2b category; no filesystem check performed).
5. **Step C — Claim:** `tools/demo-factory/apple.py`, `gpay.py`, `journey.py` (line 199) | **Category:** C2b (`tools/` is NOT on the work-concern opt-back list — only `tools/decrypt.sh` and `tools/encrypt.sh` are) | **Severity:** info.
6. **Step C — Claim:** `main.py` (line 364, 374) | **Category:** C2b | **Severity:** info.
7. **Step C — Claim:** `factory_bridge_v2.py` (line 373) | **Category:** C2b | **Severity:** info.
8. **Step C — Claim:** `session.py` (line 375) | **Category:** C2b | **Severity:** info.
9. **Step C — Claim:** `static/studio.js` (line 376) | **Category:** C2b (`static/` not internal prefix) | **Severity:** info.
10. **Step C — Claim:** `tests/test_factory_bridge_v2.py`, `tests/test_build_endpoint.py` (lines 377–378) | **Category:** C2b (`tests/` not internal prefix) | **Severity:** info.
11. **Step C — Claim:** `ws_client.py` (line 49, 128) | **Category:** C2b path-shaped (.py) | **Severity:** info.
12. **Step C — Claim:** `ops/cloud-run/` (line 456) | **Category:** C2b (`ops/` not internal prefix; under work concern, not opt-back) | **Severity:** info.
13. **Step C — Non-claim skips:** HTTP route tokens (`POST /build`, `GET /build/{buildId}`, `POST /session/{sid}/build`, `POST /session/{id}/build`, `GET /v1/config/{id}`, `GET /build/{buildId}/events`) — non-claim per §2 (HTTP route tokens, also covered by line-31 suppression).
14. **Step C — Non-claim skips:** dotted identifiers and template/brace expressions (`firestore.SERVER_TIMESTAMP`, `httpx.AsyncClient`, `asyncio.create_task`, `asyncio.to_thread`, `BackgroundTasks`, `StreamingResponse`, `WSClient`, `factory_bridge_v2.trigger_factory_v2`, `session._UPDATABLE_FIELDS`, `{shortcode}`, `{buildId}`, `{slug}`, `{uid, email, iat}`, etc.) — non-claim per §2 (dotted identifiers, brace expressions; many also covered by line-31/34 suppression).
15. **Step C — Fenced-block skip:** six fenced code blocks (one JSON request body, two JSON event payloads, three Python signature/wire-up blocks at lines 144–160, 186–198, 203–246) were not extracted per §6.
16. **Step C — Allowlist pass:** vendor bare names "Cloud Run", "Firestore", "Wallet Studio", "Slack", "Firebase Auth" are on the allowlist (Section 1) or named-roster entries (Anthropic SDK, Playwright); pass without anchor.
17. **Step C — Integration-name (anchored):** "Wallet Studio project" / "WS project" — referenced consistently as a deployed product surface; not a Section-2 specific-integration entry. Treated as vendor/product reference; pass.
18. **Step D — Sibling files:** `find plans -name '2026-04-22-p1-factory-build-ipad-link-tasks.md' -o -name '...-tests.md'` returned no results. Tasks and test plan are inlined in the plan body under `## Tasks` and `## Test plan`. Pass.

## External claims

None. Step E trigger heuristic did not fire on un-suppressed claims: the plan cites no external URLs (the only `https://` strings — `app.walletstudio.com/...`, `demo.missmp.tech/...` — are suppressed by line 36), no version-pinned third-party libraries, no RFC/spec citations. Internal Python stdlib identifiers (`httpx`, `asyncio`, FastAPI primitives) are suppressed by line 34 as code symbols. Vendor bare names rely on Section-1 allowlist and require no external verification. Budget consumed: 0 / 15.
