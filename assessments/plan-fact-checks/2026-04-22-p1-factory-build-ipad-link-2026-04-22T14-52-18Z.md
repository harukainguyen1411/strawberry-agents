---
plan: plans/proposed/work/2026-04-22-p1-factory-build-ipad-link.md
checked_at: 2026-04-22T14:52:18Z
auditor: orianna
check_version: 3
claude_cli: present
block_findings: 1
warn_findings: 0
info_findings: 12
external_calls_used: 0
---

## Block findings

1. **Step C — Integration:** `demo-factory-canary` (line 452, OQ-1 option b: "or a second Cloud Run service (\`demo-factory-canary\`)?") | **Anchor:** allowlist lookup | **Result:** Cloud Run service names require explicit anchor per `agents/orianna/allowlist.md` Section 2 ("Cloud Run service names… any named service, not the platform itself"). The token appears in a speculative OQ alternative but without a `Proposed:` / `Will:` / `In a future phase:` marker (per claim-contract §2 speculative-marker rule), so it is treated as a present-tense claim and falls under the strict-default block rule (§4). The OQ was resolved to option `a` (same-service traffic-split), so the name is not actually used — a `<!-- orianna: ok -->` line-suppression on line 452 would resolve this without altering the decision. | **Severity:** block

## Warn findings

None.

## Info findings

1. **Step A — Frontmatter:** `owner: swain` present. | **Severity:** info (pass)
2. **Step B — Gating questions:** all OQ-1..OQ-7 marked `[x]` with picks recorded in DECIDED block; no unresolved `TBD` / `TODO` / `Decision pending` inside `## Open questions`. (One `TODO(BD.F.2)` appears on line 45 but inside `## Context` describing existing code — warn-scope per spec, not blocked here as it is factual narration of the current codebase state, not an unresolved gating question for this plan.) | **Severity:** info (pass)
3. **Step C — Path (C2b):** `tools/demo-factory/main.py` (frontmatter line 23) | non-internal-prefix path token under `concern: work` (bare `tools/` is not on the opt-back list); no filesystem check performed. | **Severity:** info
4. **Step C — Path (C2b):** `tools/demo-factory/factory.py` (frontmatter line 23) | non-internal-prefix path token under `concern: work`; no filesystem check performed. | **Severity:** info
5. **Step C — Path (C2b):** `config_mgmt_client.py` (frontmatter line 24) | bare filename, not internal-prefix; no filesystem check performed. | **Severity:** info
6. **Step C — Path (C2b):** `static/studio.js` (frontmatter line 26) | non-internal-prefix path token; no filesystem check performed. | **Severity:** info
7. **Step C — Author-suppressed (bulk):** the plan carries 40+ `<!-- orianna: ok -->` line-suppressions covering `company-os/tools/demo-studio-v3/*`, `company-os/tools/demo-factory/*`, `company-os/tools/demo-config-mgmt/*` paths, Python dotted identifiers (`httpx.AsyncClient`, `asyncio.create_task`, `firestore.SERVER_TIMESTAMP`, `content.logos.wordmark`, `outputUrls.demoUrl`, etc.), Firestore collection paths, MIME types, HTTP route tokens, and deployed-service URLs (`app.walletstudio.com/*`, `demo.missmp.tech/*`). All suppressed tokens on suppressed lines logged as info author-suppressed per §8. Spot verification: `~/Documents/Work/mmp/workspace/company-os/tools/demo-factory/` and `~/Documents/Work/mmp/workspace/company-os/tools/demo-studio-v3/` both exist with the referenced filenames (`main.py`, `apple.py`, `config_mgmt_client.py`, etc.). | **Severity:** info (author-suppressed)
8. **Step C — Non-claim:** `POST /build`, `POST /session/{sid}/build`, `GET /build/{buildId}`, `GET /build/{buildId}/events`, `GET /v1/config/{id}` | HTTP route tokens per claim-contract §2; never extracted. | **Severity:** info
9. **Step C — Non-claim:** `demo-factory-projects`, `walletstudio_archive_asset` (line 469, OQ-4 option b) | identifier-shaped tokens (Firestore collection name, MCP tool identifier) in speculative rejected OQ alternative; classified as non-path, non-integration-vendor; skipped per §6 step 2 "Command / other — skip". Note: if stricter treatment is desired, adding `<!-- orianna: ok -->` to line 469 would make intent explicit. | **Severity:** info
10. **Step D — Siblings:** `find plans/ -name "2026-04-22-p1-factory-build-ipad-link-tasks.md" -o -name "2026-04-22-p1-factory-build-ipad-link-tests.md"` returned no matches. Tasks and Test plan are inlined under `## Tasks` and `## Test plan` sections per ADR §D3 one-plan-one-file rule. | **Severity:** info (pass)
11. **Step E — Budget:** 0 external-tool calls used; 15 available. No trigger conditions fired — the plan cites no library versions, no external URLs requiring fetch, no RFC or spec citations. All vendor references are to Wallet Studio / Firebase / Cloud Run platforms whose existence is already anchored via the allowlist or the work-workspace checkout. | **Severity:** info
12. **Step C — Routing:** `concern: work` detected. Opt-back list applied for `agents/`, `plans/`, `scripts/`, `assessments/`, `architecture/`, `.claude/`, `secrets/`, `tools/decrypt.sh`, `tools/encrypt.sh`. Work-workspace checkout present at `~/Documents/Work/mmp/workspace/`; no warn emitted for missing cross-repo checkout. | **Severity:** info

## External claims

None.
