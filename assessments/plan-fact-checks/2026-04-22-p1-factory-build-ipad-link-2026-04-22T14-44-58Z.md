---
plan: plans/proposed/work/2026-04-22-p1-factory-build-ipad-link.md
checked_at: 2026-04-22T14:44:58Z
auditor: orianna
check_version: 3
claude_cli: present
block_findings: 0
warn_findings: 2
info_findings: 3
external_calls_used: 0
---

## Block findings

None.

## Warn findings

1. **Step B — Gating marker (non-gating section):** literal `TODO` on line 45 in `## Context` (inside backtick span `TODO(BD.F.2)`). Not inside `## Open questions` / `## Gating questions` / `## Unresolved`, so severity is warn per Step B rule ("markers in other sections are warn, not block"). Triage: refers to an in-code TODO label marking the existing S1 scaffold, not an unresolved plan decision. | **Severity:** warn
2. **Step B — Gating marker (non-gating section):** literal `TODO` on line 248 in `### D3.4` ("mark as a known TODO in code comments, not a blocker"). Decision section, not gating → warn. Triage: intentional self-annotation for the asyncio blocking-call follow-up. | **Severity:** warn

## Info findings

1. **Step A — Frontmatter:** `owner: swain` present. | **Result:** pass | **Severity:** info
2. **Step B — Open questions resolution:** all seven OQ items in `## Open questions (original)` are `[x]` with explicit `Pick:` resolutions, mirrored by a `DECIDED:` block (lines 440–448). Question-mark-terminated bullets are treated as resolved and do not emit block findings. | **Severity:** info
3. **Step C — Claim:** `static/studio.js` (line 281) | non-internal-prefix path token (contains `/`, ends in `.js`, but `static/` is not on the C2a opt-back list for work-concern nor on the personal routing table) | C2b category; no filesystem check performed per §5/§6. | **Severity:** info

Additional notes (not individually enumerated):

- **Suppression coverage (§8):** The plan carries six standalone `<!-- orianna: ok -->` justification lines (31–36) and inline markers on the majority of Context/Decision/Impl-surface/Tasks/Test-plan/Risks lines that cite `company-os/tools/demo-studio-v3/*`, `company-os/tools/demo-factory/*`, `company-os/tools/demo-config-mgmt/*` work-workspace paths; HTTP route tokens; Firestore collection paths; Python stdlib/SDK identifiers (`httpx.AsyncClient`, `asyncio.create_task`, `BackgroundTasks`, `StreamingResponse`, `firestore.SERVER_TIMESTAMP`); MIME types; and deployed-service URL patterns. All tokens on those lines are author-suppressed per §8 and are not re-listed above.
- **Step D (sibling files):** `find plans -name "2026-04-22-p1-factory-build-ipad-link-tasks.md" -o -name "...-tests.md"` → zero matches. Plan is single-file compliant with §D3 grandfather rule.
- **Step E (external claims):** no sentence outside suppressed lines cites a pinned version, RFC, or non-template `http(s)://` URL. URL patterns present (`https://demo.missmp.tech/{slug}`, `https://app.walletstudio.com/projects/{project_id}`) contain `{…}` placeholders → template/brace non-claim per §2. Fenced JSON/Python examples skipped per §6. Zero external tool calls issued.

## External claims

None.
