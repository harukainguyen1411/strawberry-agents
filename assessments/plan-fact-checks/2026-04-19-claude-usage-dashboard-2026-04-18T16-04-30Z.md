---
plan: plans/proposed/2026-04-19-claude-usage-dashboard.md
checked_at: 2026-04-18T16:04:30Z
auditor: orianna
claude_cli: present
block_findings: 8
warn_findings: 0
info_findings: 12
---

## Block findings

1. **Claim:** `scripts/usage-dashboard/build.sh` | **Anchor:** `test -e scripts/usage-dashboard/build.sh` (this repo, per §5) | **Result:** not found | **Severity:** block
2. **Claim:** `scripts/usage-dashboard/install-cron.sh` | **Anchor:** `test -e scripts/usage-dashboard/install-cron.sh` (this repo, per §5) | **Result:** not found | **Severity:** block
3. **Claim:** `dashboards/usage-dashboard/` | **Anchor:** `test -e ~/Documents/Personal/strawberry-app/dashboards/usage-dashboard/` | **Result:** not found | **Severity:** block
4. **Claim:** `dashboards/usage-dashboard/roster.json` | **Anchor:** `test -e ~/Documents/Personal/strawberry-app/dashboards/usage-dashboard/roster.json` | **Result:** not found | **Severity:** block
5. **Claim:** `index.html` / `app.js` at `dashboards/usage-dashboard/` (referenced in architecture block and §"Deployment Shape") | **Anchor:** `test -e ~/Documents/Personal/strawberry-app/dashboards/usage-dashboard/{index.html,app.js}` | **Result:** not found | **Severity:** block
6. **Claim:** `agent-scan.mjs` / `merge.mjs` under `scripts/usage-dashboard/` | **Anchor:** `test -e ~/Documents/Personal/strawberry-app/scripts/usage-dashboard/{agent-scan.mjs,merge.mjs}` | **Result:** not found | **Severity:** block
7. **Claim:** `ccusage` (integration / CLI tool referenced as parsing engine) | **Anchor:** not on `agents/orianna/allowlist.md` Section 1 vendor list; not in the implicit CLI allowlist (`gh`, `claude`, `age`, `npm`, `pnpm`, `node`, `jq`) | **Result:** no allowlist anchor | **Severity:** block per §4 strict default. (Plan does cite absolute install path `/Users/duongntd99/.asdf/installs/nodejs/25.4.0/bin/ccusage` — consider adding `ccusage` to the allowlist or anchoring to an official docs URL to clear this finding.)
8. **Claim:** `Chart.js` (rendering dependency, "Chart.js via CDN") — appears in prose without backticks so not strictly extracted, but flagged defensively because the plan depends on it. **Note:** this is a soft block; if author prefers, treat as info. **Anchor:** not on allowlist | **Severity:** block (recommend adding to Section 1).

## Warn findings

None.

## Info findings

1. **Clean pass:** `agents/memory/agent-network.md` | `test -e` OK (this repo).
2. **Clean pass:** `plans/approved/2026-04-19-public-app-repo-migration.md` | `test -e` OK (this repo).
3. **Clean pass:** `dashboards/test-dashboard/` | `test -e` OK (strawberry-app checkout).
4. **Unknown path prefix** `/Users/...` for `/Users/duongntd99/.asdf/installs/nodejs/25.4.0/bin/ccusage`; absolute path, not in routing table. Add to contract if load-bearing.
5. **Unknown path prefix** `~/Documents/Personal/strawberry` — home-dir reference; not in routing table.
6. **Unknown path prefix** `~/.claude/projects/**` — home-dir reference; not in routing table.
7. **Unknown path prefix** `~/.claude/strawberry-usage-cache/agents.json` and `~/.claude/strawberry-usage-cache/` — home-dir reference.
8. **Unknown path prefix** `~/Documents/Work/mmp/workspace/agents/` — home-dir reference.
9. **Unknown path prefix** `strawberry-app/` (as in `strawberry-app/dashboards/usage-dashboard/` and `strawberry-app/scripts/usage-dashboard/{build.sh, agent-scan.mjs, merge.mjs}`) — the `strawberry-app/` leading segment is not in the §5 routing table. If this form is intended to disambiguate cross-repo references in plans, add to contract.
10. **Bare filename tokens** (not path-shaped per §6): `agent-scan.mjs`, `merge.mjs`, `data.json`, `agents.json`, `index.html`, `app.js`, `package.json` — passed through; load-bearing instances covered under block findings 5–6.
11. **Flag/command tokens** not flagged: `-j`, `-i`, `-p`, `-m`, `ccusage session -j`, `ccusage blocks -j`, `ccusage daily -j`, `open`, `sbu`, `file://` — commands/flags per §2 (semantic correctness out of scope).
12. **Roster names** not flagged: `Jayce`, `Viktor`, `Vi`, `Yuumi`, `Syndra`, `Ornn`, `Orianna`, `Evelynn`, `Azir`, `Kayn`, `Aphelios` — appear in `agents/memory/agent-network.md` per §2.
