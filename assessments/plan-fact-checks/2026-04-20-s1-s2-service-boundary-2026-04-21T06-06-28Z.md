---
plan: plans/proposed/work/2026-04-20-s1-s2-service-boundary.md
checked_at: 2026-04-21T06:06:28Z
auditor: orianna
check_version: 3
claude_cli: present
block_findings: 0
warn_findings: 0
info_findings: 2
external_calls_used: 0
---

## Block findings

None.

## Warn findings

None.

## Info findings

1. **Step C — Author-suppressed:** Plan body contains extensive `<!-- orianna: ok -->` suppressors covering all cross-repo file-path citations (missmp/company-os paths under `tools/demo-studio-v3/`, `reference/*.yaml`, companion amendment files). Suppressions match the intended escape-hatch usage per claim-contract §8 — each marks a cross-repo audit reference that lives in `~/Documents/Work/mmp/workspace/company-os/` and cannot be verified against this repo. | **Severity:** info
2. **Step C — HTTP routes vs filesystem paths:** Numerous backticked tokens containing `/` (e.g. `GET /v1/config/{session_id}`, `POST /build {sessionId}`, `/preview`, `/sessions`, `GET /v1/schema`) are HTTP API route templates, not filesystem paths. Classified as command-shaped tokens (C3) rather than path-shaped (C2); not routed through `test -e`. S2 OpenAPI spec (`reference/2-config-mgmt.yaml`) is cited as the anchor on the suppressed line 46. | **Severity:** info

## External claims

None. (Step E trigger heuristic did not fire: plan cites no external library versions, SDK names, URLs, or RFCs requiring verification. All claims are internal — company-os code symbols, S2/S3 internal OpenAPI contracts, and cross-ADR task handoffs within the same missmp/company-os workspace.)
