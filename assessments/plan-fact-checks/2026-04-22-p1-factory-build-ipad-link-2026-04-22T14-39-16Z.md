---
plan: plans/proposed/work/2026-04-22-p1-factory-build-ipad-link.md
checked_at: 2026-04-22T14:39:16Z
auditor: orianna
check_version: 3
claude_cli: present
block_findings: 7
warn_findings: 0
info_findings: 6
external_calls_used: 0
---

## Block findings

1. **Step B — Gating question:** unresolved `?` marker in bullet "OQ-1 — Canary split mechanics: …(`demo-factory-canary`)?" inside `## Open questions (original)` | **Severity:** block. Resolve by checking the box (`[x]`), removing the trailing `?`, or suppressing the line with `<!-- orianna: ok -->`. (A `Pick:` is stated in the nested bullet, but the heading still reads as an unresolved question.)
2. **Step B — Gating question:** unresolved `?` marker in bullet "OQ-2 — Should S1 write `buildId` and `projectId` to the session synchronously…?" inside `## Open questions (original)` | **Severity:** block.
3. **Step B — Gating question:** unresolved `?` marker in bullet "OQ-3 — Does the integration test T.P1.12 run against a real staging WS account, or a WSClient-level mock?" inside `## Open questions (original)` | **Severity:** block.
4. **Step B — Gating question:** unresolved `?` marker in bullet "OQ-4 — Orphan WS projects … clean up in P1 or defer?" inside `## Open questions (original)` | **Severity:** block.
5. **Step B — Gating question:** unresolved `?` marker in bullet "OQ-5 — S1 SSE reconnect / fallback … add `GET /build/{buildId}` polling fallback in P1?" inside `## Open questions (original)` | **Severity:** block.
6. **Step B — Gating question:** unresolved `?` marker in bullet "OQ-6 — URL hostnames … hardcode in S3 or env-var?" inside `## Open questions (original)` | **Severity:** block.
7. **Step B — Gating question:** unresolved `?` marker in bullet "OQ-7 — Lulu advises vs Neeko designs the 'Demo ready' panel; which tier?" inside `## Open questions (original)` | **Severity:** block.

## Warn findings

None.

## Info findings

1. **Step C — Path (C2b):** `tools/demo-factory/main.py` (line 23) | non-internal-prefix path token under `concern: work`; C2b category; no filesystem check performed.
2. **Step C — Path (C2b):** `tools/demo-factory/factory.py` (line 23) | non-internal-prefix path token under `concern: work`; C2b category; no filesystem check performed.
3. **Step C — Path (C2b):** `static/studio.js` (line 26) | non-internal-prefix path token; C2b category; no filesystem check performed.
4. **Step C — Path (C2b):** `project.py` (line 252) | non-internal-prefix path token (extension match only); C2b category; no filesystem check performed.
5. **Step C — Author-suppressed:** ~50 backtick spans across the plan body sit on lines carrying `<!-- orianna: ok -->` markers (work-workspace path attestations, HTTP-route notes, Firestore collection-path notes, Python identifier notes, MIME-type notes, deployed-service URL notes). All logged as info per §8 suppression rule and not re-enumerated individually.
6. **Step D — Sibling check:** no `<basename>-tasks.md` or `<basename>-tests.md` siblings found under `plans/`. §D3 one-plan-one-file rule satisfied.

## External claims

None. Step E trigger heuristic did not fire on any unsuppressed sentence (no library/SDK + version pair, no bare URL outside suppressed lines, no RFC citation). External-call budget unused (0 / 15).
