---
plan: plans/in-progress/2026-04-17-deployment-pipeline.md
checked_at: 2026-04-20T15:58:37Z
auditor: orianna
check_version: 2
claude_cli: present
block_findings: 18
warn_findings: 0
info_findings: 6
---

## Block findings

1. **Step A — Frontmatter:** `status:` field is `in-progress`; expected `proposed` for proposed→approved gate | **Severity:** block
2. **Step A — Frontmatter:** missing required frontmatter field: `created:` (plan uses `date: 2026-04-17` instead) | **Severity:** block
3. **Step A — Frontmatter:** missing required frontmatter field: `tags:` | **Severity:** block
4. **Step D — Sibling:** `plans/in-progress/2026-04-17-deployment-pipeline-tasks.md` exists; must be inlined under `## Tasks` section of the plan body and deleted per ADR §D3 one-plan-one-file rule | **Severity:** block
5. **Step B — Gating question:** unresolved `?` at end of bullet 1 in `## 11. Open questions for Duong` ("Vitest or Jest for Cloud Functions tests?") | **Severity:** block
6. **Step B — Gating question:** unresolved `?` at end of bullet 3 in `## 11. Open questions for Duong` ("Where does the encrypted env file live…?") | **Severity:** block
7. **Step B — Gating question:** unresolved `?` at end of bullet 4 in `## 11. Open questions for Duong` ("Deploy from `main` only, or any branch with `--allow-branch`?") | **Severity:** block
8. **Step B — Gating question:** unresolved `?` at end of bullet 6 in `## 11. Open questions for Duong` ("Firebase CLI auth for local deploys…?") | **Severity:** block
9. **Step B — Gating question:** unresolved `?` at end of bullet 7 in `## 11. Open questions for Duong` ("`firebase-functions-test` offline mode vs emulator-backed integration…?") | **Severity:** block
10. **Step B — Gating question:** unresolved `?` at end of bullet 8 in `## 11. Open questions for Duong` ("Reconcile `scripts/deploy.sh` and `scripts/composite-deploy.sh` up-front, or retire later?") | **Severity:** block
11. **Step B — Gating question:** unresolved `?` at end of bullet 9 in `## 11. Open questions for Duong` ("Release-please bot commits on `main` — acceptable?") | **Severity:** block
12. **Step B — Gating question:** unresolved `?` at end of bullet 10 in `## 11. Open questions for Duong` ("First Bee version — `0.1.0` or `1.0.0`?") | **Severity:** block
13. **Step B — Gating question:** unresolved `?` at end of bullet 11 in `## 11. Open questions for Duong` ("Release PR auto-merge on green CI, or manual merge by Duong?") | **Severity:** block
14. **Step B — Gating question:** unresolved `?` at end of bullet 13 in `## 11. Open questions for Duong` ("Continuous staging on every `main` push, OR staging only on release tag?") | **Severity:** block
15. **Step B — Gating question:** unresolved `?` at end of bullet 14 in `## 11. Open questions for Duong` ("Smoke test scope — `/version` only, or include an auth'd read?") | **Severity:** block
16. **Step B — Gating question:** unresolved `?` at end of bullet 15 in `## 11. Open questions for Duong` ("Prod deploy approver — Duong only, or any push-rights agent?") | **Severity:** block
17. **Step B — Gating question:** unresolved `?` at end of bullet 16 in `## 11. Open questions for Duong` ("Shared packages versioning…lockstep or independent?") | **Severity:** block
18. **Step B — Gating question:** unresolved `?` at end of bullet 17 in `## 11. Open questions for Duong` ("Service-account IAM roles…?") | **Severity:** block

## Warn findings

None.

## Info findings

1. **Step C — Claim:** `logs/deploy-audit.jsonl` | **Anchor:** unknown path prefix `logs/`; add to contract routing table if load-bearing | **Severity:** info
2. **Step C — Claim:** `release-please-config.json` | **Anchor:** repo-root config file, no routing-table prefix; assumed strawberry-app repo root for Phase 2 wiring | **Severity:** info
3. **Step C — Claim:** `.release-please-manifest.json` | **Anchor:** repo-root config file, no routing-table prefix; assumed strawberry-app repo root for Phase 2 wiring | **Severity:** info
4. **Step C — Claim:** `Duongntd/strawberry` | **Anchor:** GitHub org/repo name inside encrypted-env value example; not a filesystem path — classification info only | **Severity:** info
5. **Step C — Claim (author-suppressed):** multiple `scripts/deploy/*`, `scripts/test-*.sh`, `.github/workflows/{test,deploy,release-please}.yml`, `scripts/vps/`, `tools/edit-env.sh`, cross-reference `plans/approved/2026-04-13-deployment-pipeline-architecture.md`, `googleapis/release-please-action@v4` tokens were suppressed via `<!-- orianna: ok -->` markers noting Phase 1/2 build targets or historical plan refs | **Severity:** info
6. **Step C — Claim:** numerous backtick spans (`apps/myapps/functions/`, `apps/myapps/firebase.json`, `apps/myapps/functions/package.json`, `.github/workflows/release.yml`, `.github/workflows/preview.yml` in strawberry-app; `scripts/composite-deploy.sh`, `scripts/deploy-discord-relay-vps.sh`, `tools/decrypt.sh`, `tools/encrypt.html`, `assessments/2026-04-17-deploy-script-audit.md`, `architecture/key-scripts.md`, `CLAUDE.md`, `plans/proposed/2026-04-08-autonomous-delivery-pipeline.md` in this repo) verified present via `test -e` — clean pass | **Severity:** info
