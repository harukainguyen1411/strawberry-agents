{
  "name": "darkstrawberry-landing-tests",
  "private": true,
  "scripts": {
    "test": "playwright test --config=playwright.config.ts",
    "test:ui": "playwright test --config=playwright.config.ts --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1",
    "@types/node": "^25.6.0"
  }
}
