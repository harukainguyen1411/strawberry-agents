## Role
PR reviewer focused on plan and ADR fidelity. Opus executor. Activated 2026-04-19.

Lucian verifies that implementation PRs faithfully execute the approved plan: scope matches the plan, tasks are completed in order, no out-of-scope changes landed, ADR decisions respected, and commit discipline upheld. Pairs with Senna (code quality + security) — together they replace the retired Jhin. Every PR gets both reviewers before merge.

## Persistent context

- `harukainguyen1411/strawberry-app` `main` has classic branch protection as of 2026-04-19 (per `plans/implemented/2026-04-19-branch-protection-restore.md`). Required contexts include `Playwright E2E`, `Unit tests (Vitest)`, `E2E tests (Playwright / Chromium)`, plus two more (5 total). `enforce_admins: false` — `harukainguyen1411` can UI-bypass. Only admins can read `/branches/main/protection`; non-admin accounts get 404.
- Repo convention: any workflow whose job name is a required status check MUST trigger on every `pull_request` to `main` (no `paths-ignore`) and gate work on an internal `changed` step that reports success on skip. Precedents: `myapps-test.yml`, `myapps-pr-preview.yml`. GitHub does not synthesise success for `paths-ignore` skips.
- Rule 18 — never review a PR you authored. Duong uses two gh accounts (`Duongntd` and `duongntd99`); switch with `gh auth switch --user <name>` when the author matches the active account.
- `gh pr review --approve` is GitHub-blocked when the active account authored the PR (even if you're switching between Duong's two accounts, whichever one authored the PR cannot `--approve`). Fallback: post `--comment` with explicit APPROVE verdict and note that Rule 18 still requires a non-author approval before merge. Precedent: PR #48 re-review 2026-04-19.
- Prefer `scripts/reviewer-auth.sh gh pr review ...` for ALL reviews — routes through `strawberry-reviewers` bot identity so self-review block never triggers. Preflight `scripts/reviewer-auth.sh gh api user --jq .login` should return `strawberry-reviewers`.
- Workflow-only hotfixes (`.github/workflows/**`) with `chore:` prefix sit in a Rule 13 grey zone: pre-push hook targets bug/bugfix/regression/hotfix tags, so `chore:` typically doesn't trip it. Flag as drift note; suggest `Orianna-Bypass:` trailer only if hook actually blocks. Precedent: PR #54 (2026-04-19).
- When reviewing PRs in a sibling repo, NEVER use `gh pr checkout` from inside an unrelated repo's cwd — it switches the current repo's branch, masking missing files (e.g. `scripts/reviewer-auth.sh`). Use `gh pr diff` + `gh api` for review, or clone to `/tmp` and fetch `pull/N/head` there. Precedent: PR strawberry-app#59 (2026-04-19).
- **Rule 12 xfail-first verification technique:** fetch each commit's patch separately via `gh api repos/OWNER/REPO/commits/<sha> --jq '.files[] | select(.filename==...) | .patch'`. Confirm the pre-impl commit contains an XFAIL sentinel that exits 0, and the impl commit removes that guard. Textbook shape: pre-impl has `if [ "$FAIL" -gt 0 ]; then printf 'XFAIL: <tag>\n'; exit 0; fi`; impl replaces with `[ "$FAIL" -eq 0 ]`. Precedent: PR #11 (2026-04-21).
- **When the delegation prompt flags a deviation, also audit the plan itself.** In PR #11, Talon's deviation (`agent_proxy.py` vs plan's `session_store.py`) was correct *because the plan cited a non-existent file*. The plan was the wrong one, not the PR. Always spot-check plan-cited file paths against reality before ruling a deviation as divergence. Precedent: PR #11 (2026-04-21).
- **Body-hash fidelity is the highest-signal gate for Orianna-signed plans.** When reviewing a PR that touches an Orianna-signed plan (or whose plan has `orianna_signature_approved` frontmatter), always run `scripts/orianna-hash-body.sh <plan-path>` on the PR-branch copy and compare to the `orianna_signature_approved` value. If they match, the plan body is unchanged since signing; if not, that's a structural block (silent plan drift). Precedent: PR #12 (2026-04-21).
- **Dogfooding claims in plans are easy to falsify.** When a plan's §4 Dogfooding section asserts "this plan file passes the new linter," actually stage the plan file against the new linter and run it. In PR #12 the plan claimed compliance but the new rule-4 check flagged prose references like `` `proposed/` ``. Grandfathering saved the current state but the claim was false. Always test dogfooding assertions literally. Precedent: PR #12 (2026-04-21).
- **Stash-based test baseline is unreliable for PR regression checks.** When the PR adds new tests that assume the new impl, running the test file against main via `git stash` then re-popping can misreport failures (tests fail because impl is missing, not because the test is broken). Always run tests in the PR branch's own worktree. Precedent: PR #15 (2026-04-21).
- **Bug-fix-without-plan reviews anchor to the originating plan's design-intent quote.** When a PR is dispatched as a direct fix with no standalone plan, find the originating plan's specific sentence of design intent (e.g. "hook only runs on staged diffs, so existing plans are grandfathered") and evaluate the PR against that sentence literally. No plan to align with — the quote IS the contract. Precedent: PR #15 rule-4 staged-diff scope (2026-04-21).
- **Work-concern PRs (e.g. `missmp/company-os`) — reviewer bot has no access.** `strawberry-reviewers` + `strawberry-reviewers-2` are not collaborators on work repos; `scripts/reviewer-auth.sh` returns 404 on `gh api repos/missmp/company-os`. Fallback: post a regular `gh pr comment` under `duongntd99` — plain comments on self-authored PRs are allowed, formal `--approve`/`--request-changes` are not. Preflight with `scripts/reviewer-auth.sh gh api repos/<owner>/<repo>` BEFORE drafting the review so you know the posting path upfront. Sona has been flagged to grant bot access. Precedent: PR missmp/company-os#57 (2026-04-21).
- **Plan-file-not-in-diff shortcut for signature fidelity.** If `gh pr diff N --name-only` shows the Orianna-signed plan file is NOT in the PR diff, the body-hash-vs-signature check collapses to trivially valid — skip the `scripts/orianna-hash-body.sh` comparison. Only when the plan is in-diff does signature drift become a gate. Precedent: PR #19 (2026-04-22).
- **Subsumption recommendations require reading the adjacent plan.** When a delegation prompt flags overlap between the PR's plan and a sibling plan, open the sibling and verify the sibling's task set STRICTLY supersedes the deferred tasks in scope before recommending SUBSUME. Cite specific sibling task IDs. Watch for target-path divergence (e.g. legacy `.claude/_script-only-agents/orianna.md` vs new `agents/orianna/prompts/*.md` layout) — flag as a janitorial sub-task, not a blocker. Precedent: PR #19 T-prompt-2/3 subsumed by substance-vs-format rescope (2026-04-22).
- **Textbook wave-migration PR pattern.** When reviewing "Wave N of a multi-wave plan" PRs, the cleanest fidelity signals are: (a) diff scope = single new folder/module boundary; (b) commit chain = xfail → impl → flip (Rule 12 textbook); (c) OQ resolutions cited by name in code comments (e.g. "OQ4 dual-write: S1 writer stays live until T.W3.3"). When all three hold, fidelity review collapses to a scope-creep grep and a route-coverage table against plan §2.1. Precedent: PR missmp/company-os#66 W2 route migration (2026-04-22).
- **Test-patching constraints create "dead-code-by-design" modules.** When a plan task adds a helper module (e.g. `test_results_store.py`) but the consuming routes inline the logic instead of importing (because tests need to patch module-level globals of the consuming module, not the helper), flag as drift note NOT blocker. The code is correct; the helper is reserved for a later wave's reuse (e.g. S1 writer rewire in T.W3.3). Precedent: PR #66 (2026-04-22).
- **Textbook T1..Tn plan commit-count = task-count review pattern.** When Karma-style plans declare T1 xfail → T2..Tn-1 impl → Tn flip, the fidelity review collapses to: (a) `git log` count equals plan task count; (b) first commit's patch contains `pytest.mark.xfail` citing the plan filename; (c) last commit's patch removes the xfail marker; (d) each commit's `.files[]` stays inside that task's declared file set. Rule 12 parent-SHA verification (`gh api repos/.../commits/<impl-sha> --jq '.parents[].sha'` equals T1 sha) is cheaper than fetching full patches. Precedent: PR missmp/company-os#67 preview-iframe port (2026-04-22).
- **Plan-path-prefix cosmetic drift.** Sona-delegated plans sometimes write file paths with `mmp/workspace/tools/...` prefix reflecting her workspace layout; PRs land the file at `tools/...` relative to the target repo root. Call out as plan hygiene note for future Karma plans, not as structural divergence. Precedent: PR missmp/company-os#67 (2026-04-22).
- **Docs-consistency fix reviews = four-way grep.** When a PR is a post-merge advisory to align an outlier doc with canonical phrasing (e.g. Rule 18 amendment), the fidelity check collapses to: grep the rule across CLAUDE.md, architecture/agent-network-v1/pr-rules.md, agents/memory/agent-network.md, and the file being fixed; confirm they now all agree; confirm the removed clause was genuinely tautological (not weakening the rule). Fast approval. Precedent: PR #26 three-gate → two-gate (2026-04-22).
- **Delegation prompts may omit repo owner.** Prompts sometimes name only the PR number. Initial `gh pr view N --repo Duongntd/strawberry-agents` will 404 because the repo is `harukainguyen1411/strawberry-agents`. Always run `git remote -v` first when the owner is ambiguous. Precedent: PR #26 (2026-04-22).
- **Stacked wave PRs — xfail-first is per-branch, not per-stack.** When Aphelios decomposes a loop into stacked PRs (PR_N bases on PR_{N-1}'s branch), Rule 12 requires xfail-first ON THE CURRENT BRANCH. A prep commit on an ancestor branch that adds ALL future xfails in one shot (e.g. Xayah's c257942 with 13 xfails) is NOT a substitute — each wave PR must re-declare its own xfails. Fidelity check reduces to: (a) per-branch xfail→impl parent-SHA chain; (b) cross-PR base chain (W2.xfail.parent == W1.impl; W5.xfail.parent == W2.impl); (c) each PR's file set inside plan's declared Files. Precedent: PR missmp/company-os #80/#81/#82 (2026-04-23).
- **Phasing-bridge non-drift.** When a wave changes a function's signature (e.g. positional args removed, Optional→str) and a to-be-deleted-in-later-wave caller still exists, the PR must supply stub values (e.g. `owner_uid=""`). This is NOT scope creep — it's required by the plan's wave phasing. Flag as drift note, not blocker. Precedent: PR missmp/company-os #80 POST /session bridge (2026-04-23).
- **Dead-import sweep grep via gh api contents + base64.** For T.W5.6-style static-asset-hygiene tasks, cheapest verification is `gh api repos/.../contents/<path>?ref=<branch> --jq .content | base64 -d | grep -nE "<patterns>"`. Use this instead of cloning the repo for a one-line check. Precedent: PR missmp/company-os #82 W5.6 (2026-04-23).
- **God-PR fidelity heuristics.** For 250-commit / 483-file long-lived-branch ships: (a) `gh api .../pulls/N/commits --paginate` + grep for task-tag prefixes instead of reading all commits; (b) anchor on the most recent fixup commit's message — it usually documents every deliberate concession and tags follow-ups; (c) boundary-file spot-check via `for f in <modules>; gh api contents | base64 -d | grep "<boundary import>"` beats full-diff; (d) distinguish SDK-helper imports (decorators, enums, version probes — fine) from data-path bypasses (e.g. `db.collection().stream()` — violation). Single drift note + APPROVE-with-followup is the textbook verdict shape. Precedent: PR missmp/company-os #32 demo-studio-v3 god PR (2026-04-25).
- **Anonymity scan denylist for work-scope reviews.** `scripts/post-reviewer-comment.sh` rejects bodies containing agent names (Azir, Orianna, Akali, Heimerdinger, etc.). When quoting code comments that contain `# azir: boundary` or similar, paraphrase or redact before composing the review body. Safer pattern: compose work-scope reviews from scratch using role descriptors (e.g. "deploy + QA leads", "standard plan-lifecycle path"). Precedent: PR #32 (2026-04-25), recurrence on PR #119 (Akali echoed from plan vocabulary).
- **"Was task T-N skipped?" non-task disambiguation.** When a delegation prompt asks whether tasks were skipped or rolled in, check if the task's DoD is "<file> untouched" or similar no-op. If yes, skipped == satisfied — the task exists in the plan to prevent overreach (e.g. PR #119 T5 "no deploy.sh change"). Precedent: PR missmp/company-os #119 (2026-04-25).
- **Behavioural Rule 12 verification at commit boundaries.** When the xfail test exercises a script also being modified by the plan, clone branch into /tmp, `git checkout <T1-sha> -- <test> <impl>`, run test (expect xfail-honored skip), then `git checkout <tip> -- <test> <impl>`, run again (expect pass). Stronger than diff-only Rule 12 because it proves the xfail truly fails red and impl truly flips green at the right SHAs. Precedent: PR #57 Orianna identity alignment (2026-04-25).
- **Trailer vs commit-msg hook rejection-pattern check.** Persona-trailer reviews (`Promoted-By: Orianna`, etc.) collapse to grepping `scripts/hooks/commit-msg-no-ai-coauthor.sh` for the persona keyword vs BODY_MARKERS. The hook only blocks `claude/anthropic/sonnet/opus/haiku/AI-generated/🤖/claude.com` + Co-Authored-By + AI email domains — non-AI persona words survive. Precedent: PR #57 (2026-04-25).
- **Karma quick-lane plan PR pattern: 3-commit chain, single-digit-minute review.** Karma plans typically decompose into T1 (xfail) / T2..Tn-1 (impl) / Tn (wire-or-flip), landing as a 3-commit branch. Fidelity review collapses to: (a) three `gh api commits/<SHA> --jq '.parents[].sha,.files[].filename'` calls — confirms Rule-12 parent-SHA chain plus per-commit file scope; (b) line-count algebra on any sync-script outputs (include source N lines + 1 directive line == both wired defs net `+N+1 -0` with byte-identical expansion blocks — diagnostic of clean `scripts/sync-shared-rules.sh` invocation vs hand edits); (c) deferred-option grep — when plan defers a sibling option (e.g. Lux option C → PreToolUse hook), grep diff for that option's surface (`scripts/hooks/`, `.claude/settings.json`); absence is the gate. Precedent: PR #58 coordinator-routing-discipline (2026-04-25).
- **Cornerstone-PR fidelity-pass shape.** Phase-1-of-multi-phase PRs: grep ADR for N architectural decisions and 1-file-lookup each; Rule-12 chain via single `pulls/N/commits` parents; Phase-2 boundary grep on SQL/HTML for next-phase column names; OQ-resolution-claims cross-checked between PR body, impl-file comments, AND xfail-test status. The last two can drift independently — wired-impl-without-test-flip is a coverage gap drift note. Precedent: PR #59 retrospection-dashboard Phase 1 (2026-04-25).
- **Fixture-correction-vs-test-fudge disambiguation.** When impl commits modify fixtures, three categories: (a) comment-vs-data inconsistency (legit — comment was always intended); (b) schema-alignment (legit — camelCase upstream conventions); (c) expected-output rewrite to match buggy impl (illegit — make-it-pass). Diff the comment-AND-test-assert-AND-data-value triple to disambiguate. Precedent: PR #59 idle-gap T6 `10:06:28→10:06:25` honest correction (2026-04-25).
- **Rule-5 `feat:` vs `chore:` for non-`apps/**` diffs is plan-explicit but unenforced.** Plans for `tools/`, `scripts/`, `architecture/` work typically include a "All `chore:` prefix" line in §Phase-N-cadence. Repo has no active prefix-vs-scope hook (pre-push is a 16-line stub). When violated, squash-on-merge with the PR title (usually `chore:`) masks it on mainline. Drift note + recommend follow-up prefix-vs-scope guard. Precedent: PR #59 (2026-04-25).
- **Cross-plan ADR fidelity at contract surfaces.** When wave-N PR documents a contract owned by a sibling cornerstone plan (e.g. §Q6 canonical-v1 lock spec living in cornerstone but documented by consolidation W2), diff the contract surface line-by-line — paths, trailer names, audit-log locations are silent-drift hotspots. Two plans approved on the same day is not a license to redefine; one must mirror the other verbatim or land an amendment. Precedent: PR #65 Lock-Bypass log path drift `architecture/canonical-v1-bypasses.md` (cornerstone) vs `agents/<coordinator>/audit/lock-bypass-<date>.md` (PR) (2026-04-25).
- **Delegation-prompt cite-list literal-verification.** When the prompt enumerates rule numbers (e.g. "cites Rules 5/12/13/14/15/16/17/18/19/21"), grep the file (`grep -nE "Rule [0-9]+"`) and compare to the prompt list verbatim. Don't accept gestalt fidelity — the prompt named the contract. Precedent: PR #65 missing Rules 14 and 19 in pr-rules.md (2026-04-25).
- **Round-2 fidelity-pass collapses to 4-step verify.** When initial review APPROVE-with-N-drifts and the implementer's round-2 push claims all N resolved + Senna B/I fixes: (a) cat seeded files and diff vs plan DoD verbatim; (b) grep documentation extension for §-spec bullet checklist; (c) `git show --stat` each fix commit, confirm file scope = existing-only (no new fields/tools/hooks); (d) re-run named tests for corrected formula assertions. Co-located xfail commits bundling multiple findings (e.g. one xfail for B1+I2+I4 → three separate impl commits parented on it) are clean Rule 12 — parent-SHA chain shows xfail-first. Defense-in-depth gate fixes (I3-class: narrow when existing code runs) don't require xfail; behavioral fixes (B1/I2/I4) do. Precedent: PR #64 round-2 (2026-04-25).
- **Parallel-track fan-out: first-PR precedent surface.** When reviewing the first PR in a plan's parallel-track triple (e.g. T.P2.2/T.P2.3/T.P2.4 dispatched off the same base), watch for new mechanisms (sidecar files, new SQL annotations, new module patterns) that will become precedent for the siblings. Even when within the literal DoD of the current task, surface as a follow-up for the parent coordinator to either (a) amend the plan to bless the pattern across siblings or (b) explain why the siblings should diverge. Cross-PR consistency is a coordinator concern but the first-PR reviewer is the only one positioned to spot it early. Precedent: PR #88 T.P2.2 sidecar `feedback-events.jsonl` vs plan §line-290 "extends events.jsonl" framing (2026-04-26).
- **DuckDB schema-inference + empty-JSONL footguns are real.** When a plan says "extend events.jsonl with kind: X events" and the implementer instead uses a sidecar JSONL, two technical justifications hold: (a) DuckDB `read_ndjson_auto` schema inference confuses across disjoint field sets between row kinds; (b) DuckDB opening an empty JSONL as a database arg creates a `file` table with only a `json` column → column-not-found errors on named-column queries. Both are documented engineering trade-offs, not laziness. The contract test is the read-contract column shape, not the file boundary. Precedent: PR #88 (2026-04-26).
