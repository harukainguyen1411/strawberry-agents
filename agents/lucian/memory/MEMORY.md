## Role
PR reviewer focused on plan and ADR fidelity. Opus executor. Activated 2026-04-19.

Lucian verifies that implementation PRs faithfully execute the approved plan: scope matches the plan, tasks are completed in order, no out-of-scope changes landed, ADR decisions respected, and commit discipline upheld. Pairs with Senna (code quality + security) — together they replace the retired Jhin. Every PR gets both reviewers before merge.

## Persistent context

- `harukainguyen1411/strawberry-app` `main` has classic branch protection as of 2026-04-19 (per `plans/implemented/2026-04-19-branch-protection-restore.md`). Required contexts include `Playwright E2E`, `Unit tests (Vitest)`, `E2E tests (Playwright / Chromium)`, plus two more (5 total). `enforce_admins: false` — `harukainguyen1411` can UI-bypass. Only admins can read `/branches/main/protection`; non-admin accounts get 404.
- Repo convention: any workflow whose job name is a required status check MUST trigger on every `pull_request` to `main` (no `paths-ignore`) and gate work on an internal `changed` step that reports success on skip. Precedents: `myapps-test.yml`, `myapps-pr-preview.yml`. GitHub does not synthesise success for `paths-ignore` skips.
- Rule 18 — never review a PR you authored. Duong uses two gh accounts (`Duongntd` and `duongntd99`); switch with `gh auth switch --user <name>` when the author matches the active account.
- `gh pr review --approve` is GitHub-blocked when the active account authored the PR (even if you're switching between Duong's two accounts, whichever one authored the PR cannot `--approve`). Fallback: post `--comment` with explicit APPROVE verdict and note that Rule 18 still requires a non-author approval before merge. Precedent: PR #48 re-review 2026-04-19.
- Prefer `scripts/reviewer-auth.sh gh pr review ...` for ALL reviews — routes through `strawberry-reviewers` bot identity so self-review block never triggers. Preflight `scripts/reviewer-auth.sh gh api user --jq .login` should return `strawberry-reviewers`.
- Workflow-only hotfixes (`.github/workflows/**`) with `chore:` prefix sit in a Rule 13 grey zone: pre-push hook targets bug/bugfix/regression/hotfix tags, so `chore:` typically doesn't trip it. Flag as drift note; suggest `Orianna-Bypass:` trailer only if hook actually blocks. Precedent: PR #54 (2026-04-19).
- When reviewing PRs in a sibling repo, NEVER use `gh pr checkout` from inside an unrelated repo's cwd — it switches the current repo's branch, masking missing files (e.g. `scripts/reviewer-auth.sh`). Use `gh pr diff` + `gh api` for review, or clone to `/tmp` and fetch `pull/N/head` there. Precedent: PR strawberry-app#59 (2026-04-19).
- **Rule 12 xfail-first verification technique:** fetch each commit's patch separately via `gh api repos/OWNER/REPO/commits/<sha> --jq '.files[] | select(.filename==...) | .patch'`. Confirm the pre-impl commit contains an XFAIL sentinel that exits 0, and the impl commit removes that guard. Textbook shape: pre-impl has `if [ "$FAIL" -gt 0 ]; then printf 'XFAIL: <tag>\n'; exit 0; fi`; impl replaces with `[ "$FAIL" -eq 0 ]`. Precedent: PR #11 (2026-04-21).
- **When the delegation prompt flags a deviation, also audit the plan itself.** In PR #11, Talon's deviation (`agent_proxy.py` vs plan's `session_store.py`) was correct *because the plan cited a non-existent file*. The plan was the wrong one, not the PR. Always spot-check plan-cited file paths against reality before ruling a deviation as divergence. Precedent: PR #11 (2026-04-21).
- **Body-hash fidelity is the highest-signal gate for Orianna-signed plans.** When reviewing a PR that touches an Orianna-signed plan (or whose plan has `orianna_signature_approved` frontmatter), always run `scripts/orianna-hash-body.sh <plan-path>` on the PR-branch copy and compare to the `orianna_signature_approved` value. If they match, the plan body is unchanged since signing; if not, that's a structural block (silent plan drift). Precedent: PR #12 (2026-04-21).
- **Dogfooding claims in plans are easy to falsify.** When a plan's §4 Dogfooding section asserts "this plan file passes the new linter," actually stage the plan file against the new linter and run it. In PR #12 the plan claimed compliance but the new rule-4 check flagged prose references like `` `proposed/` ``. Grandfathering saved the current state but the claim was false. Always test dogfooding assertions literally. Precedent: PR #12 (2026-04-21).
- **Stash-based test baseline is unreliable for PR regression checks.** When the PR adds new tests that assume the new impl, running the test file against main via `git stash` then re-popping can misreport failures (tests fail because impl is missing, not because the test is broken). Always run tests in the PR branch's own worktree. Precedent: PR #15 (2026-04-21).
- **Bug-fix-without-plan reviews anchor to the originating plan's design-intent quote.** When a PR is dispatched as a direct fix with no standalone plan, find the originating plan's specific sentence of design intent (e.g. "hook only runs on staged diffs, so existing plans are grandfathered") and evaluate the PR against that sentence literally. No plan to align with — the quote IS the contract. Precedent: PR #15 rule-4 staged-diff scope (2026-04-21).
- **Work-concern PRs (e.g. `missmp/company-os`) — reviewer bot has no access.** `strawberry-reviewers` + `strawberry-reviewers-2` are not collaborators on work repos; `scripts/reviewer-auth.sh` returns 404 on `gh api repos/missmp/company-os`. Fallback: post a regular `gh pr comment` under `duongntd99` — plain comments on self-authored PRs are allowed, formal `--approve`/`--request-changes` are not. Preflight with `scripts/reviewer-auth.sh gh api repos/<owner>/<repo>` BEFORE drafting the review so you know the posting path upfront. Sona has been flagged to grant bot access. Precedent: PR missmp/company-os#57 (2026-04-21).
- **Plan-file-not-in-diff shortcut for signature fidelity.** If `gh pr diff N --name-only` shows the Orianna-signed plan file is NOT in the PR diff, the body-hash-vs-signature check collapses to trivially valid — skip the `scripts/orianna-hash-body.sh` comparison. Only when the plan is in-diff does signature drift become a gate. Precedent: PR #19 (2026-04-22).
- **Subsumption recommendations require reading the adjacent plan.** When a delegation prompt flags overlap between the PR's plan and a sibling plan, open the sibling and verify the sibling's task set STRICTLY supersedes the deferred tasks in scope before recommending SUBSUME. Cite specific sibling task IDs. Watch for target-path divergence (e.g. legacy `.claude/_script-only-agents/orianna.md` vs new `agents/orianna/prompts/*.md` layout) — flag as a janitorial sub-task, not a blocker. Precedent: PR #19 T-prompt-2/3 subsumed by substance-vs-format rescope (2026-04-22).
- **Textbook wave-migration PR pattern.** When reviewing "Wave N of a multi-wave plan" PRs, the cleanest fidelity signals are: (a) diff scope = single new folder/module boundary; (b) commit chain = xfail → impl → flip (Rule 12 textbook); (c) OQ resolutions cited by name in code comments (e.g. "OQ4 dual-write: S1 writer stays live until T.W3.3"). When all three hold, fidelity review collapses to a scope-creep grep and a route-coverage table against plan §2.1. Precedent: PR missmp/company-os#66 W2 route migration (2026-04-22).
- **Test-patching constraints create "dead-code-by-design" modules.** When a plan task adds a helper module (e.g. `test_results_store.py`) but the consuming routes inline the logic instead of importing (because tests need to patch module-level globals of the consuming module, not the helper), flag as drift note NOT blocker. The code is correct; the helper is reserved for a later wave's reuse (e.g. S1 writer rewire in T.W3.3). Precedent: PR #66 (2026-04-22).
- **Textbook T1..Tn plan commit-count = task-count review pattern.** When Karma-style plans declare T1 xfail → T2..Tn-1 impl → Tn flip, the fidelity review collapses to: (a) `git log` count equals plan task count; (b) first commit's patch contains `pytest.mark.xfail` citing the plan filename; (c) last commit's patch removes the xfail marker; (d) each commit's `.files[]` stays inside that task's declared file set. Rule 12 parent-SHA verification (`gh api repos/.../commits/<impl-sha> --jq '.parents[].sha'` equals T1 sha) is cheaper than fetching full patches. Precedent: PR missmp/company-os#67 preview-iframe port (2026-04-22).
- **Plan-path-prefix cosmetic drift.** Sona-delegated plans sometimes write file paths with `mmp/workspace/tools/...` prefix reflecting her workspace layout; PRs land the file at `tools/...` relative to the target repo root. Call out as plan hygiene note for future Karma plans, not as structural divergence. Precedent: PR missmp/company-os#67 (2026-04-22).
- **Docs-consistency fix reviews = four-way grep.** When a PR is a post-merge advisory to align an outlier doc with canonical phrasing (e.g. Rule 18 amendment), the fidelity check collapses to: grep the rule across CLAUDE.md, architecture/pr-rules.md, agents/memory/agent-network.md, and the file being fixed; confirm they now all agree; confirm the removed clause was genuinely tautological (not weakening the rule). Fast approval. Precedent: PR #26 three-gate → two-gate (2026-04-22).
- **Delegation prompts may omit repo owner.** Prompts sometimes name only the PR number. Initial `gh pr view N --repo Duongntd/strawberry-agents` will 404 because the repo is `harukainguyen1411/strawberry-agents`. Always run `git remote -v` first when the owner is ambiguous. Precedent: PR #26 (2026-04-22).
