# PR #41 V0.7 IB CSV — Plan Fidelity Approve

**Date:** 2026-04-19
**PR:** harukainguyen1411/strawberry-app#41
**Verdict:** APPROVE

## Scope check
V0.7 plan asks for `parseIbCsv` with same return shape as V0.6, multi-section (Trades vs Open Positions), IB-assigned trade ID. All present in `csv/ib.ts`.

## TDD Rule 12
Senna findings #1/#2 added mid-PR. Author landed xfail commit `2ac6d4b` (A.5.9–A.5.12 with `it.fails()`) BEFORE fix commits `0416baf` (short/cover) and `1239d27` (Asset Category). Correct pattern — xfail-first still enforced when in-PR feedback expands test scope, not just at task-start.

## Senna findings resolution without contract drift
- #1 short/cover: solved by `rawPayload.openClose: 'O'|'C'` — Trade type unchanged, new info rides in existing escape hatch. Good architectural decision; v0.8 position-math can distinguish without a type bump.
- #2 Asset Category: added to `REQUIRED_TRADE_HEADERS` so a schema drift surfaces as `bad_headers` instead of corrupting totals. Non-stock rows emit `unsupported_asset_category` warning and skip — safer than silently treating options as stocks.

## Pattern: Senna + Lucian loop on stacked PR
When Senna catches correctness bugs post-approval-from-Lucian, the author should (a) add xfail tests first, (b) then fix. Lucian re-review should verify ordering. Worked cleanly here.

## Follow-up deferral discipline
PR body explicitly names V0.20/DV0-4 for real anonymized IB export. Matches plan TODO. Good.
