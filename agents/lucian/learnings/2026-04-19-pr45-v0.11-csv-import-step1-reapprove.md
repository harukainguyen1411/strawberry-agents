# PR #45 V0.11 CSV Import Step 1 — reapprove after main-merge scope collapse

**Tip:** b985c68  **Verdict:** APPROVE (plan/ADR fidelity lane).

## Prior state
CHANGES_REQUESTED on scope drift — 8 ancestor tasks (V0.3–V0.10) bundled into the branch because Ekko had chain-merged feature branches into V0.11 before ancestors landed.

## Alt-path Ekko applied
Instead of cherry-picking or rebasing, Ekko waited for ancestors to land on main, then merged `origin/main` into the feature branch (065d0d8). This is the correct play under Rule 11 (never rebase) — once ancestors are in main, the merge commit reduces the *effective* diff to the unique contribution, even though the branch commit history still contains the ancestor feat commits.

## Verification technique
`git diff --stat origin/main..<tip>` is the authoritative scope signal, not `git log`. Branch log showed V0.3–V0.10 feats still present; diff-stat showed only 6 V0.11 files. Don't be misled by branch log history after a main-merge.

## Fidelity mapping
Plan §V0.11 `Outputs:` listed 4 files; PR ships those 4 + xfail test file + `useCsvParser.ts` composable. The composable isn't in Outputs verbatim but is within task intent ("Drop or paste triggers parser invocation") — treated as acceptable structural drift, not a block.

## Rule 12 check
Xfail commit 369925e precedes impl 1a6916e on the same branch. Ancestor chain stayed intact through the main-merge; TDD gate honored.

## Takeaway
When a multi-ancestor branch gets re-reviewed after its ancestors land, always re-run diff-stat vs origin/main before judging scope. Branch commit history is noisy; the diff is ground truth.
