# PR #33 — V0.3 Firestore schema + rules clean-approve

Date: 2026-04-19
PR: harukainguyen1411/strawberry-app#33
Verdict: APPROVED

## Signal
Fifth consecutive portfolio-v0 window (V0.2 → V0.9 → V0.11 → V0.3) with clean plan/ADR fidelity. Author pattern is now stable:
- xfail-first commit on same branch, both Refs <TaskID>
- Exactly the plan-listed files, nothing more
- ADR invariants (per-user isolation, trades append-only, server-only snapshots/digests) faithfully encoded in rules
- PR body links plan + declares QA-Waiver at correct gate (V0.18)

## Pattern worth flagging
`firebase.json` including `functions`+`hosting` blocks at V0.3 is a forward reference, not scope creep — those paths don't exist yet but the inert config is fine. Distinguish "inert harness config" from "implementation code that belongs to a later task" when judging scope.

## Checks taken
- Base=main, TDD ordering 13909e9 → 4ad1064, all 12 B.1.x tests present, no `allow:if true`, baseCurrency USD|EUR enforced at rule layer. No V0.4+ bleed.
