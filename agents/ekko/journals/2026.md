## 2026-04-19 — Plan promotions: stale-green-merge-gap + reviewer-identity-split (s27)

Promoted two plans from `plans/proposed/` to `plans/approved/` via `scripts/plan-promote.sh`.

- **stale-green-merge-gap**: Orianna gate passed (0 block, 0 warn). Promoted cleanly. Commit: 45e9adb.
- **reviewer-identity-split**: First attempt blocked — plan referenced `plans/proposed/2026-04-19-stale-green-merge-gap.md` (stale path after first promotion). Fixed path to `plans/approved/`, committed, re-ran. Second attempt passed (0 block, 0 warn). Commit: 4210f11.
- Both plans now at `plans/approved/`. Pushed to origin/main.

---

## 2026-04-19 — PR #25 firebase public path fix (round 3)

Single-iteration fix to drive PR #25 (`chore/p1-2-lib-sh-xfail`) to fully green CI.

- **Root cause**: `apps/myapps/firebase.json` has `"public": "dist"` but `composite-deploy.sh` assembles output into `deploy/`. The preview workflow copied firebase.json as-is, so firebase-tools looked for `dist/` and failed.
- **Fix**: Added `sed -i 's|"public": "dist"|"public": "deploy"|' firebase.json` after the copy step in `preview.yml`. Commit `4871740` on `chore/p1-2-lib-sh-xfail`.
- **Result**: All required checks green (xfail-first, regression-test, unit-tests, Playwright E2E, QA report). `E2E tests (Playwright / Chromium)` remains failing — pre-existing `auth-local-mode` heading bug, confirmed NOT a required check per branch protection rules.

---

## 2026-04-19 — PR #25/#26/#28 CI diagnosis round 2

CI unblock sweep across the P1.2 / P1.4 / P1.3 deployment pipeline PRs.

- **PR #25**: Fixed `task-list/src/router/index.ts` bare ternary (missed by prior lint-fix commit). Added `QA-Waiver` to PR body. Merged `origin/main` to resolve `read-tracker` router conflict. Branch now conflict-free; lint + unit + QA checks green.
- **PR #26**: Fixed `portfolio-tracker/src/router/index.ts` bare ternary. Synced lockfile with main to restore `@rollup/rollup-linux-x64-gnu` package entry. Relaxed vitest exact pin from `4.0.18` to `^4.0.18` to match main's resolved lockfile.
- **PR #28**: No code fixes needed — all checks pass except Firebase Hosting Preview (infra).
- **Root causes uncovered**: (a) Multiple local worktrees had unpushed commits — CI was running against stale branch state. (b) `FIREBASE_SERVICE_ACCOUNT` secret missing from repo — Firebase Preview fails on ALL PRs. (c) Pre-existing E2E failures in `auth-local-mode` tests cascade to block `Lint+Test+Build` once lint is fixed.

Open at close: E2E `auth-local-mode` failures + FIREBASE_SERVICE_ACCOUNT secret both need Duong action.

## 2026-04-18 — testing-process workstream

Critical-path unblocker session for the `testing-process` team.

- **Task #1 (TDD hooks + CI wiring)**: Delivered `pre-commit-unit-tests.sh`, `pre-push-tdd.sh`, `tdd-gate.yml`, `architecture/testing.md`, PR template Testing section. PR #149 merged.
- **Task F4 follow-up**: Documented `tools/decrypt.sh` usage in `dashboards/server/README.md`.
- **Task #8 (C2)**: Pre-commit hook wiring for dashboards-side unit tests. Fixed CWD-relative `require()` bug (absolute path via REPO_ROOT). PR #165.
- **xfail grep fix**: Updated `tdd-gate.yml` + `pre-push-tdd.sh` to recognize `it.fails` (Vitest 4.x). PR #175.
- **Azir regression blocker**: Fixed PR #165 stale base — 7 spurious file deletions. Merged origin/main, resolved MEMORY.md conflict, staged 6 untracked agent learning files. Pushed at `efef1f3`.

Open at close: PR #165 awaiting Azir re-verification + merge. PR #175 awaiting Jhin + Azir review.

---

## 2026-04-19 (s6) — Branch protection ruleset migration

Executed Camille's branch protection restore plan for harukainguyen1411/strawberry-app.

- **Ruleset API blocker**: `POST /repos/{owner}/{repo}/rulesets` requires admin. Duongntd has write-only access to strawberry-app — 404 returned (not 403). harukainguyen1411 is not in `~/.config/gh/hosts.yml`. API call cannot be made by agent. Documented manual step for Duong.
- **Scripts rewritten**: `scripts/setup-branch-protection.sh` updated in both strawberry-agents (f6a4cf7) and strawberry-app (0810bc1 on PR #50 branch). Both now use Rulesets API, bypass_mode: "pull_request", 5 required checks unchanged.
- `.github/branch-protection.json` in strawberry-app updated to document ruleset shape; classic keys retired.
- **PR #50** opened on strawberry-app (`chore/branch-protection-ruleset`). Duong or harukainguyen1411 must merge and then run `setup-branch-protection.sh` as harukainguyen1411 to activate the ruleset.
- **Plan promoted**: `2026-04-19-branch-protection-restore.md` → implemented (3cb704d).

Open at close: Ruleset not yet live — Duong must `gh auth switch --user harukainguyen1411` and run `bash scripts/setup-branch-protection.sh harukainguyen1411/strawberry-app`, then merge PR #50.

---

## 2026-04-19 (s8) — Branch protection ruleset → classic revert

**Task:** Swap harukainguyen1411/strawberry-app main from ruleset 15256914 to classic branch protection, because ruleset UI bypass is broken for `pull_request` rule type on personal repos (GitHub community discussion #113172).

**Identity confirmed:** `gh api user --jq .login` returned `harukainguyen1411`.

**Actions:**
- Deleted ruleset 15256914 (`DELETE /rulesets/15256914`). Verified `[]`.
- Applied classic protection (`PUT /branches/main/protection`): enforce_admins: false, 5 required checks (strict), 1 required review (dismiss_stale, require_last_push_approval), required_conversation_resolution: true, no force-pushes, no deletions.
- Verified: enforce_admins: false, 5 checks, 1 review — all correct.
- Rewrote `scripts/setup-branch-protection.sh` to use classic PUT path (not ruleset POST). Added comment block explaining ruleset abandonment and admin-bypass tradeoff.
- Appended Correction #3 section to `plans/implemented/2026-04-19-branch-protection-restore.md`.
- Committed (ba1def9) and pushed to main.

Open at close: None. Protection is live. PR #50 on strawberry-app is now stale/superseded — Duong should close it.
