## 2026-04-19 — PR #25/#26/#28 CI diagnosis round 2

CI unblock sweep across the P1.2 / P1.4 / P1.3 deployment pipeline PRs.

- **PR #25**: Fixed `task-list/src/router/index.ts` bare ternary (missed by prior lint-fix commit). Added `QA-Waiver` to PR body. Merged `origin/main` to resolve `read-tracker` router conflict. Branch now conflict-free; lint + unit + QA checks green.
- **PR #26**: Fixed `portfolio-tracker/src/router/index.ts` bare ternary. Synced lockfile with main to restore `@rollup/rollup-linux-x64-gnu` package entry. Relaxed vitest exact pin from `4.0.18` to `^4.0.18` to match main's resolved lockfile.
- **PR #28**: No code fixes needed — all checks pass except Firebase Hosting Preview (infra).
- **Root causes uncovered**: (a) Multiple local worktrees had unpushed commits — CI was running against stale branch state. (b) `FIREBASE_SERVICE_ACCOUNT` secret missing from repo — Firebase Preview fails on ALL PRs. (c) Pre-existing E2E failures in `auth-local-mode` tests cascade to block `Lint+Test+Build` once lint is fixed.

Open at close: E2E `auth-local-mode` failures + FIREBASE_SERVICE_ACCOUNT secret both need Duong action.

## 2026-04-18 — testing-process workstream

Critical-path unblocker session for the `testing-process` team.

- **Task #1 (TDD hooks + CI wiring)**: Delivered `pre-commit-unit-tests.sh`, `pre-push-tdd.sh`, `tdd-gate.yml`, `architecture/testing.md`, PR template Testing section. PR #149 merged.
- **Task F4 follow-up**: Documented `tools/decrypt.sh` usage in `dashboards/server/README.md`.
- **Task #8 (C2)**: Pre-commit hook wiring for dashboards-side unit tests. Fixed CWD-relative `require()` bug (absolute path via REPO_ROOT). PR #165.
- **xfail grep fix**: Updated `tdd-gate.yml` + `pre-push-tdd.sh` to recognize `it.fails` (Vitest 4.x). PR #175.
- **Azir regression blocker**: Fixed PR #165 stale base — 7 spurious file deletions. Merged origin/main, resolved MEMORY.md conflict, staged 6 untracked agent learning files. Pushed at `efef1f3`.

Open at close: PR #165 awaiting Azir re-verification + merge. PR #175 awaiting Jhin + Azir review.
