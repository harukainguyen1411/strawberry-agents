# 2026-04-19 — PR #44 V0.10 main-drift merge

## What happened

PR #44 (`feature/portfolio-v0-V0.10-base-currency-picker`) was CONFLICTING vs main after V0.3 (#33, commit `a6f95d2`) landed.

## Conflict

Single add/add conflict in `apps/myapps/portfolio-tracker/test/rules/firestore.rules.test.ts` at line 180. HEAD had B.1.13 and B.1.14 (hasOnly enforcement tests added by V0.10 Senna review). `origin/main` had nothing after B.1.12 (V0.3 didn't add those tests). Resolution: keep HEAD's B.1.13 and B.1.14 tests (V0.10 content is the superset).

## Pre-existing test failure

`emulator-boot.test.ts` — "firestore.indexes.json exists and is empty" — was already failing on both HEAD and `origin/main` before the merge. V0.3 added a `trades/executedAt` composite index to `firestore.indexes.json` while the V0.1-authored test still asserts it must be empty. This is a known pre-existing condition, not introduced by this session. CI will handle it the same way as before.

## Pre-commit hook behavior

The hook only ran tests for `functions/` sub-package (no `test:unit` script → skip). Did not run the top-level portfolio-tracker vitest, so the pre-existing failure did not block the commit.

## Push outcome

Push succeeded without tdd-gate rejection. The merge commit subject (`chore: merge origin/main ...`) passed all hook checks. No TDD-Waiver commit needed.

## CI at push time

- xfail-first: pass
- regression-test: pass
- validate-scope: pass
- QA report present: pass
- Lint+Test+Build, Unit tests, E2E, Preview: pending
