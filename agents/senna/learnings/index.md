# Senna learnings index

- 2026-04-25-pr52-heredoc-fp-two-stage-parse-approve.md — PR #52 strawberry-agents heredoc-FP fix in plan-lifecycle-guard via two-stage parse (bashlex rc=3 → conservative fallback): (1) when reviewing a fail-open/fail-closed contract change, identify the trigger condition + compare fallback's coverage to primary on adversarial patterns + characterize NEW FP regressions + verify the fallback test triggers it end-to-end (not just function in isolation); (2) `scan_conservative()` exact verb-token match makes any prefix-wrapping bypass — `(mv ...)`, `{ mv ...; }`, `$(mv ...)` all slip through inside an unparseable heredoc; (3) running heredoc-FP probes from inside strawberry-agents repo gets blocked by current main's guard — copy lib to `/tmp` and probe standalone; (4) `verb.lower().lstrip("#")` on the conservative scanner's first-token verb makes commented `#mv plans/...` (no space) FP-fire — recommend remove. Bashlex's `_lib_bash_path_scan.py` only emits exit 3 from the `bashlex.parse()` exception handler, contract well-defined. | last_used: 2026-04-25
- 2026-04-24-pr32-t-p1-12-syspath-shadow.md — `sys.path.insert(0, sibling_pkg)` inside a module shadows every subsequent bare-import name collision with the sibling; enumerate sibling file names and cross-check against imports AFTER the insert line; prefer `importlib.util.spec_from_file_location` (one-file import, no sys.path touch) or `sys.path.append` (sibling loses name collisions); module-level test-fallback stores always need autouse conftest clearing; weakening required kwargs → optional-with-default for test convenience is a prod contract regression; weak failure-path assertions (status==failed on any exception) should also check reason-code to actually exercise fault-injection contracts | last_used: 2026-04-24
- 2026-04-24-mcps-pr32-option-b-dormant-scaffold.md — dormant-scaffold PR verification protocol: grep `process.env` + `^import|^from|require(` + tsc --noEmit + scan sibling files for imports of the new module; belt-and-braces dormancy = throw-in-body + not-imported-anywhere (two speed bumps before activation); `Promise<T>` declared + synchronous `throw` in body is signature mismatch — `.catch()` chains wouldn't fire; harmless for a stub but note as a suggestion; Rule 18(a) "no checks reported" on doc-only PRs = plausible-but-surface-to-coordinator for branch-protection routing | last_used: 2026-04-24
- 2026-04-24-pr45-round2-two-new-bypasses.md — PR #45 round 2 re-review: C1/C2/C3 blocks fire on canonical shapes but two new bypasses found — (BP-1) literal-quoted `-c` arg (`git -c "user.email=X" commit`) defeats `-c[[:space:]]+user\.` regex due to quote char, (BP-2) space-separator `--author` (`git commit --author "Viktor <X>"`) defeats `--author=.*` regex. Both leak full persona author on live commit. Name-only leak via `GIT_AUTHOR_NAME=Viktor` (no email domain) is edge-of-plan-scope (plan invariant worded around `@strawberry.local`) but still surfaces in squash-UI `Co-authored-by:`. Commit-time companion (`pre-commit-reviewer-anonymity.sh`) is work-scope-only — universalising PreToolUse without universalising commit-time leaves personal-scope one-layer-deep. Human-typed commits never hit PreToolUse on either scope. Standing checklist for future flag-blocking regex reviews: test `=` form, space-sep form, quoted form, flag-before/after subcmd, env-var equivalent, `-c` equivalent. | last_used: 2026-04-24
- 2026-04-24-pr117-min-instances-review.md — missmp/company-os PR #117 single-line `--min-instances=1` deploy-flag review confirms work-scope protocol (PR comment via duongntd99, not GH review); base on feature branch → no branch protection → Rule 18(a) vacuously satisfied; heuristic: single-line fixes on shell scripts still need full-file read to verify backslash continuations not broken; call out "masks vs fixes root cause" as a non-blocking follow-up, don't over-scope an emergency ship-blocker | last_used: 2026-04-24
- 2026-04-24-pr115-demo-preview-dotenv-review.md — work-scope reviewer protocol shifted: post verdict as PR **comment** via `duongntd99` account (`gh auth switch --user duongntd99` → `gh pr comment -F body.md`), NOT `scripts/reviewer-auth.sh --lane senna`; Duong approves manually from `harukainguyen1411`; Rule 18(a) with empty statusCheckRollup = vacuously satisfied; dotenv auto-load patterns: `load_dotenv(".env.local", override=False)` is CWD-relative, bare `load_dotenv()` uses upward-search (robust); `from dotenv import load_dotenv` worth wrapping in `try/except ImportError` for install-hint consistency when file already has that pattern for other deps; sign work-scope bodies `-- reviewer` | last_used: 2026-04-24
- 2026-04-23-pr87-rereview-c1-c2-c3-cleared.md — handler-level `async with lock: await backend(...)` is canonical asyncio serialization (superior to backend-level because it protects injected backends too); lazy-init of asyncio primitives safe within single-loop asyncio only if NO `await` separates check from insert; `except FooError` scope analysis = trace back to the factory and enumerate every status/condition that raises that class — narrow `except` is safe only if factory raises that class exactly for the conditions you want to swallow; permission system correctly blocks `gh pr review --approve` from a lane that can't view the repo — file-fallback is the documented path for missmp/* reviews | last_used: 2026-04-23
- 2026-04-23-pr32-subagent-identity-propagation.md — adding a new identity source to a guard is safe IFF the new field is runtime-populated at payload top-level, never reachable from `tool_input`; verify by adversarially forging under `tool_input` and confirming the jq query ignores it; jq `//empty` handles null+missing+empty-string uniformly, no `"null"` string leak; precedence "runtime beats env" is a feature — agent cannot downgrade its own identity by leaking a different env var into its process; spoof probe set S1–S10 reusable for any `.agent_type`-style additions. | last_used: 2026-04-23
- 2026-04-23-pr31-round3-bashlex-ast-walker-gaps.md — swapping a tokenizer for an AST walker trades one bypass family for another; bashlex CompoundNode uses `.list` not `.parts` (walker that uses `getattr(node,'parts',[])` silently skips subshells + function bodies); WordNode can contain CommandsubstitutionNode + ProcesssubstitutionNode children that must be walked recursively; silent fallback to whitespace tokenizer on parse error re-introduces the very failure mode the AST was meant to fix — always fail-closed on parse error; `eval`/`bash -c`/`sh -c` requires re-parsing the quoted argument as a command (canonical static-analysis evasion); `KEY=value` prefix stripping in argument position for `dd of=`/`env FOO=` etc.; brace-expansion (`plans/{a,b}/x.md`) preserved as literal text by bashlex — walker must pre-expand; absolute-path bypass: if `is_protected_path` anchors at start-of-string, normalize must strip `$REPO_ROOT/` first. Red-team probe script at `/tmp/senna-bypass-probe.sh` covers 30+ shapes, reusable fixture. | last_used: 2026-04-23
- 2026-04-23-pr31-rereview-four-more-bash-bypasses.md — tokenizer-based Bash guards lose to an open-ended set of escapes; every fix round enumerates another. After C1–C4 closed, four more remained: bash var-assignment (`dest=$PATH; git mv src $dest`), ANSI-C quoting (`$'…'`), redirect-without-space (`>path` and `>>path` — most common Bash idiom), and APFS case-insensitivity (`PLANS/APPROVED` resolves to protected dir on macOS). Real fix: shell-parser (shlex/bashlex) operating on post-parse argv, or filesystem-layer enforcement (chattr +i). Add APFS case-insensitivity to the default bypass probe table. | last_used: 2026-04-23
- 2026-04-23-pr77-pr78-rereview-both-cleared.md — §D5 read-BC one-liner fix propagates correctly across a stacked PR when the base is refreshed via merge (Rule 11 compliant) rather than rebase; prose commit-body acknowledgements of reviewer agent-name bypass the commit-msg anonymity hook (only machine trailers are scanned) — worth flagging up the chain for strict work-scope anonymity; regression-test-for-a-one-liner-fix pattern: feed the exact terminal payload, patch module seams, assert the named field is written with the expected value — stronger than shape assertions | last_used: 2026-04-23
- 2026-04-23-pr77-pr78-factoryrunid-reader-drift.md — cross-PR reader-drift blocker class: when a PR renames/deprecates a writer field, grep for all readers in the same service — writer-side deprecation comments mean nothing if a reader still looks for the old name; unit tests that patch the I/O seam directly with a test-supplied value never exercise the session-doc→reader→seam chain end-to-end | last_used: 2026-04-23
- 2026-04-23-pr30-c1-fix-rereview-approve.md — two-phase hook split (pre-commit identity+paths / commit-msg trailer) is the correct design for any trailer-dependent authorization; when reproducing hooks locally, Duong's global `core.hookspath` silently overrides repo-local `.git/hooks` — always set `git config --local core.hooksPath "$(pwd)/.git/hooks"` in the sandbox first; test suites that pre-write `.git/COMMIT_EDITMSG` simulate a state git never actually produces at pre-commit time — the correct pattern is real `git commit` through installed hooks | last_used: 2026-04-23
- 2026-04-22-pr69-firebase-auth-frontend-review.md — review against server backbone first for frontend auth (SameSite=Strict on POST cookies materially closes CSRF surface and changes what frontend defenses are needed); popup-error fallthrough pattern that maps all unknown errors to "cancelled" masks real bugs; env-driven server policy (ALLOWED_EMAIL_DOMAIN) vs hardcoded frontend strings drift; reviewer-auth scope is `harukainguyen1411/*` only — work-concern `missmp/*` reviews always need the Yuumi fallback path | last_used: 2026-04-22
- 2026-04-22-pr27-orianna-sign-snapshot-trap-review.md — four-item checklist for reviewing universal signal-trap PRs (restore-before-reraise, success-path-disarm, re-raise via `trap - SIG; kill -SIG $$` for correct exit codes, no mutation between resource creation and trap arming); also: check every `|| true` in error-handling paths — they often mask the failures the handler exists to report | last_used: 2026-04-22
- 2026-04-22-pr26-cross-repo-workflow-three-gate-cleanup.md — doc-drift-cleanup-PR review pattern: when CLAUDE.md (canonical) and architecture/*.md (day-to-day) reference the same invariant via enumerated lists, later amendments to CLAUDE.md silently leave architecture doc with dangling restatements; sweep for "gate (c)"-style artifacts in live docs only (transcripts + pre-orianna plans are immutable history) | last_used: 2026-04-22
- 2026-04-22-pr67-demo-preview-port-main-review.md — port-plus-additions review structure (fence to additive delta, not mechanical port body); port-number-triple-alignment smell (Dockerfile ENV + code default + spec file always drift); branch-name-only deploy guard is weakest of 3 defenses (dirty-tree + unpushed-HEAD are sibling incidents); stdlib http.server `log_message` override check for dropped args + IndexError fragility; 400s excluded from logging is security observability miss when log buffer auth-gated; regression-test template-grep proof for `BAD_VALUE not in html` assertions | last_used: 2026-04-22
- 2026-04-22-pr23-orianna-speedups-fast-follow-review.md — snapshot/restore for Rule 1 must cover all abort paths (exit 2, missing-report, SIGINT) not just the xfail-reproduced one; passing xfail test can mask partial fix coverage; use `trap` after snapshot `cp` for full Rule 1 compliance | last_used: 2026-04-22
- 2026-04-22-pr19-orianna-gate-speedups-review.md — `grep -c || echo 0` produces `"0\n0"` breaking integer compares; TTY-guard `[ ! -t 2 ]` before stderr-log hijack preserves interactive UX without breaking test harnesses; pre-fix-before-check patterns need rollback on check-failure path to honor Rule 1 | last_used: 2026-04-22
- 2026-04-21-pr17-staged-scope-guard-rereview.md — test scripts using `git rev-parse --show-toplevel` bind to invocation cwd not script path; `git update-index --chmod=+x` is a superior sandbox-safe executable-bit workaround; strawberry-agents CI xfail-first/regression-test checks are metadata-only | last_used: 2026-04-21
- 2026-04-21-pr57-s3-project-reuse-s4-trigger.md — httpx `HTTPStatusError.__str__` leaks URLs with query strings; module-level test-only seams expose prod attack primitives; in-memory-stub-vs-Firestore silent 404 on Cloud Run container recycle | last_used: 2026-04-21
- 2026-04-21-sed-greedy-hunk-header-parsing.md — greedy `.*+` in sed mis-parses git hunk headers when trailing context has `+`; always anchor and test fallback paths not just the primary | last_used: 2026-04-21
- 2026-04-21-pr11-concern-root-flip.md — worktree-plus-scratch-plan probe method for path-routing changes; three-surface opt-back alignment check | last_used: 2026-04-21
- 2026-04-19-pr42-v08-post-mainmerge-reapprove.md — `accountCurrency` capture must precede TRADE_ACTIONS skip; scoped main-merge re-reviews | last_used: 2026-04-19

- 2026-04-17-dependabot-lockfile-review-patterns.md — how to sanity-check lockfile diffs without line-by-line | last_used: 2026-04-18
- 2026-04-17-preview-yml-entrypoint-gap.md — preview deploy workflow entrypoint mismatch pattern | last_used: 2026-04-17
- 2026-04-18-always-fetch-origin-before-review.md — always `git fetch origin` before reading PR branch state | last_used: 2026-04-18
- 2026-04-18-decrypt-sh-stderr-containment.md — `tools/decrypt.sh` contains plaintext to child env, never stdout/stderr | last_used: 2026-04-18
- 2026-04-18-firebase-admin-adc-vs-cert.md — ADC on Cloud Run vs `cert("")` silent failure | last_used: 2026-04-18
- 2026-04-18-firebaseauth-admin-over-privilege.md — `roles/firebaseauth.admin` not needed for token verification | last_used: 2026-04-18
- 2026-04-18-gh-actions-billing-block.md — all-red+empty-logs means billing; check billing before workflows | last_used: 2026-04-18
- 2026-04-18-noclobber-lock-race-macos.md — noclobber lock race on macOS `set -C` with background subshells | last_used: 2026-04-18
- 2026-04-18-re-review-partial-state-on-bounded-loop-exit.md — bounded-loop retry can leave partial state; re-review vigilance | last_used: 2026-04-18
- 2026-04-18-shared-account-invariant-18-blocker.md — all agents share one GH account; `gh pr review --approve` structurally impossible | last_used: 2026-04-18
- 2026-04-18-systemic-ci-failures.md — diagnose systemic CI red vs per-PR red | last_used: 2026-04-18
- 2026-04-18-timingsafeequal-bypass-pattern.md — `timingSafeEqual` defeated by `||` short-circuit on length mismatch | last_used: 2026-04-18
- 2026-04-18-vitest-it-fails-vs-it-failing.md — `it.fails` (Vitest 4) vs `it.failing` (Playwright); silent 0-test registration | last_used: 2026-04-18
- 2026-04-23-pr75-firebase-loop2c-route-migration.md — xfail PR and impl PR must be diffed against each other before approving either; route-list drift silently blocks xfail-flip step | last_used: 2026-04-23
- 2026-04-23-pr69-hotfix-rereview.md — re-review technique: git show --stat + show + read final source + targeted pytest -k to distinguish hotfix-introduced regression from pre-existing flake | last_used: 2026-04-23
- 2026-04-23-pr77-pr78-factoryrunid-reader-drift.md — when a PR deprecates a field WRITE, grep READERS of the old name across the whole service; unit tests that patch downstream consumers directly mask data-flow breaks between the session-doc read and the async-generator entry | last_used: 2026-04-23
- 2026-04-23-pr31-physical-guard-bypass-vectors.md — shell word-splitting as a guard primitive is fail-open on quoted paths, double-slash, `..` traversal, malformed JSON; always probe with a ~20-case vector table before approving any PreToolUse/sandbox guard | last_used: 2026-04-23
- 2026-04-23-pr31-round4-ast-walker-fixes-land.md — iterative security-gate approval heuristic: approve when the live bypass set collapses OR when the plan carries explicit catch-all follow-up language for the residual family ("any future AST gaps"); the difference between "silently accepted" and "deferred" is a named scope + committed escalation plan, not "we'll think later"; red-team probe scripts have arc-long value, keep stable path; `git clone --depth 30 --branch <head> /tmp/<slug>` as dirty-worktree isolation pattern; three-round CHANGES_REQUESTED → APPROVED on same lane is legitimate (GitHub keeps each review as a distinct submission) | last_used: 2026-04-23
- 2026-04-24-pr105-pr106-p1-factory-s3-stream.md — import-fallback must define `class BuildFailed(Exception): pass` not `BuildFailed = None` (else `except BuildFailed` raises TypeError); redundant `except (Specific, Specific, Exception)` tuples collapse to `except Exception`; split-work between async handler + bg task creates double-fetch + zombie Firestore records unless failure-path writes are explicit; advisory PR comments (cross-repo reviewer-auth 404) must sign `-- reviewer` with no agent names per work-scope anonymity | last_used: 2026-04-24
- 2026-04-24-pr35-t1-regex-bypass.md — PreToolUse commit-interception hook regex `git([[:space:]]+-[^[:space:]]+)*[[:space:]]+commit` misses `git -c KEY=VAL commit` and `git -C <path> commit` because those have non-dash positional args; always enumerate `-c`/`-C`/`--git-dir=`/`--work-tree=` forms when auditing commit-intercept hooks; plan-called-out attack vectors ARE the regression tests; single-source-of-truth denylist drift (lib vs T3 Python copy) is worth flagging even when lists currently match | last_used: 2026-04-24
- 2026-04-24-pr43-rule19-rereview-approve.md — hermetic test-scaffold pattern: bake `env -u VAR1 -u VAR2 ... "$@" bash "$HOOK"` into the invocation helper so outer-shell env never leaks into cases that test "all env empty" baselines; re-review hygiene: map each prior finding to a specific diff hunk, re-run the previously-failing scenario with the exact bad env, and note pre-existing unrelated failures so you don't request changes for orthogonal breakage | last_used: 2026-04-24
- 2026-04-25-pr49-coordinator-include-deadtext-wiring.md — `<!-- include: _shared/X.md -->` is NOT runtime-evaluated; it is a build-time marker for `scripts/sync-shared-rules.sh` which inlines content beneath the marker; coordinators are explicitly skipped by the drift hook (`[ -n "$concern" ] && continue`), so adding the marker without inlined content + without sync.sh ships a no-op that LOOKS shipped; structural tests that grep only for the marker line pass on this dead-text state; future shared-include reviews must verify (a) inlined content present, (b) sync.sh was run, (c) drift hook fires for the file, (d) test asserts content not just marker; plan-fidelity reviewers cannot catch this — they verify plan-vs-PR words match, but the plan itself made the wiring assumption | last_used: 2026-04-25
- 2026-04-25-pr49-coordinator-deliberation-rereview-approve.md — re-review pattern: when prior CHANGES_REQUESTED had clearly categorized severity (C/I/S) plus actionable "why" rationales, re-review can verify each finding pointwise against HEAD rather than re-reading the whole PR; for "test that inlined content matches canonical," prefer asserting via the maintenance tool's idempotency (run sync twice, expect zero changes on second pass) over re-implementing byte-compare in the test — the latter creates two sources of truth for "what counts as drift" | last_used: 2026-04-25
- 2026-04-25-pr50-worktree-hooks-propagation-test-unsoundness.md — xfail-claimed tests must actually fail against the pre-change SHA — verify by running the PR's test against the base branch HEAD, takes 30s and catches "test that doesn't test what it says" bugs (PR #50 INV-1 invoked guard directly via `bash $HOOK` rather than via `git commit`, so the test never exercised the dispatcher / `core.hooksPath` it claimed to validate; passed on pre-T2 main with exit 127 from a hook crash, mistaking crash for guard-fired); bare `[ $rc -ne 0 ]` is rarely a sound assertion — pair with grep of stderr/stdout for the expected error message or assert specific exit code; relative `core.hooksPath` silently disables hooks on branches that don't contain the dispatcher dir (git warns to stderr but does not block) — branches forked pre-merge get no coverage in their worktrees until rebased forward; migration checks against `git rev-parse --git-common-dir` are brittle from worktrees (relative `.git` from main vs absolute path from worktree) — string compares over `_old_default` defending against a non-existent state are pure cost | last_used: 2026-04-25
- 2026-04-25-pr55-watcher-arm-source-gate-comment.md — quick-lane PR review pattern: when a PR claims xfail-floor tests turn from red→green between commits, verify by checking out the T1 commit and running the suite (takes 30s) — confirms TDD discipline is real, not narrative; `run_xfail` aliasing to `run_real` (line 38 of inbox-watch-test.sh) means no test in this suite is actually skipped — always read the harness helpers before trusting an xfail label; `pr-lint-no-ai-attribution.sh` (CI Layer 3) regex flags any `Claude` token anywhere — including `CLAUDE_AGENT_NAME` env-var literals in smoke transcripts inside PR bodies — workaround is `Human-Verified: yes` trailer in PR body; literal-vs-goal directive bug pattern (PR #49 deliberation, PR #55 watcher-arm) recurs whenever a SessionStart-style hook re-emits a "do X first" directive across boundaries that preserve prior state — defense-in-depth is gate-narrowing PLUS verify-then-act prompt rewrite, not either alone | last_used: 2026-04-25
