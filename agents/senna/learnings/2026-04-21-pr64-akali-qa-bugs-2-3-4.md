# PR #64 review — Akali QA bugs 2/3/4 (missmp/company-os, work-concern)

## Verdict
Advisory LGTM via `gh pr comment` under `duongntd99` (reviewer-lane gap on missmp persists — S27 pattern, Session 34).
Comment URL: https://github.com/missmp/company-os/pull/64#issuecomment-4289513760

## Scope discipline
PR #64 is a long-lived branch (`fix/akali-qa-bugs-2-3-4`) that had accumulated ~80+ unrelated commits before Talon added the 3-bug fix pair (49cd838 xfail, c242c8e impl). Task brief said review the 2 QA commits only. I stayed in that scope and said so in the review — otherwise the "files changed" and "commits" tabs would drag in factory_v2, stop-button, archive, multi-agent-orchestration, etc. that are already-merged or separately-owned.

**Pattern: when a PR branch looks inflated, confirm the intended review scope with the two SHAs the caller named, use `gh api repos/.../commits/<sha> --jq '.parents[].sha'` to verify Rule 12 within those two commits, and explicitly scope-fence the review body. Don't expand scope to files you weren't asked about.**

## Hostname-detection adversarial check
Bug 3's detection logic:
```js
h.includes('run.app') || (h !== 'localhost' && h !== '127.0.0.1' && h !== '')
```

Traced every adversarial case the caller prompted (`localhost.evil.com`) plus my own additions (`run.app.evil.com`, `[::1]`, `0.0.0.0`). All "weird" inputs default to **prod**, which is the safe direction because `_initialMode` only selects a backend URL for a local dev/ops dashboard — not an auth boundary.

**Pattern for UI-default-selection bypass reviews: first identify what the variable is used for. If it's never an auth/authz gate and only picks a default URL/mode, even a "bypass" is cosmetic. Say so explicitly so the caller doesn't treat it as critical.**

## TODO scrub — user-visible vs developer-visible
Demo-preview main.py has 4 remaining `TODO:` strings *after* the fix. All in Python comments/docstrings inside `_fetch_config`, `_render_preview`, `preview` endpoint, and `logs` endpoint — none reach user-rendered HTML. The fix only needed to scrub the HTML template literals, not the code comments.

**Pattern: `TODO` scrub audits must distinguish output channels: (a) string-interpolated into rendered HTML/JSON → user-visible → must fix; (b) Python comments/docstrings → dev-visible only → fine. Grep alone over-reports. Trace each hit to its output channel.**

## Test-quality gradient
- **Bug 2 test**: behavioral — imports `_render_preview`, runs it, asserts `"TODO" not in html`. Strong.
- **Bug 3 test**: textual — greps dashboard.html for substrings. Weak. Passes if substrings exist anywhere, even in comments.
- **Bug 4 test**: textual — uses `html.find()` indices for ordering. Weak but robust given single `const SERVICES`.

**Pattern: HTML-file regression tests in Python are often reduced to grep because executing JS is hard without jsdom/Playwright. Accept this for regression pins; push for behavioral tests if the surface grows or the invariant becomes load-bearing.**

## Reviewer-lane gap status (Session 34)
`strawberry-reviewers-2` still has no access to missmp/*. Confirmed via `gh api repos/missmp/company-os` → 404. Posted as comment under duongntd99 per S27 established pattern.
