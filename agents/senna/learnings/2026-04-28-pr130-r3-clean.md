# PR #130 r3 — Clean, two non-blocking observations flagged

**PR:** missmp/company-os#130 — `chore: wire /v1/schema to canonical schema.yaml`
**Branch:** `feat/wire-real-schema` @ `ec70704`
**Verdict:** r3 LGTM (comment-only, no `--approve` — second-identity reviewer's job)

## What landed in r3

Three commits since r2:
- `e662f1a` — fixed I1-residue `cache-dependency-path` line 24, swapped `make lint`/`make test` for `python -m compileall .` and `python -m pytest tests/integration -q` (B2 fix)
- `dc5934c` — added `pytest>=8.0` to requirements.txt (CI exit 127 on missing pytest)
- `ec70704` — added `httpx>=0.27` (FastAPI TestClient → Starlette → httpx)

Both r2 BLOCKER (B2 missing Makefile) and r2 IMPORTANT (I1-residue) cleanly resolved.
CI lint-and-test green at HEAD.

## Non-blocking observations I flagged

1. **Test deps in prod `requirements.txt`** — pytest+httpx ship into the prod Cloud Run
   image. Acceptable at this stage; flagged for follow-up to split `requirements-dev.txt`
   or move to `[dev]` extras. Note Starlette imports httpx lazily so prod runtime cost is
   near zero — but supply-chain surface still exists.
2. **`python -m compileall .` is a syntax check, not a lint** — catches SyntaxError only,
   not undefined names / unused imports / type errors / style drift. Adequate smoke gate
   but real lint (ruff/flake8) is the obvious next step.

Both flagged as tracked-follow-up severity, not r3-blocking — comment-only verdict.

## Process note

`scripts/post-reviewer-comment.sh` for the work-concern verdict path worked clean. The
anonymity scan passed the `-- reviewer` signature without issue. Distinguishing
`--approve` (second-identity-only via reviewer-auth.sh) from `--comment` (work-concern
via post-reviewer-comment.sh) per the dispatch instruction was the key call here —
duongntd99 cannot self-approve own-authored PRs so a comment is the right vehicle.

## Comment URL

https://github.com/missmp/company-os/pull/130#issuecomment-4329108582
