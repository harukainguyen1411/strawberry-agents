# PR #127 r4 — APPROVE (B1 cleared)

**Repo:** missmp/company-os, work-concern. PR #127 head SHA `85058e1`. Verdict: **APPROVE**.

## What changed since r3

- `42f649b` — xfail T6 behavioral test (cookie POST /build → not 401; no-auth → 401).
- `85058e1` — impl: `/session/{id}/build` migrated from inline `verify_internal_secret` to `Depends(require_session_or_owner)`. Dual-auth preserved (cookie owner OR X-Internal-Secret).

## How I confirmed

1. Read `auth.py:274-294` — confirmed `require_session_or_owner` checks `verify_internal_secret` first, falls back to `require_user` → `require_session_owner` for cookies. Both legs intact.
2. TestClient empirical repro (in worktree at PR HEAD): no-auth → 401, X-Internal-Secret → passes auth seam. r3 reproduction now reverses cleanly.
3. Ran the new behavioral test against PR HEAD — `test_build_with_valid_cookie_not_401` PASSED, `test_build_without_auth_returns_401` PASSED. xfail decorators stripped in impl commit (correct — they pass now).
4. Scope hygiene: studio.js `doDeploy()` calls only `/session/<sid>/build`. No other deployBtn-adjacent endpoint needs the same migration.

## Reviewer-process generalizable rule

When a re-review clears a prior auth-seam BLOCKER, **always run the actual behavioral test against PR HEAD in a worktree** — not just read the diff. The diff can show the right `Depends(...)` injection while a missed import or shadowed name keeps the old behavior. TestClient + the test the impl claims to satisfy is the cheapest way to close the loop. (PR #127 r3 → r4 was clean; doing the empirical step costs ~30s and removes phantom-citation risk.)

## Outcome

Posted as comment under `duongntd99` via `scripts/post-reviewer-comment.sh`. Comment URL: https://github.com/missmp/company-os/pull/127#issuecomment-4327931062.

B1 cleared. PR ready to merge once required reviewers approve (Rule 18 still requires non-author approval).
