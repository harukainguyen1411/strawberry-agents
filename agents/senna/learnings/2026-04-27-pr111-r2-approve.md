# Learning — PR #111 R2 re-review: APPROVE

**Date:** 2026-04-27
**PR:** https://github.com/harukainguyen1411/strawberry-agents/pull/111
**Verdict:** APPROVE (2 NITs, no blockers)
**Head SHA reviewed:** `83b8590a`

## What happened

R1 surfaced 4 IMPORTANT findings (F1 wrong delineator, F2 synthesized fixtures,
F3 duplicate fixture, F4 exit-code-only assertions). R2 verified the repair held
against real evidence rather than just synthesized inputs.

The repair was honest: empirical re-sampling of `~/.claude/projects/**/*.jsonl`
(8001 lines, 8 large transcripts) confirmed Talon's `is_genuine_turn_boundary()`
predicate matches the categorization the bug fix claims:

- 977 loopback (`type:user` + content list with tool_result blocks) — within-turn, skip
- 424 genuine boundaries (383 str content + 41 list w/o tool_result) — stop here
- 0 `hook_event_name:UserPromptSubmit` in transcript JSONL (dead branch correctly removed)

xfail-then-pass ordering verified by checking out 6f667f82 (xfail) → both cases XFAIL,
then 83b8590a (impl) → both cases PASS.

## Pattern to repeat

**For re-reviews of empirically-driven fixes: re-run the exact same empirical
sample I used in R1.** I caught F1 in R1 by sampling real transcripts; in R2
I re-sampled to verify the fix's distribution claim matched what I had seen.
This took <2 minutes and converted "trust the commit message" into "verified
against the same evidence I used to file the finding." Cheap, conclusive.

**For xfail-first ordering on PRs that bundle xfail and impl in two commits:**
checkout the xfail commit alone, run the test, confirm XFAIL. Then checkout
impl, run again, confirm PASS. This is cheap (two `git checkout`s in a clone
worktree under /tmp) and proves the test would actually fail without the fix —
the strongest form of regression-test honesty.

## Edge-case probes

- Empty transcript: parser returns `[]`, hook warns. Conservative — fine.
- Zero genuine boundaries (only loopbacks): walks entire history. Sensible.
- Orphan fixture (`teammate-idle-real-transcript.jsonl`): no test references
  it after the F2 replacement moved coverage to `teammate-idle-real-shape.jsonl`.
  Filed as NIT — non-blocking housekeeping.

## Files referenced

- `scripts/hooks/posttooluse-teammate-idle-marker.sh:90-110` (is_genuine_turn_boundary)
- `tests/hooks/test_teammate_idle_marker_senna_findings.sh` (xfail scaffold)
- `tests/hooks/fixtures/teammate-idle-real-shape.jsonl` (real captured fixture)
- `tests/hooks/fixtures/teammate-idle-real-transcript.jsonl` (orphan after F2)
