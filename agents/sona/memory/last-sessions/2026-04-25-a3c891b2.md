# Handoff shard — 2026-04-25 (a3c891b2, hands-off normal track, second post-compact leg)

Session ID: c1463e58-16f0-4a89-b958-ffc669430619
Continuation of: 2026-04-25-f993d23d.md

## Active state at compact boundary

**Mode:** hands-off normal track (continuing from first leg correction).

**All subagents returned — nothing in flight at this boundary:**
- Talon PR #54 (T-new-D canonical Slack wrapper for work-repo) — MERGED at `5081069`-equivalent merge commit as duongntd at 06:57:08Z. Third attempt after PR #48 (wrong codebase) + PR #33 (wrong scope) closures. Four smoke assertions pass. Three non-blocking polish items folded as future-work.
- Akali QA on PR #32 (company-os) — RETURNED with FAIL verdict. Two blockers found: F1 (factory_bridge wired to v1 scaffold; `tool_dispatch.py:127` imports wrong module + v1 approved-status guard has no UI route + v1 has unimplemented TODO for factory_client.start_build) and F2 (.env.local baked into prod Docker image hardcoding `__s5Base = http://localhost:8090`). PR #32 cannot merge until F1+F2 fixed and re-QA'd.
- Yuumi — pending dispatch to commit Akali's QA findings to `assessments/qa-reports/` (Akali hard rule blocked her from writing the .md herself).

**Two queued dispatches not yet fired at compact boundary:**
1. F1+F2 fix on demo-studio-v3 (Viktor or Karma quick-lane — design call on F1 still pending Duong: delete approved-status guard vs add UI approve button)
2. Yuumi to write Akali QA findings as `assessments/qa-reports/`

## What landed this leg

- **T.P1.14 Ekko deploy** — LANDED clean. All five Cloud Run revisions green: demo-factory-00012-9mg (FACTORY_REAL_BUILD=1 confirmed), demo-studio-00029-8bk, demo-config-mgmt-00014-2bn, demo-studio-verification-00005-756, demo-preview-00010-ff4. No rollbacks fired.
- **T-new-D canonical Slack wrapper (PR #54)** — SHIPPED after two prior closed PRs (#48 wrong codebase, #33 wrong scope). Senna + Lucian both formal-APPROVE. Merged as duongntd. Three non-blocking polish items noted: dead grep in assertion d, bats skip weakness in xfail body, "or absent" fallback.
- **Self-invite SuperAdmin ADR archived** — `plans/archived/work/2026-04-24-self-invite-to-walletstudio-org.md` via Orianna `3b69faf1`. Replacement runbook at `assessments/work/2026-04-25-superadmin-self-invite-runbook.md` (commit `b232a697`).
- **Secretary ADR promoted approved → in-progress** — `baf56941` per Karma revised plan `70d275f9`. Universal scope rule (wrapper-not-modify) now in plan §0.
- **Swain corrected SuperAdmin path** — `Config.SuperAdmin` argocd email list is NOT a global rego override. OPA input builder at `core/tse/authz/opa.go:159-177` never injects it; rego consults only `input.user.org_roles[org_id]` per `authz/rego/main.rego:21-23`. Duong's case is bootstrap-from-zero (no existing SuperAdmin/OrgOwner in target org). Only path under "no tse change" constraint is direct DB write to `users_orgs`. Learning at `agents/swain/learnings/2026-04-25-config-list-not-rego-input.md`. Runbook needs supplemental "Bootstrap-from-zero" section once Duong confirms prd DB access.
- **Slack-ping before compact** — `mcp__slack__notify_duong` fired successfully (ts `1777101622.324479`). Corrected prior inversion: remote-control means user is NOT at CLI keyboard, so Slack ping is MORE appropriate, not less.

## Critical corrections to carry forward

- **SuperAdmin two-call flow is wrong framing for Duong's case.** Requires caller to already hold SuperAdmin or OrgOwner. Duong's case is bootstrap-from-zero. Direct DB write to `users_orgs` is the only path under no-tse-change constraint. Runbook supplement needed.
- **Skarner's quote dig confirmed:** Duong's original phrasing was "SuperAdmin account that can invite users regardless if they belong into an org" — cross-org override intuition correct, bootstrap-from-zero implicit.
- **Universal scope rule (wrapper-not-modify) is now non-negotiable.** PRs #48 + #33 closed for scope error class. Plan §0 of secretary ADR makes this explicit.

## Blockers awaiting Duong

- **F1 design decision:** delete approved-status guard vs add UI approve button. F1+F2 fix cannot be dispatched until Duong calls F1 design.
- **prd DB access confirmation** for SuperAdmin bootstrap-from-zero runbook supplement.
- **PR #2108 → #2109** still pending Duong harukainguyen1411 approve + merge (TSE self-invite chain).

## Open threads into next session

- **RUNWAY Wave D — re-blocked on F1+F2:** Akali QA returned FAIL. After Duong calls F1 design → dispatch Viktor or Karma for F1+F2 fix on demo-studio-v3 → Ekko redeploy → Akali re-QA → PR #32 merge. Yuumi dispatch for QA report write still queued.
- **SuperAdmin bootstrap-from-zero:** Runbook at `assessments/work/2026-04-25-superadmin-self-invite-runbook.md` needs "Bootstrap-from-zero" supplement. Gate: Duong confirm prd DB access.
- **Secretary ADR T-new-D COMPLETE; P1-T2 next:** Slack age blob provisioning + .mcp.json registration when Duong confirms direction. T-new-C (multi-var decrypt.sh extension, Syndra+Heimerdinger) and T2 (Slack migration) are the next parallel dispatches.
- **TSE self-invite ADR:** PRs #2108 + #2109 awaiting Duong manual approve+merge. After merge: resume Swain #108 + Viktor T3 (MUST include `admin.POST("/invite-user-to-org", SuperAdminInviteUserToOrg(a))` route registration in `core/tse/api/v3/api.go`).
- **Coordinator deliberation primitive:** PR #49 merged (`7cb7fb07`) — applies to `_shared/coordinator-intent-check.md` on next fresh session boot.

## Notes for next instance

- Inbox-watch: re-arm immediately on resume — `CLAUDE_AGENT_NAME=sona bash scripts/hooks/inbox-watch.sh`.
- Evelynn FYI inbox `20260425-0712-101144.md`: Slack-ping Duong via `mcp__slack__notify_duong` BEFORE `/pre-compact-save`. This has been applied correctly this leg.
- PR #32 (company-os) scope note: PR #32's own scope (config-mgmt + dashboard) does NOT touch F1+F2 broken code. The merge gate unmet is purely E2E pass requirement. Akali QA findings must go into assessments/qa-reports/ via Yuumi before F1+F2 fix dispatch.
- Anti-AI-attribution correction stays LOCAL to strawberry-agents only. Do NOT port to missmp/* repos.
