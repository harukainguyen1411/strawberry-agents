# Handoff Shard — 2026-04-24-4eb1eb78

**Session ID:** 84b7ba50-c664-40d8-9865-eb497b704fb3
**Date:** 2026-04-24 (compact boundary — second consolidation this session)
**Consolidated by Lissandra (pre-compact)**
**Concern:** work
**Prior shard this session:** 2026-04-24-9b238384 (first consolidation)
**Prior full session close:** 2026-04-23-536df25c

---

## What happened since last compact

This is the second half of session `84b7ba50`. The first half (shard `9b238384`) covered TOCTOU/S2 plan flips, P1 chain setup, W3 xfails, and Viktor W3 impl dispatch. This shard covers everything that followed Viktor's return.

**Conflict-resolve wave — PRs #105 + #106 re-conflicted:**
- After PRs #103 + #104 landed on `feat/demo-studio-v3`, both #105 (T.P1.5b) and #106 (T.P1.7) had conflicts.
- Talon round 1: resolved both via `git merge` (no rebase, Rule 11 followed). Took base-parity on stale `@P1_XFAIL` decorators. Both merged.
- PR #106 re-conflicted a second time after #105 landed; Talon round 2 resolved.

**W3 PR #107 shipped:**
- Viktor W3 impl at commit `2a10732`. Rakan xfails gate at `5a8ad11`.
- Lucian plan-fidelity clean with 3 drift notes: D-1 plan-slug typo fixed in PR body; D-2 SYSTEM_PROMPT crossing wave boundaries noted (non-blocking); D-3 LWW race intentional.
- Senna found 2 critical + 2 important blockers:
  - SSE `status` event silently dropped by `_VANILLA_APPROVED_EVENTS` filter
  - ADR prose/impl shape mismatch
  - Wrong `error_code` on non-ValidationError force-retry
  - Agent-prompt loop risk
- Viktor `51a39e2` fix: added `"status"` to allowlist; full `{status, phase, sessionId, configVersion}` shape emitted; `error_code` dispatched by exception type; SYSTEM_PROMPT hard-rule 5 added for `force_applied: True` termination.
- PR #107 merged.

**Orianna plan flips — both implemented:**
- S2 patch-drift plan: proposed/in-progress → implemented (commit `d4112dd8` on main)
- TOCTOU I1 plan: proposed/in-progress → implemented (commit `4fa6ef8b` on main)

**T.P1.12 cleanup — PR #109:**
- Talon stripped 10 W3-gated xfails + `_w3_impl_present()` helper now W3 is live.
- Restored `strict=True` on P1_XFAIL marker (Lucian nit from #105 review).
- 2 files, 97 deletions, 2 insertions.
- Lucian CLEAN (one drift note: PR body mislabeled as T.P1.12 P1-factory, actually W3.T8 cleanup).
- Senna LGTM with 2 non-blocking suggestions (stale module docstring; `P1_XFAIL` dead code).
- PR #109 open, ready to merge.

**Local W3 testing — end-to-end verified:**
- Server on :8080 initially broken: 5 commits behind origin + 3 competing uvicorn processes.
- Killed stales, pulled origin. Then `Auth not configured` — Firebase vars in `.env.local` but not `.env`.
- Root cause for `set_config` silently failing: local S2 stub at `tools/demo-config-mgmt/main.py` reads `initialConfig` (pre-W3) and is create-only.
- Duong scope decision: no fix to S2 stub, fix S1 to fall back to deployed S2.
- Solution: comment out `CONFIG_MGMT_URL=http://localhost:8002` and `CONFIG_MGMT_TOKEN=dev-token` in `.env.local`; fall back to `.env` prod URL (Cloud Run `demo-config-mgmt-4nvufhmjiq-ew.a.run.app`).
- Deployed S2 confirmed: accepts `{sessionId, config}`, validates, `?force=true` bypasses + upserts with version bump — full W3 contract.
- Restart with both env files sourced. Duong confirmed W3 write path works end-to-end.
- Preview staleness observed separately — out of scope, covered by existing plan `2026-04-22-preview-iframe-staleness-triage.md`.

**SECURITY INCIDENT — transcript secret leak:**
- I `cat`-ed `.env.local` for debugging, exposing Anthropic API key, Firebase Web API key, CONFIG_MGMT_TOKEN, INTERNAL_SECRET, DEMO_STUDIO_MCP_TOKEN, WALLET_STUDIO_API_KEY into the live JSONL.
- Yuumi round 1: scrubbed 43 occurrences in JSONL + 54 task-output files.
- Skarner audit found 2 misses: `INTERNAL_SECRET=fb5c824...` in `agents/sona/transcripts/2026-04-21-0cf7b28e.md` line 10137 (pre-existing) and `Authorization: Bearer b812740...` in `agents/sona/transcripts/2026-04-17.md` line 398 (pre-existing), both echoed into current JSONL.
- Yuumi round 2: scrubbed both transcripts + committed `74632bfc` to main.
- Yuumi round 2b: scrubbed 8 residual `fb5c` + 6 residual `b812740` lines from live JSONL.
- Both secrets are in git history (commit `0c2c5362`). **Duong MUST rotate `INTERNAL_SECRET` + `CONFIG_MGMT_TOKEN` on GCP side — action pending.**
- ANTHROPIC_API_KEY, FIREBASE_WEB_API_KEY, WALLET_STUDIO_API_KEY, DEMO_STUDIO_MCP_TOKEN from `.env.local` cat: gitignored (not in history) but were in JSONL; now scrubbed, backups at `~/.claude-transcript-backups/`.
- Scrub hardening recs: add `INTERNAL_SECRET=[a-f0-9]{40,}` + `Authorization:\s*Bearer\s+[a-f0-9]{40,}` to default pre-scrub regex list. Add rule: agents must never transcribe `.env` file contents verbatim into session summaries.
- compact-excerpt deferred: `scripts/clean-jsonl.py` `--since-last-compact` flag not confirmed available.

---

## Active threads at compact boundary

- **PR #109 — Senna LGTM + Lucian CLEAN.** Ready to merge. Waiting on Duong web-UI approve.
- **Secret rotation CRITICAL:** `INTERNAL_SECRET` + `CONFIG_MGMT_TOKEN` in git history (commit `0c2c5362`). Duong must rotate on GCP side.
- **Secret rotation (session leak, history-clean):** `ANTHROPIC_API_KEY`, `FIREBASE_WEB_API_KEY`, `WALLET_STUDIO_API_KEY`, `DEMO_STUDIO_MCP_TOKEN` — still need rotation.
- **Yuumi hardening recs** — regex list expansion + no-.env-transcription rule — not yet implemented.
- **Preview iframe staleness** — existing plan `2026-04-22-preview-iframe-staleness-triage.md`, separate track.
- **Local S2 stub divergence** — residual candidate for `assessments/work/`.
- **PR #32 god-PR** — hold until full P0+P1+P2 chain.
- **Plan flips (P1 umbrella + W4/W5 config-architecture ADR)** — both stay in-progress, no flip needed.

---

## Blockers

1. **CRITICAL: Secret rotation.** `INTERNAL_SECRET` + `CONFIG_MGMT_TOKEN` are in git history. Human action by Duong required to rotate on GCP side.
2. PR #109 needs Duong web-UI approve to merge.
3. Reviewer-auth cross-repo gap persists — both `strawberry-reviewers` + `strawberry-reviewers-2` return 404 on `missmp/company-os`. All reviews posted as advisory comments via executor auth.

---

## What the next instance needs to know

- W3 is live and verified end-to-end by Duong. `feat/demo-studio-v3` HEAD is W3-complete.
- The local S2 stub (`tools/demo-config-mgmt/main.py`) is pre-W3 (reads `initialConfig`, create-only). For local dev, comment out `CONFIG_MGMT_URL` + `CONFIG_MGMT_TOKEN` in `.env.local` and fall back to the deployed S2 prod URL.
- Both `.env` and `.env.local` must be sourced for local server startup (`set -a && source .env && source .env.local && set +a`).
- PRs #105 + #106 are MERGED. PR #107 is MERGED. PR #109 is open and ready to merge.
- Talon's conflict-resolve wave took base-parity on stale `@P1_XFAIL` decorators — this is correct; those tests are now passing and the `@P1_XFAIL` decorators were removed in PR #109.
- The INTERNAL_SECRET and CONFIG_MGMT_TOKEN rotation is a P0 human action. Do not proceed with any GCP operations before Duong confirms rotation.
- Yuumi's round 2b scrub covered the live JSONL; backups are at `~/.claude-transcript-backups/`.

---

## Open threads into next session

- **CRITICAL: Secret rotation** — INTERNAL_SECRET + CONFIG_MGMT_TOKEN on GCP (git history leak). Duong action required.
- **Secret rotation (session leak)** — ANTHROPIC_API_KEY, FIREBASE_WEB_API_KEY, WALLET_STUDIO_API_KEY, DEMO_STUDIO_MCP_TOKEN. Less urgent (history-clean) but still needed.
- **PR #109** — Duong merge gate (Senna LGTM + Lucian CLEAN)
- **Yuumi scrub hardening** — add `INTERNAL_SECRET` + `Authorization: Bearer` regex patterns to default scrub list; enforce no-.env-transcription rule
- **Skarner hardening rec** — rule that agents must never transcribe `.env` file contents verbatim into session summaries
- **Local S2 stub divergence** — document in `assessments/work/` as a residual (separate from deployment)
- **W4 + W5 config-flow** — dispatch window opens now that W3 is merged
- **PR #32 god-PR** — hold
- **Preview iframe staleness** — existing plan, separate track
- **Reviewer-auth cross-repo gap** — standing; third identity or org-access grant needed
- **Zombie Firestore records** — gated behind `FACTORY_REAL_BUILD=1` toggle
- **38 stale worktrees on company-os** — non-urgent Ekko task
- **AI-coauthor hook port to company-os** — Evelynn's lane, follow up
- **Identity-leak fix** — Evelynn inbox thread, her call
