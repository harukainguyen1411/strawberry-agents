# Last session — 2026-04-22 (session 0cf7b28e, overnight ship, pre-compact shard 9 — ninth leg)

**Incremental shard.** Earlier legs documented in:
- `agents/sona/memory/last-sessions/2026-04-21-a0893a81.md` — first half (signing wave, ADR approved)
- `agents/sona/memory/last-sessions/2026-04-21-17a90992.md` — second half (impl wave, integration branch)
- `agents/sona/memory/last-sessions/2026-04-21-a0a51dd8.md` — third leg (ship-day fastlane, deploy infra B1/B4/B5)
- `agents/sona/memory/last-sessions/2026-04-21-4c6f055d.md` — fourth leg (architecture pivot, Explore audits, god plan v2 + Option B)
- `agents/sona/memory/last-sessions/2026-04-21-3f9a8c58.md` — fifth leg (Option A confirmed, 5-plan promo chain, Wave 1 dispatched x3)
- `agents/sona/memory/last-sessions/2026-04-21-da7d5b12.md` — sixth leg (Wave 1 merged, PR review wave, Swain Option B, Viktor Wave 2 dispatched)
- `agents/sona/memory/last-sessions/2026-04-21-c83020ad.md` — seventh leg (Wave 2 S1-new-flow PR #61 merged, deploy PR #63 merged, Akali e2e QA in flight, Option B promote complete)
- `agents/sona/memory/last-sessions/2026-04-22-9835724c.md` — eighth leg (pivot to Option B as primary, compass committed, Aphelios queued)

This shard covers the ninth leg — Option B ship execution: Rakan xfails, Viktor impl Waves 1–5, Vi integration NO-GO + Viktor-3 blocker cleanup, Ekko prod deploy, Senna NO-GO + Lucian GO, request_id leak root cause discovered, Viktor hotfix + Akali parallel QA dispatched.

## What happened (ninth leg)

**Option B dispatch chain executed (serial):**

Following the compass (`assessments/work/2026-04-22-overnight-ship-plan.md`) and eighth-leg queue, the Swain Option B vanilla-API ship proceeded:

1. **Rakan** — xfail skeletons committed per TDD gate. Xfails cover native chat loop, config-tool writes, preview render, factory trigger + verification.
2. **Viktor Waves 1–4** — native chat loop implemented. No managed agent, no MCP server. `POST /session/new` refactored, `/chat` handler refactored, config tools writing config, preview + factory + verification wired.
3. **Viktor-2 Wave 5** — Wave 5 completed, shipping the full Option B feature surface.
4. **Vi integration** — test integration run returned **NO-GO**: 7 blockers surfaced. Blocking issues not enumerated by caller but sufficient to halt deploy.
5. **Viktor-3 blocker cleanup** — returned **GO**: all 7 Vi blockers resolved.
6. **Ekko deploy** — deployed to prod revision `demo-studio-00023-hjj`. Deploy succeeded.

**Senna PR review — NO-GO:**

Senna reviewed the Option B PR and returned NO-GO:
- **C1 (CRITICAL):** auth-bypass — `POST /session/new` / `/chat` accessible without authentication check.
- **C2 (CRITICAL):** multi-turn context leak — session context not properly scoped between users/turns.
- **6 HIGH findings** — additional non-critical but serious issues.

**Lucian PR review — GO:**

Lucian reviewed plan fidelity and returned GO. Plan-to-impl alignment is clean.

**Root cause discovered — request_id leak:**

Duong reported a request_id leak. Root cause identified:
- `POST /session/new` still calls `create_managed_session()` and writes `managedSessionId` to the session record even after Option B refactor.
- `/chat` handler reads `managedSessionId` and branches to the managed-agent path whenever the field is present.
- Effect: ALL chat requests route through the managed-agent code path despite Option B intending pure vanilla-API. The managed agent is still active in the hot path.
- This is also the mechanism behind Senna's C1/C2 findings — the auth-bypass and context leak are in the managed-agent code path, not in the new vanilla-API code.

**Viktor hotfix — in flight:**

Viktor (agent `a12c50af11f160a10`) dispatched to fix the root cause: strip `create_managed_session()` call from `POST /session/new`, remove `managedSessionId` write, ensure `/chat` routes purely through vanilla-API path. Hotfix in progress at consolidation boundary.

**Akali-A parallel QA — in flight:**

Akali-A (agent `a0754360a2719e79f`) dispatched for session lifecycle QA — parallel with Viktor hotfix. Duong requested parallel QA across multiple aspects. Mid-dispatch when `/pre-compact-save` fired. Additional Akali dispatches may be pending.

## Active in-flight agents at consolidation

- **Viktor** (`a12c50af11f160a10`) — hotfix: strip `create_managed_session()` + `managedSessionId` from `POST /session/new` / `/chat` managed-path branch.
- **Akali-A** (`a0754360a2719e79f`) — session lifecycle QA.
- Possible additional Akali instances dispatched mid-session; check inbox / subagent results on resume.

## Open threads into next session

1. **Viktor hotfix — await result** — hotfix for `create_managed_session()` + `managedSessionId` leak. Critical; blocks all prod correctness. After return: re-run Senna review against hotfix diff.

2. **Akali QA results — await result** — session lifecycle QA (Akali-A `a0754360a2719e79f`). May be additional Akali instances. Collect all results before merging.

3. **Senna re-review after hotfix** — C1 auth-bypass + C2 multi-turn findings were rooted in managed-agent path. Re-run Senna after Viktor hotfix lands to confirm C1/C2 resolved. 6 HIGH findings also need resolution or documented deferral.

4. **Parallel QA scope** — Duong requested multi-aspect parallel QA. Confirm all intended aspects are covered and dispatched. Aspects not covered by Akali-A need separate Akali dispatches.

5. **Ekko redeploy after hotfix** — prod revision `demo-studio-00023-hjj` has the pre-hotfix code. After Viktor hotfix + Senna GO: redeploy to a new prod revision.

6. **Rakan xfails — verify no skips** — TDD gate: confirm all Rakan xfail skeletons are now matched by Viktor impl commits. No orphaned xfails in the branch.

7. **PR merge gate** — PR not mergeable until Senna returns GO on hotfix diff. Rule 18: one approving review from non-author (Senna or Lucian); Lucian GO is on record but applies to pre-hotfix plan fidelity. Re-check after hotfix.

8. **Swain Option B plan signature** — verify `scripts/orianna-verify-signature.sh` against current plan body after hotfix diff. Hotfix may touch plan-tracked surfaces.

9. **Legacy MCP Cloud Run retirement** — deferred until native chat proves stable. After hotfix + Akali QA green, decommission `demo-studio-mcp`.

10. **Azir god plan Orianna signature** — still invalidated (Xayah edit `79e73cc`). Non-blocking for Option B, but note for future Option A work.

11. **Idle-threshold env var discrepancy** — 55/60 min shipped vs 60/120 min ADR spec. Dormant until `MANAGED_SESSION_MONITOR_ENABLED=true`.

12. **xfail debt — MAD.B.5 + MAD.F.1** — carried forward. No action until integration env available.

13. **PR #58 (demo-preview-v2)** — do not merge; scope conflict. Duong decision pending.

14. **Branch protection — main** — payload ready, not applied. Duong action when available.

15. **Duong Discord availability** — channel `1489550059547787325`. Intermittently online. Post blockers there if Senna finds new CRITICALs after hotfix.

## Key facts for next instance

- Compass file: `assessments/work/2026-04-22-overnight-ship-plan.md`. Re-read after every compact.
- Primary plan: `plans/in-progress/work/2026-04-21-demo-studio-v3-vanilla-api-ship.md` (Swain Option B).
- ROOT CAUSE: `POST /session/new` calls `create_managed_session()` + writes `managedSessionId` → `/chat` branches to managed-agent path. Viktor hotfix in flight.
- Prod revision at consolidation boundary: `demo-studio-00023-hjj` (pre-hotfix, has the bug).
- Viktor agent ID: `a12c50af11f160a10` (hotfix). Akali-A agent ID: `a0754360a2719e79f` (session lifecycle QA).
- Senna: NO-GO (C1 auth-bypass, C2 multi-turn, 6 HIGHs). Lucian: GO.
- Serial dispatch is still the baseline; parallel QA is a Duong-directed exception for this specific window.
- Workspace: `~/Documents/Work/mmp/workspace/company-os/tools/demo-studio-v3/`, branch `feat/demo-studio-v3`.
- Companion services in `europe-west1`, project `mmpt-233505`.

## Directives locked (carried forward)

- Hands-off until Duong says stop.
- `git pull` first on all target repos before impl delegation.
- gh executor identity: `Duongntd`. Confirm before any impl delegation.
- Serial dispatch default; parallel only as Duong directs.

## compact-excerpt deferred

Transcript excerpt deferred — `scripts/clean-jsonl.py --since-last-compact` support not confirmed available.
