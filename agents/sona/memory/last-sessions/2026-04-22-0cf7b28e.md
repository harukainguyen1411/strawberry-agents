# Session handoff — 2026-04-22 (cli, direct mode)

Short-UUID: `0cf7b28e` (from transcript `agents/sona/transcripts/2026-04-21-0cf7b28e.md`)

## What happened

Full direct-mode execution session per Duong's standing cron directive (58158480): YOU DO EVERYTHING YOURSELF, no subagents. Playwright-test → inspect → TDD xfail → fix → re-verify loop against `demo-studio-v3` chat flow on `localhost:8088` (8080 had orphan uvicorn workers the sandbox refused to kill). Shipped two coupled fixes on `feat/demo-studio-v3`:

1. **SSE deadlock inversion** — `/chat` now spawns `run_turn` directly into per-session queue; `/stream` is a pure consumer. Retired `_vanilla_pending`. Three tests in `test_chat_sse_handshake.py` green (T1 run_turn-without-stream, T2 exactly-one-per-chat, T3 stream-before-chat handshake).
2. **Chat bubble rendering** — `_renderTextEvent` now reads `data.text` (was `data.content`); module state `currentAssistantNode` + `currentAssistantText` accumulates fragments; `tool_use`/`turn_end`/`cancelled` listeners reset the accumulator. Four tests in `test_chat_text_delta_rendering.py` promoted from xfail to passing.

Playwright verified live on two fresh sessions (Aviva + Lemonade). Screenshots under `assessments/qa-reports/2026-04-22-chat-bubble-render-live*.png`.

## Mid-close misroute — worth flagging hard

I started the close as Evelynn (compaction-summary inheritance) despite the session being entirely work-concern. Duong caught it, we reverted and redid under Sona. Full postmortem in `assessments/work/2026-04-22-coordinator-identity-misroute-feedback.md`. Class of bug — will recur if resume-from-compact doesn't re-derive coordinator from concern signals.

## Open threads into next session

- **Preview iframe staleness** — `demo-preview` remote service renders Allianz regardless of S2 state. Separate bug from chat; S2 appears to seed new sessions with Allianz template default. Next iteration.
- **Deploy → S3 trigger_factory** — Duong's standing directive includes "trigger_factory kicks S3, verification readable" as still-pending. Not touched this session.
- **Whitespace concat artifacts** — `brandand`, `Brandcolors`, `Allfields` cosmetic bugs where Anthropic splits mid-word and studio.js concats verbatim. Not fixed; not a ship-blocker.
- **Coordinator-identity misroute** — feedback written; no plan yet. Worth commissioning Swain or Karma for a concern-check-on-resume mechanism.
- **FastAPI Dependant graph contamination** — learning written; pattern committed in `test_chat_sse_handshake.py:_restore_clean_main`.

## Dangling commits / PRs

- `feat/demo-studio-v3` has 4 pushed commits covering the chat-bubble + SSE work; no PR opened yet.
- No PRs authored this session.
- Working tree is dirty with accumulated untracked artifacts (screenshots, fact-check files, qa-reports, learnings from Ekko/Senna) — not this session's responsibility but worth a sweep.

## Blockers

None for next session. Chat rendering works end-to-end. Duong ended with "Good progress" — shipping cadence was the signal.
