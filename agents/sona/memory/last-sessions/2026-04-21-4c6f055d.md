# Last session — 2026-04-21 (session 2, ship-day, pre-compact shard 4 — fourth leg)

**Incremental shard.** Earlier legs documented in:
- `agents/sona/memory/last-sessions/2026-04-21-a0893a81.md` — first half (signing wave, ADR approved)
- `agents/sona/memory/last-sessions/2026-04-21-17a90992.md` — second half (impl wave, integration branch, plan-in-progress signs)
- `agents/sona/memory/last-sessions/2026-04-21-a0a51dd8.md` — third leg (ship-day fastlane, deploy infra B1/B4/B5)

This shard covers the post-compact architecture pivot leg — from the fourth compact boundary onward.

## What happened (fourth leg)

Session opened with Duong trying to validate the product locally. I provided a local-run recipe: pull `.env` from Bitwarden, run `scripts/run-backend.sh`, set the new feature-flag env vars (MAD flag, etc.). First attempt failed immediately: the agent screenshot showed "Config Management service is currently unreachable." I traced the failure to `demo-studio-mcp` Cloud Run returning 503 — source project `ds-v3-workspace-2026` was deleted per `docs/cloud-run-config-snapshot.md`. This is a pre-existing infra gap, not a ship-day regression.

Duong pushed on the architecture: "so we need a demo-studio backend AND an mcp server?" and "Why? why can't our BE be the mcp server?" I presented three options honestly:
- **Option A**: Keep managed + merge MCP into S1 process (MCP runs in-process, no separate Cloud Run service)
- **Option B**: Ditch managed agent → vanilla Messages API, client-side tools, synchronous loop
- **Option C**: Status quo + redeploy MCP to a new project

Duong chose Option A immediately: "ok let's still try A first." I dispatched Karma #59 to fast-lane the MCP-merge plan at `plans/proposed/work/2026-04-21-mcp-inprocess-merge.md`.

Duong then asked for an E2E flow walkthrough. My initial description didn't match Duong's actual intended flow. Duong's corrected flow: empty-session Slack trigger → free-form UI chat → S5 iframed preview + fullview button → S3 auto-triggers S4 → iterate on same projectId. This correction revealed significant gaps between the current architecture and the target state.

I dispatched two parallel Explore audits:
- **Explore `ade0098ce33f7b629`** (Task #57): services audit across S1–S5 + MCP
- **Explore `a011ac32ee9987f61`** (Task #58): S1 internal audit

Both completed. Key findings:
- **Services audit**: S2 complete; S3 missing projectId reuse + S4 auto-trigger; S4 orphaned (no callers); S5 missing fullview route; MCP trigger_factory misrouted through S1; naming confusion between `demo-factory` (S3) and `demo-factory-cloud` (Slack bot)
- **S1 audit**: initialContext still passes brand/market; `/approve` route deleted but UI still calls it; `/session/{id}/preview` route deleted but UI still iframes it; no `verificationStatus`/`projectId` fields on session schema; no `/logs` SSE; no S4 polling; no projectId capture from S3

These audits together defined the full scope of the E2E gap.

I proposed a lightweight ship plan v2 + 4 ADRs. Dispatched **Azir #60** (`add0b8159a6612454`) to write `plans/proposed/work/2026-04-21-demo-studio-v3-e2e-ship-v2.md` incorporating audit findings and naming 4 ADR scopes: MCP-merge, S3-projectId+S4-trigger, S5-fullview, S1-new-flow.

Dispatched **Karma #61** (`a8dcdf69bf3de8944`) to fast-lane the S5 fullview plan at `plans/proposed/work/2026-04-21-s5-preview-fullview-route.md`. Karma #61 returned done — plan authored — but commit was blocked by the pre-commit plan-structure-check. The check fired on Karma **#59**'s plan (MCP-merge), not #61's S5 plan. Violations on the MCP plan: "h)" time units in body (hours notation triggers the estimates hook) + missing `## Test plan` heading. S5 file itself was clean; it was left untracked on disk. Karma #61 terminated unable to commit.

Duong: "have Swain write another god plan but ditch the managed agent." Dispatched **Swain #62** (`a7cbeff69d302f934`) for vanilla-API alternative at `plans/proposed/work/2026-04-21-demo-studio-v3-vanilla-api-ship.md`. This plan deletes MAL/MAD/MCP/setup_agent and keeps SE/BD only — a full re-architecture that competes with Azir's plan.

Vi `a604c23c978c4820c` (from the prior leg) was killed mid-run after reporting "Collection clean — 856 tests, no errors" during an `/exit` interaction. No full suite result available. Ship-ready greenlight still unissued.

`chore/ship-day-deploy-infra` sibling branch still not merged into the integration branch.

## Active in-flight agents at consolidation

- **Karma #59** (`a1a77fd0c0741668a`) — MCP-merge plan (`2026-04-21-mcp-inprocess-merge.md`) — in flight, commit likely still blocked
- **Azir #60** (`add0b8159a6612454`) — god plan v2 (`2026-04-21-demo-studio-v3-e2e-ship-v2.md`) — in flight
- **Swain #62** (`a7cbeff69d302f934`) — vanilla-API god plan B (`2026-04-21-demo-studio-v3-vanilla-api-ship.md`) — in flight

## Open threads into next session

1. **Karma #59 MCP-merge plan blocking Karma #61 S5 commit.** Pre-commit structure-check violations on the MCP plan file: "h)" time units (hours→minutes required) + missing `## Test plan` section. S5 plan is clean on disk as untracked file. Resolution: dispatch Ekko to fix Karma #59's MCP plan structure, then re-attempt commit of both. Or wait for Karma #59 to self-resolve before Karma #61 retries.

2. **Azir #60 god plan v2** — `plans/proposed/work/2026-04-21-demo-studio-v3-e2e-ship-v2.md`. In flight. Will supersede `plans/implemented/work/2026-04-21-demo-studio-v3-e2e-ship.md`. Names 4 ADR scopes: MCP-merge, S3-projectId+S4-trigger, S5-fullview, S1-new-flow.

3. **Swain #62 vanilla-API god plan B** — `plans/proposed/work/2026-04-21-demo-studio-v3-vanilla-api-ship.md`. In flight. Competes with Azir's plan. Deletes MAL/MAD/MCP/setup_agent. Keeps SE/BD. Re-architecture decision for Duong.

4. **S3 changes ADR** (projectId reuse + S4 auto-trigger) — not yet dispatched. Azir's god plan will name it; decompose after god plan lands.

5. **S1 new-flow ADR** (empty session + /approve removal + S5 iframe + session schema fields + /logs SSE + S4 polling + iterate UI) — not yet dispatched. Biggest scope. Decompose after god plan lands.

6. **Vi ship-ready audit** — killed mid-run; never completed. Integration branch HEAD `bda562e` still unverified for ship. Redispatch Vi before merging deploy-infra.

7. **MCP 503 still live in stg/prod.** Nothing fixed it this leg. If Duong wants local test before MCP-merge lands, either Heimerdinger redeploys MCP to a new project or Duong accepts vanilla-API plan.

8. **Ship sequence** (from prior leg) still pending: merge deploy-infra → preflight → staging → smoke → prod → 30-min obs. Blocked on Vi audit + architecture decision.

## Directives locked (carried forward)

- Hands-off until Duong says stop.
- Do not push impl branches unless directed.
- `git pull` first on all target repos before impl delegation.
- gh executor identity: `Duongntd`. Confirm before any impl delegation.

## compact-excerpt deferred

Transcript excerpt deferred — `scripts/clean-jsonl.py --since-last-compact` support not confirmed available.
