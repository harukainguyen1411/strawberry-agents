# Sona — Handoff Shard 2026-04-22-dd3ae6e1

**Session ID:** 69f3fb3e-b759-4c53-9e4e-88ba7e728afe
**Consolidated by Lissandra (pre-compact) — second consolidation this session**
**Concern:** work
**Mode:** coordinator (subagent dispatch)
**Prior shard:** `2026-04-22-3a5b4781` (first pre-compact, commit `514b112`)
**Differential from:** shard `3a5b4781` — records only activity since that consolidation

---

## What happened since the last shard

### PR #65 (dashboard-split W1) — RESOLVED
- Viktor completed task #33: merged `main → feat/demo-studio-v3` with conflict resolution. God-branch drift closed.
- PR #65 merged (commit `0bb60d8` in company-os). Dashboard W1 scaffold live on `feat/demo-studio-v3`.

### PR #66 (dashboard W2) — MERGED
- Viktor W2 branch (`feat/demo-dashboard-w2-routes`) shipped 8 route migrations from S1 to the new dashboard service.
- Lucian review: learning committed at `c6b820b`. Senna review also completed.
- PR #66 merged at `4c1d4bb` (10:48 UTC+7). Dashboard W2 live.

### PR #67 (demo-preview port) — MERGED + DEPLOYED
- Preview iframe staleness plan promoted to in-progress (Ekko, `7bd09d3`).
- Viktor / Jayce ported `origin/main server.py` to `feat/demo-preview-port-main`; wired `/preview` route, brand-correctness fixes.
- Senna review: advisory LGTM with notes (commit `a9cd335` refs Senna learning `2026-04-22-pr67-demo-preview-port-main-review.md`).
- PR #67 merged at `ccd7a32` (11:25 UTC+7).
- Ekko deployed: revision `demo-preview-00010-ff4` live. Smoke green: `/health` 200, `/preview/__bad__!!!` 400 `INVALID_SESSION_ID`, `/preview/nonexistent-session-123` 200 HTML fallback. Commit `b791d56`.

### PR #68 (dashboard W5 deploy prep) — OPEN, waiting Duong IAM
- Ekko created SA `demo-dashboard-sa@mmpt-233505.iam.gserviceaccount.com`. IAM role bind BLOCKED (setIamPolicy denied — needs project Owner identity).
- `deploy.sh` fixed: `--ingress=all` + `--service-account=...` + secret name bug (`DS_SHARED_SESSION_SECRET` → `DS_STUDIO_SESSION_SECRET`).
- PR #68 OPEN on `feat/demo-dashboard-w5-deploy-prep`. Two deploy blockers:
  1. `roles/datastore.user` IAM bind — Duong `harukainguyen1411` manual action required.
  2. `secretAccessor` grants on `DS_STUDIO_SESSION_SECRET` + `DS_SHARED_SESSION_SECRET` — same identity required.
- Next action: Duong grants IAM binds in GCP console → Ekko deploys dashboard service.

### PR #69 (Firebase Loop 2b frontend) — OPEN, request-changes from Lucian
- Vi authored xfail tests (`feat/firebase-auth-2c-xfails`) and Jayce authored impl on `feat/firebase-auth-2b-frontend-signin` (separate worktree).
- 19 xfail source-inspection pytest tests → all XPASS after impl.
- Akali QA: PASS (report `assessments/qa-reports/2026-04-22-pr69-firebase-2b-qa.md`; screenshots `pr69-01` through `pr69-03`).
- Senna review: advisory LGTM — 3 important findings (popup-cancelled error fallthrough, hardcoded `@missmp.tech` vs env-driven ALLOWED_EMAIL_DOMAIN, silent `/auth/logout` failure leaves server cookie). Not blocking ship. Learning `2026-04-22-pr69-firebase-auth-frontend-review.md`.
- Lucian review: REQUEST-CHANGES — plan specified Playwright TypeScript + Firebase Auth Emulator; impl shipped source-grep pytest. T.8 DoD ("verify emulator run") unfulfilled. Verdict written to `/tmp/lucian-pr-69-verdict.md` (reviewer-auth denied for `missmp/company-os`). Learning `2026-04-22-pr69-firebase-2b-test-strategy-divergence.md`.
- PR #69 is OPEN. Lucian request-changes is a blocker — either the test strategy gap must be resolved or Duong overrides with explicit acceptance of the source-grep coverage stance.
- Senna finding on reviewer-auth: `strawberry-reviewers-2` has no collaborator access to `missmp/company-os`; Yuumi-fallback path is the only option for work-concern code review.

### T7 Firestore wipe — EXECUTED (Ekko harness-blocked, outcome unclear)
- T7 wipe: Ekko attempted to run the wipe script (Duong green-light received). Ekko hit harness block — subprocess denied by sandbox. Learning `ae08c07`.
- Status ambiguous: wipe may not have run. Confirm by checking if `config.firestore_url` field still exists in staging Firestore docs before declaring T7 done. If field still present, T7 needs a human-run or a different execution path.

### Firebase 2a → implemented; 2b + 2c → approved
- Loop 2a (`firebase-auth-loop2a-server-backbone`) promoted full chain to implemented (Ekko, `5d76d1c`). Server backbone `/auth/config`, `/auth/login`, `/auth/me`, `/auth/logout` is the canonical server-side auth.
- Loop 2b (`firebase-auth-loop2b-frontend-signin`) at `plans/approved/work/` — impl is in PR #69 (OPEN, see above).
- Loop 2c (`firebase-auth-loop2c-route-migration`) at `plans/approved/work/` — ready to dispatch once 2b lands.

### Loop 2c Vi + Jayce dispatched
- Vi (`feat/firebase-auth-2c-xfails`) and Jayce (`feat/firebase-auth-2c-impl`) have been dispatched in parallel for Loop 2c (route migration: `require_session → User`, `require_session_owner`, `ownerEmail` + claim-on-first-touch, all `/session/{sid}/*` route migration).
- Status at compact boundary: branches exist locally (`feat/firebase-auth-2c-xfails`, `feat/firebase-auth-2c-impl`); remote push status not confirmed. Check before assuming PRs are open.

### Dashboard scope contracted
- Duong scoped dashboard deliverable to "working URL + handoff" — not the full P5 pillar. Dashboard W1+W2 are merged; W5 deploy prep is OPEN (PR #68). Dashboard work is feature-complete on the code side; blocked on IAM grants for live deploy.

### Coordinator QA verification feedback (c19c190)
- Duong landed a feedback doc (`feedback/2026-04-22-coordinator-verify-qa-claims.md`) instructing coordinator to verify QA agent claims post-report — do not relay Akali pass verdicts without independently checking test counts, coverage claims, and screenshots are real.

---

## Open threads into next session

### CRITICAL — PR #68 (dashboard W5 deploy) blocked on IAM
- IAM bind for `roles/datastore.user` + `secretAccessor` on 2 secrets requires Duong `harukainguyen1411`. PR #68 OPEN on `feat/demo-dashboard-w5-deploy-prep`.
- Next action: Duong grants IAM → Ekko deploys dashboard service.

### CRITICAL — PR #69 (Firebase 2b) Lucian request-changes
- Lucian flagged test-strategy divergence: plan promised Playwright + emulator; impl delivered source-grep pytest.
- Resolution options: (a) Duong accepts source-grep coverage explicitly, (b) Vi dispatches real emulator tests on same branch, (c) Duong closes as-is and tracks emulator debt separately.
- Next action: Duong decision on 2b test-strategy gap → proceed or request Vi emulator fix.

### Loop 2c — dispatch in-flight, verify branch state
- `feat/firebase-auth-2c-xfails` and `feat/firebase-auth-2c-impl` dispatched. Remote push unconfirmed.
- Next action: check branch push status and Vi/Jayce return messages; open PRs if commits are on remote.

### T7 Firestore wipe — ambiguous (harness-blocked)
- Ekko harness denied the wipe script execution. T7 may not have run. Field `config.firestore_url` may still be present in staging docs.
- Next action: Duong or Ekko confirms field presence; if still there, schedule human-run wipe or alternative execution path.

### reviewer-auth.sh gap — missmp/company-os (structural)
- Both `strawberry-reviewers` and `strawberry-reviewers-2` lack collaborator access to `missmp/company-os`. All code reviews for work PRs go through Yuumi-fallback (write verdict to `/tmp/`, Yuumi posts as PR comment). Rule 18 binding approval requires Duong `harukainguyen1411` in web UI.
- Next action: Commission plan to grant `strawberry-reviewers` collaborator access to `missmp/company-os`, or extend `reviewer-auth.sh` with target-repo parameter.

### Preview iframe staleness triage — plan in-progress
- Plan `2026-04-22-preview-iframe-staleness-triage.md` at `plans/in-progress/work/`. PR #67 deployed the ported server; remaining triage (T1-T4) not yet dispatched.
- Next action: Dispatch Jayce or Viktor on triage tasks T1-T4 per the in-progress plan.

---

## Open asks for Duong

1. Grant IAM `roles/datastore.user` + `secretAccessor` for PR #68 dashboard deploy (GCP console, `harukainguyen1411`).
2. Decide on PR #69 Loop 2b test-strategy gap — accept source-grep coverage or request emulator tests.
3. Confirm T7 Firestore wipe outcome — check staging Firestore docs for `config.firestore_url` field presence.
4. Web-UI approve PR #68 once IAM grants are in place (no `strawberry-reviewers` access to `missmp/company-os`).

---

## Blockers

- PR #68 deploy: IAM binds require project Owner identity.
- PR #69: Lucian request-changes on test strategy.
- T7 wipe: harness-blocked; outcome unconfirmed.
- Loop 2c: dispatch in-flight, remote push not confirmed.

---

## Compact excerpt note

`scripts/clean-jsonl.py --since-last-compact` not available. Compact excerpt deferred.
