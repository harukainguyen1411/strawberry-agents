# 2026-04-20

Planning day. Duong wanted to sort out the managed-agent lifecycle problem and by the end of the session we had three ADRs landed on `feat/demo-studio-v3`.

The session started with housekeeping — 9 backlogged commits that needed pushing, a `gh auth switch` fumble, then a `ws-pull`. Found a small but annoying mess: two `last-session.md` files had been written to `company-os/secretary/` by agents running with the wrong cwd. Moved them as shards into the canonical `workspace/secretary/`, gitignored the stray path, and had Ekko verify nothing had ever leaked to the GitHub remote. Clean.

From there the work kept branching into architecture. Tried to start the local backend, hit a stale port 8080, killed the zombies, got it green. Then Duong asked why Firestore was still live, which led to the discovery that `ARCHITECTURE.md` had never been updated for the 5-service split. Azir rewrote it, and in the process flagged ten spec drifts between `1-content-gen.yaml` (PR #40) and the actual Service 1 code.

The real pivot happened when Duong corrected my first ADR draft: "session is owned by us, Service 2 is only for config." I had to kill the old ADR and have Azir write a smaller, better one — session stays on Service 1, just encapsulated behind a proper `session_store.py` module. That's the pattern I should remember: when someone says "we own X", they mean the whole store, not just the API surface.

Lifecycle monitoring came next. Duong's instinct to poll Anthropic directly rather than our DB was exactly right — I hadn't even considered that our DB can drift from reality on partial writes. Revised the design to idle-only, Anthropic-as-source-of-truth. Lux surfaced a good research point: regular API key handles list/retrieve/terminate, admin key only for cost reports. That simplifies the shipping story.

Then the dashboard tab came up organically — if we already have the scanner, why not give operators a view into the same data with a terminate button? Wrote it as a third ADR to keep the scopes clean. Locked the last two open questions in-session (orphans visible, auth deferred).

Committed + pushed all three plans as `d68df34`. Implementation next session, starting with Spike 1.

What I want to do differently: catch scope-corrections earlier. The "session on Service 2" draft was a wasted half-loop that could have been avoided if I'd asked one clarifying question up front. Also: the Sona/Strawberry memory fixes Ekko did for me are still sitting uncommitted. Need to finalize those early next session, not let them drift another 24 hours.

---

## Session 2 — gate-v2 migration + CI audit

Duong came back asking to apply the new Orianna gate-v2 + agent pair taxonomy to the three ADRs I'd left on `feat/demo-studio-v3`. We expanded scope to four (`s1-s2-service-boundary` came along) and ran the whole chain.

First lesson: the claim-contract routing was only configured for strawberry-agents + strawberry-app. Work plans cite paths in `~/Documents/Work/mmp/workspace/company-os/`, so every work plan would have failed the proposed→approved gate on the first `test -e`. Commissioned Karma for a quick-lane plan to extend the routing with a `concern: work` gate. Talon implemented it. Senna's code-review caught a real latent bug: `concern: "work"` (YAML-quoted) was silently falling back to strawberry-app. Not a polish suggestion — an actual correctness gap my test matrix hadn't exercised. Good catch, and a lesson that "5 invariants" isn't enough when YAML flexibility opens variant surfaces.

Yuumi handled the mechanical migration — copied 4 ADRs plus sibling `-tasks.md` / `-bd-amendment.md` files into `plans/proposed/work/`, inlined siblings, added frontmatter. Hit the plan-structure pre-commit hook twice: missing `tests_required: true` + `## Test plan` sections + stray `h)` time-unit tokens that the estimate lib substring-matches. Second Yuumi pass cleaned them up; all four now pass `check_plan_structure`.

Parallel thread: CI was red on PR #7 across 13 checks. Ekko triaged — it wasn't code, it was a GitHub Actions billing block on `harukainguyen1411`. Duong upgraded to Pro and unblocked it. But the deeper question Duong raised was right: *why does the agent repo even have all these workflows?* Full audit showed 8+ of the 14 were strawberry-app artifacts that migrated over during the public-repo split and never got pruned. Release.yml deploys Firebase Functions from `apps/myapps/` which doesn't exist here. `pr-lint.yml` checks for UI changes under `apps/*/src/` — also doesn't exist here. `validate-scope.yml` literally says "informational only — never blocks" in its own code. Pure ceremony. Killed 8 in PR #8, then 4 more in PR #10, then `auto-rebase.yml` in PR #9 (that one was actively harmful — `push:main` → `git rebase` on every open PR → force-push → cascades CI across the whole fleet; also violates Rule 11). After all three land, only `tdd-gate.yml` remains.

Branch protection payload drafted and saved to `assessments/branch-protection/2026-04-21-main-branch-protection-payload.md` — Duong has to apply it manually as `harukainguyen1411` since no agent identity has admin on this repo. Required checks scoped to just `xfail-first check` + `regression-test check`. Projection: ~120–180 min/month at 30 PRs. Under 6% of the 3k Pro budget.

Process wins this session:
- The `<!-- orianna: ok -->` suppression marker came up organically when Karma's own plan was full of meta-examples. Duong asked the right follow-up question — "could this be used to bypass everything?" — and yes, within the claim-contract step specifically. Flagged as a governance gap for a later plan; didn't patch today.
- Hands-off mode worked cleanly once I internalized the "no questions, just decide and report outcomes" cadence. Caught myself almost re-asking Duong about option (a) cleanup approach; swallowed it, dispatched Ekko.

Dangling at close:
- PR #7 CI re-running after billing fix — one `unit-tests` job came back FAIL (11s fresh, not billing). Ekko flagged; Duong should check whether that job actually matters post-PR-#10 (it's in the deleted workflow set, so should vanish on its own).
- PR #10 pending Senna review (background agent still running when I closed).
- 4 migrated work ADRs staged in `plans/proposed/work/` awaiting Orianna signing — held until PR #7 merges so the routing code is live.
- Branch protection blocked on Duong's admin credential.

What I want to do differently: the structural pre-commit hook firing on `(d)` and `h)` substrings is a known footgun and Karma authored a plan with both. Next time I commission a Karma plan, include an explicit "pass `check_plan_structure` before closing" step in the brief so Karma self-verifies before handing off.
