# 2026-04-03 — First Session

Launched by system to verify and fix tasklist UI/UX work.

## What happened
- Verified all 10 of Neeko's UI/UX changes on the HTML tasklist — all clean, no issues
- Cloned myapps repo, applied 4 missing features to Ornn's Vue migration (PR #53): whole-card drag, inline done tasks, dropdown-above, 1024px tablet breakpoint
- Lissandra caught a regression — I'd removed touch drag handlers when switching to whole-card drag. HTML5 drag API doesn't fire on touch. Fixed by restoring touch handlers on the card with 15px threshold and simplifying the emit chain
- Two commits pushed, build/typecheck/tests green both times

## Observations
- Ornn's Vue migration is solid structurally but was missing some of Neeko's polish — the gap between design and implementation is real
- The pre-commit hooks on myapps are strict (typecheck + tests + lint) — good, caught my unused vars immediately
- Had to merge with remote mid-push — someone else was committing to the same branch concurrently

# 2026-04-03 — Session 2: Contributor Pipeline Bot

## What happened
- Evelynn delegated: build the Discord bot for the contributor pipeline (`apps/contributor-bot/`)
- Built it in one pass: discord.js watching #suggestions forum, Gemini 2.5 Flash-Lite triage, GitHub issue creation via Octokit, workflow_dispatch trigger, status updates back to Discord
- Swain reviewed, flagged two issues: fragile JSON.parse and unverified Gemini model ID. Also suggested input sanitization, env validation, and scope note on GITHUB_TOKEN
- Applied all fixes in a second commit: try/catch with fence stripping, versioned model ID, markdown sanitization + length cap, startup env validation, .env.example annotation
- Both commits pushed to feature/contributor-pipeline

## Observations
- Ornn added interactions.js and server.js imports to index.js between my commits — other agents working on the same branch concurrently
- Clean task. In, out, no lingering threads on my end. Approval buttons are Ornn's problem.

# 2026-04-03 — Session 3: GitHub Webhook for Discord

## What happened
- Task from system inbox: set up GitHub webhook to post PRs/issues to Discord #pr-and-issues
- Found the channel already existed in the strawberry Discord server
- Hit a permission blocker creating the Discord webhook via MCP — flagged as persistent external modification
- Reported to Evelynn, got the webhook URL from Duong within minutes
- Configured GitHub webhook via `gh api` — triggers on `issues` and `pull_request` events, posts to Discord

## Observations
- Quick task, blocked briefly by permissions. Right call to escalate instead of fighting it.
- The whole thing took maybe 5 minutes of actual work. Classic Katarina job.

# 2026-04-04 — Session 4: Discord-CLI Integration (discord-relay)

## What happened
- Evelynn delegated: build the discord-relay bot and bridge scripts per the Discord-CLI integration plan
- Created `apps/discord-relay/` — thin discord.js bot that writes ThreadCreate events as JSON to a data dir and watches a responses dir to post back to Discord. Stripped all triage/github/gemini logic from the old contributor-bot pattern.
- Created `scripts/discord-bridge.sh` — watches event queue, spawns `claude --message` per event with rate limiting, archives processed events
- Created `scripts/result-watcher.sh` — fallback watcher that nudges pending responses if relay bot restarts
- Evelynn (or someone) enhanced my initial bridge script into a two-pass system (triage + delegation) and added thread_reply handling to the relay bot. Good improvements — the two-pass approach is cleaner than one big Claude call.
- Notified Evelynn, task complete.

## Observations
- Clean cut from the old pipeline. No Gemini, no Octokit, no GitHub issues. Just Discord I/O and file-based IPC with Claude CLI.
- The file-based event queue is simple but effective — no Redis, no database, just JSON files and fs.watch.

# 2026-04-05 — Session 5: GH_TOKEN and API Key Isolation

## What happened
- Evelynn launched me to diagnose why `$GH_TOKEN` was empty in agent sessions
- Ran two diagnostic commands — confirmed GH_TOKEN not set, active gh account was Duongntd (not harukainguyen1411)
- Implemented plan `2026-04-05-gh-token-injection-fix.md`: one-line shell scoping fix in `server.py` — changed `VAR=$(cmd) cmd` to `export VAR=$(cmd) && cmd`. PR #29.
- Implemented plan `2026-04-05-agent-api-key-isolation.md`: read per-agent ANTHROPIC_API_KEY from their `settings.local.json`, write to temp file, inject via `export ANTHROPIC_API_KEY=$(cat ...)`. PR #30.
- Lissandra caught a missing try/except around `json.load()` — pushed fix to PR #30 branch same session.

## Observations
- Both fixes follow the same scrollback-safe `$(cat file)` pattern — consistent with existing GH_TOKEN code.
- The shell scoping bug (`VAR=$(cmd) next_cmd` vs `export VAR=$(cmd) && next_cmd`) is a classic pitfall worth remembering.
- Lissandra's code review was fast and correct — the JSONDecodeError edge case was a real gap.
