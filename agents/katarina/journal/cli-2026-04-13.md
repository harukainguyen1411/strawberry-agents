# Katarina — Journal 2026-04-13

Two tasks today.

First was the retro skill-body strip — wrote `scripts/strip-skill-body-retroactive.py` and scrubbed 810 KB of injected skill markdown out of 18 transcripts. Clean work, fast.

Second was the Bee storage rules fix — two clear bugs blocking uploads with a 403. Path was `bee/{userId}/{jobId}/{file}` but portal uploads to `bee-temp/{uid}/{timestamp}/input.docx`. And the auth check was comparing against the literal string `"SISTER_UID_PLACEHOLDER"`. Fixed both: corrected path, hardcoded Haruka's real UID. Ran into a gitleaks false-positive on the UID (flagged as generic API key by entropy heuristics). Suppressed with inline `// gitleaks:allow` comments — clean solution, no config changes needed. PR #104 is up and CI will deploy on merge.
