# Swain — operational memory

Architecture specialist. Opus planner. Writes plans to `plans/proposed/` and stops — never self-implements, never assigns implementers (`owner:` is authorship only).

## Key Knowledge

- Plans use `chore:` commit prefix and go directly to main, never via PR.
- In subagent mode (invoked by Evelynn), no inbox / MCP / Mac stack. Return summary to Evelynn at end. Always run `/end-subagent-session swain` as the final action.
- May spawn only Skarner (memory recall) or Yuumi (errands), always `run_in_background: true`.
- Schema changes that touch derived totals (currency, units, time zones) must propagate through: data model, invariants, architecture bullet, UI rendering, and snapshot semantics. A `baseCurrency` field is not a footnote — snapshots must embed the base used at write-time so a later switch doesn't silently rewrite history.
- When auditing governance docs, don't trust the doc — verify each claimed enforcement against `scripts/hooks/`, `.claude/settings.json`, and `.github/workflows/`. Stale enforcement claims (rule text says "a hook enforces X" when no hook exists) are the single highest-risk class of governance drift because they create false confidence.

## Sessions

- 2026-04-19: Amended portfolio ADR with v0 kickoff Q4-Q7; Q7 (per-user base currency USD/EUR) propagated as schema change through §3/§4/§8; v0 scope tightened to CSV-only + handler stub.
- 2026-04-19 (2): Rules-to-hooks audit — classified CLAUDE.md invariants 1-18 by enforcement. Plan at `plans/proposed/2026-04-19-rules-to-hooks-audit.md`. Top 5 to migrate: Rule 5 (commit-prefix — stale enforcement claim), Rule 11 (git rebase), Rule 3 (raw git checkout), Rule 1 (uncommitted work), Rule 18 (gh pr merge --admin).
- 2026-04-20: Orianna-gated plan lifecycle ADR → `plans/proposed/2026-04-20-orianna-gated-plan-lifecycle.md` (commit 1bb8620). Signature mechanism: frontmatter `orianna_signature_<phase>` with body-hash, tamper-evidence via git commit author identity (`orianna@agents.strawberry.local`). Phase checks: approved (existing fact-check + gating-Q scan), in-progress (task list + AI-minute estimates + Caitlyn section), implemented (grep re-verify + architecture_changes OR explicit architecture_impact: none). Escape hatch: `Orianna-Bypass:` trailer, Duong-only.
- 2026-04-20 (revision): Addressed Duong's 10 inline comments on the Orianna ADR → commit c716a16. Key resolutions: one plan = one file (no sibling -tasks.md / -tests.md), estimate_minutes upper bound = 60 (block over), architecture_impact declaration mandatory (block if missing), demote all plans/approved/ back to proposed/ at migration, freeze new plan creation until gate infra validated. 3 new round-2 gating questions raised: Q9 demotion script ownership, Q10 sibling-file merge during demotion, Q11 freeze hook scope (edits vs new files only).
- 2026-04-20 (taxonomy): Agent pair taxonomy ADR → `plans/proposed/2026-04-20-agent-pair-taxonomy.md` + Orianna ADR revised to reference role slots (combined commit `0d1c7f6`). Two-track complex/normal pair matrix across 9 role slots. 4 new agents (Xayah Opus-high test planner, Rakan Opus-low test implementer, Soraka Sonnet-low normal frontend impl, Syndra Sonnet-low normal AI-specialist). Rescopes: Lux (narrowed to AI+agent-def-org, effort high), Viktor (complex-track builder, drop refactor-only), Swain (effort xhigh). Shared-rules pattern: `.claude/agents/_shared/<role>.md` + inlined-with-sync via `scripts/sync-shared-rules.sh` + pre-commit drift hook. Pair-mate frontmatter fields: `tier`, `pair_mate`, `role_slot`. Default-lean = normal. 7 gating questions raised Q1-Q7.
- 2026-04-20 (consolidation): Both ADRs revised with Duong's answers. Orianna ADR Q9/Q10/Q11 all manual — no `--demote-to-proposed` mode, no auto-merge for siblings, freeze scope = new files only. Taxonomy ADR adopts Lux's retiering: **never Opus-low** as canonical rule — Rakan Opus-low→Sonnet-high, Syndra Sonnet-low→Sonnet-high, Viktor Sonnet-medium→Sonnet-high, Jayce Sonnet-low→Sonnet-medium, Lulu Opus-low→Opus-medium. Matrix expanded from 9 to 17 rows (one row per single-lane agent + coordinator row 0 for Evelynn+Sona). Canonical ordering rule: Opus-xhigh > Opus-high > Opus-medium > Sonnet-high > Sonnet-medium > Sonnet-low (§D1.1b). Model frontmatter convention: Opus omit `model:`, Sonnet declare `model: sonnet` (§D1.1a). Cost footnote: Opus-xhigh vs Sonnet-low ≈ 50× on Max (§D1.6). Pre-commit hook extended with 3 checks: shared-rules drift + pair-mate symmetry + model-convention. New round-3 gating questions Q8-Q10: Sona inclusion scope, coordinator `pair_mate:` semantics, Senna/Lucian tier asymmetry.
- 2026-04-20 (round-3 revision): Taxonomy ADR revised → commit a79bb28. Q8 resolved: Sona committed as first-class coordinator (.claude/agents/sona.md becomes tracked in Phase A). Q9 resolved: new `concern: personal | work` frontmatter field for coordinators — they do NOT carry `pair_mate:`/`tier:`; pair-mate symmetry hook skips agents with `concern:` set. Q10 resolved: Senna Opus-high / Lucian Opus-medium asymmetry kept — code+security reasoning is deeper than plan-fidelity comparison. Sonnet adaptive-thinking added to §D1.1a: Opus 4.7 has it as the only mode (automatic), Sonnet 4.6 opt-in but roster adopts uniformly; `effort:` = ceiling-plus-tendency, not a floor (at medium, model may skip thinking on trivial tasks and adapt upward on harder ones). ADR ready for Orianna fact-check + promotion.
- 2026-04-20 (post-fact-check amend): Addressed 3 Orianna findings on taxonomy ADR → commit 156cea3. (1) §Context path: orianna-gated-plan-lifecycle.md moved proposed/ → approved/, updated reference. (2) §D3 added rescope entries D3.3c Caitlyn (Opus-high → Opus-medium), D3.3d Neeko (Opus-medium → Opus-high), D3.3e Kayn (Opus-high → Opus-medium) to reconcile matrix tiers with current agent-def effort values. (3) §D8.1 step 4: sona.md is tracked+modified (not untracked); reworded step header "Track" → "Update". No decisions change. Plan still `proposed/` — not promoted.
- 2026-04-21 (memory-consolidation ADR): Translated Lux's recommendation `assessments/personal/2026-04-21-memory-consolidation-redesign.md` into executable plan `plans/proposed/personal/2026-04-21-memory-consolidation-redesign.md` (uncommitted per task brief — Duong commits via admin identity due to §D12 plan-authoring freeze). Two-layer boot: eager `open-threads.md` + auto-generated `last-sessions/INDEX.md` at the dynamic tail; shards pulled on demand. Scope = Evelynn + Sona in one cutover. Retention = 14d OR 20 shards. INDEX regen on every `/end-session`. Boot order = static prefix → dynamic tail (prompt-cache). 12 tasks, 440min total. `filter-last-sessions.sh` deleted. `memory-consolidate.sh` rewritten with INDEX regen + archive policy + `--index-only` flag + pre-boot validator (moved from filter-). Gotcha: plan-structure linter banned-literals check matches `h)` and `(d)` even in non-time prose — wrap in backticks or reword (linter strips backtick spans before prose check).
- 2026-04-21 (retrospection dashboard ADR): `plans/proposed/personal/2026-04-21-retrospection-dashboard.md` (uncommitted). Location = new sibling repo `~/Documents/Personal/strawberry-retro/` (rejected: `apps/retro-dashboard/` in either repo — strawberry-agents has no `apps/` convention, strawberry-app couples deploy cadence). Stack = Vite + Vue 3 + TS + Tailwind + Pinia + vue-router + date-fns + Chart.js (mirrors `apps/myapps`). Storage = flat `retro.json` in v1 (SQLite/DuckDB rejected at this scale); shard monthly when > 5 MB. Data sources = reuse existing `ccusage` outputs + planned `subagents.json` (from 2026-04-19-usage-dashboard-subagent-task-attribution.md) + new retro-indexer reading agent memory/journals/learnings/git-log/tasklist-mirror. ETL = 5-min mtime-watch, not blind poll. Axis = two-signal derivation (repo + commit-prefix for git records; path-tag for sessions; frontmatter override for captures) with `mixed` as a legitimate bucket not a bug. Capture = markdown files in `agents/retro/captures/` + `/retro capture` skill (rejected: new MCP tool, per-agent inbox). 10 named queries back the retro cards. Phased rollout: walking skeleton (P1, 175m) → retro queries + axis (P2, 285m) → captures + polish (P3, 220m). Localhost-only v1; no Anthropic API, no paid service. 16 tasks, 680 min total. 6 open gating questions (phone access, cross-concern scope, subagent-attribution dependency, Playwright exemption, capture cross-concern, tasklist write-through).
- 2026-04-21 (decision-feedback ADR): `plans/proposed/personal/2026-04-21-coordinator-decision-feedback.md` (uncommitted). Three-file/per-coordinator preference-learning system: `preferences.md` (eager, ≤150 lines, hand-curated digest w/ per-axis pseudo-count confidence) + `axes.md` (eager, append-only, axis defs) + `log/*.md` (lazy, one file per decision w/ YAML frontmatter) + auto-generated `INDEX.md` (lazy). Capture ritual extends the a/b/c format with inline `Predict:` + `Confidence:` (three buckets low/med/high — no float). Per-session fold in `memory-consolidate.sh --decisions-only`. Retrieval = eager `preferences.md` for synchronous axis lookup, Skarner delegation for deep-dive. Axes coordinator-proposed, Duong-approved; seeded with 4 defaults (scope-vs-debt, explicit-vs-implicit, hand-curated-vs-automated, rollout-phased-vs-single-cutover). v2 auto-decide explicitly gated behind future ADR (n≥10 per axis + explicit per-axis opt-in). 12 tasks, 425 min. Slots under memory-consolidation plan's two-layer architecture at boot positions 8 and 9. Serial-after-memory-consolidation to avoid `memory-consolidate.sh` rewrite conflict. 5 OQs (initial axis kernel size, preferences-vs-axes boot order, Predict/Pick divergence allowed, blocking vs non-blocking capture, serial-vs-parallel rollout). Gotcha: fact-check-plan.sh line 328 skips `<coordinator>`-style angle-bracket placeholders automatically — only concrete prospective paths like `.claude/skills/decision-capture/SKILL.md` need `<!-- orianna: ok -->` markers.
- 2026-04-21 (demo-studio-v3 E2E ship ADR): `plans/proposed/work/2026-04-21-demo-studio-v3-e2e-ship.md` (commit 41ff9b8). Orchestration/integration plan above 4 sibling ADRs (SE proposed, BD/MAL/MAD approved). 10-wave landing sequence W0-W10 with hard serial points: SE.A.6 gates MAL.B.2+MAD.C.2; MAL.0.1 spike gates MAL.D+MAD.B degraded wording; BD delete-from-S1 must land inside SE.A.4 and before SE.B.2/B.4; MAD.E.1 allowlist row ships in MAD PR per Camille advisory. E2E narrative spans 7 steps: session create → agent set_config via MCP → build thin-pass-through to S3 → QC → completed → MAL terminal hook fires → dashboard visibility → manual terminate. Ship gate = §4.1 green-checklist + prod smoke §5 + `MANAGED_AGENT_DASHBOARD=1` flip in separate W10 PR. Deploy = single Cloud Run service `demo-studio` via `deploy.sh`; canary traffic-split at W4+W5. Data migration: SE.C enum migration idempotent + BD orphan strategy (no backfill). Rollback: per-wave revert + Firestore snapshot pre-W4. 7 OQs (min-instances pinning, S3 self-fetch confirmation, SE.C reverse mode, preview route deprecation copy, scanner startup default, retroactive grep-gate enforcement, PR granularity). Workspace state at authoring: company-os@13fc893 on feat/demo-studio-v3 (clean); MAD worktree on chore/mad-a1-a3-d6-xfail@1de8549 with MAD.A.1-A.4 + MAD.D.1-D.6 committed; api/main@4056ac9; mcps on unrelated branch; ops on unrelated work. Top-3 blockers surfaced: (1) SE still proposed — gates W1; (2) MAL.0.1 spike not executed — gates scanner + MAD degradedFields wording; (3) S3 self-fetch unconfirmed — gates W5 factory-path canary. Discipline reminder: plan-structure linter requires exact `## Test plan` (not `## N. Test plan`) — renamed 3 section headers to conform.
