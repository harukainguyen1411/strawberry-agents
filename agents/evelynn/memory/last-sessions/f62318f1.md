# Last Session — 2026-04-20 (S61, cli, direct)

Work-agent migration + dual-coordinator + plan-lifecycle overhaul.

## Shipped
- **Dual-coordinator launch wired** (`1c4049a`) — evelynn `initialPrompt` absorbs old SessionStart hook, hook narrowed to resume-only, `scripts/mac/aliases.sh` gives Duong `evelynn` + `sona` aliases via `claude --agent <name>`. Zshrc sourced, verified.
- **strawberry-inbox Channels plugin** (`0d1c7f6` ADR approved → Jayce shipped) — `.claude/plugins/strawberry-inbox/` watches each coordinator's inbox, emits channel events to their running session. `/check-inbox` skill auto-marks read. One-time `npm install` needed in plugin dir.
- **Work-agent migration bulk import** (`0c2c536`) — Sona def + tree, historical learnings for 14 agents, orianna fact-check report. Pre-existing untracked; committed as single chore for clean tree.
- **Orianna-gated plan lifecycle ADR** (Swain, `2c8b70b` + `a79bb28`, still `proposed`) — signature gate at every phase transition, AI-minute estimates, architecture freshness checks. Zero open questions.
- **Agent pair taxonomy ADR** (Swain, same commits, still `proposed`) — complex/normal two-track matrix, 4 new agents (Xayah, Rakan, Soraka, Syndra), Viktor/Lux rescopes, coordinator row for Evelynn + Sona (both Opus-medium + `concern: personal|work` field), shared-rules pattern, canonical model/effort ordering. Zero open questions.
- **Final-message rule** (`c400aa7`) — moved from per-Lux to universal in `agents/memory/agent-network.md` §Session Protocol. Agent-agnostic.
- **Lux startup path fix** (`c18444a`) — was pointing at stale `/strawberry/` dir.
- **Evelynn memory default-model correction** (`a01a62e`) — global settings no longer has `"model"` field; default is Opus 4.7 1M.

## Open threads
1. **Both ADRs ready for Orianna fact-check + promotion.** Fresh session picks it up. Path: `plans/proposed/2026-04-20-{orianna-gated-plan-lifecycle,agent-pair-taxonomy}.md`.
2. **strawberry-inbox plugin untested E2E.** Jayce shipped it but we never did the live ping test (Sona running, Evelynn pings, channel fires). Duong needs to `npm install` + restart sessions first.
3. **4 new agents to scaffold** (Xayah, Rakan, Soraka, Syndra) — part of taxonomy ADR implementation after promotion.
4. **Viktor/Jayce/Lulu/Rakan/Syndra tier bumps** — apply to agent defs after promotion.
5. **Sona first-class commit** — taxonomy ADR commits her as coordinator with `concern: work`; `.claude/agents/sona.md` already tracked as of `0c2c536`.
6. **PR #62 Phase 1 rename still red** (from S60) — 4 failing checks, Viktor re-dispatch still pending.
7. **Portfolio v0 importCsv** export still pending (S60).
8. **P6 migration purge** gated to 2026-04-26.

## Watchouts
- **Agent-def caching in top-level session** — Evelynn session caches `.claude/agents/<name>.md` at startup. Mid-session edits don't apply to subagents. Lux failed 3x before session restart let the fix take effect. Workaround: restart session after agent-def edits.
- **Ekko Edit denial punt** — hit Edit denial on `.claude/agents/evelynn.md`, bailed into permissions discussion instead of retrying. Duong overrode "you do it" — I executed directly. Pattern worth monitoring.
- **Lux failure mode** — three closes with just `/end-subagent-session` string, losing the full report. Fixed universally in agent-network.md, plus her startup path fix. Lesson: fix systemic, not per-agent.
- **Band-aid scope trap** — I patched Lux's def individually before Duong caught it. Always check whether a symptom is agent-specific or systemic before editing.

## Dangling
- Nothing unpushed post session close.

## Next session boot
Start by running Orianna fact-check on both proposed ADRs, then `scripts/plan-promote.sh` proposed → approved. Then Phase A implementation of taxonomy ADR (shared rules files, 4 new agent scaffolds, pair-mate symmetry hook).
