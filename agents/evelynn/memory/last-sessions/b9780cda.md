# Last Session — 2026-04-19 (S60, cli, direct + auto)

Migration tail + restructure kickoff + portfolio v0 prep.

## Shipped
- **Folder migration complete** — P2 slug retarget #59 `4ea1884`, P4 close-all (11 non-Dep PRs closed via Ekko, branches deleted), P5 living-docs rewrite `19fd743` (8 files). Only P6 purge at T+7 (earliest 2026-04-26) remains.
- **Workspace sweep** — /tmp scratch trees purged, 12 strawberry-app worktrees removed, main back on `4ea1884`, `~/strawberry` old clone preserved per Duong.
- **`.claude/_retired-agents/`** — moved out of `.claude/agents/` so harness stops surfacing retired lowercase agents (bard/fiora/katarina/etc.) as callable subagent_types. 3 doc refs rewritten.
- **Apps-restructure ADR promoted to approved** — all 10 gating Qs + 6 removal confirmations from Duong. Azir audit findings: `apps/platform/` is the darkstrawberry launcher shell (keep), `apps/shared/` keep (`@shared` alias is live), `dashboards/{dashboard,shared}/` only `.gitkeep` (delete). Orianna fact-check 0 block. Kayn broke down into 31 tasks across 5 PRs.
- **Phase 0 merged** — PR #60 `a34bc02`, two-line workspaces cleanup.
- **PR #61 merged** — T212 API fixtures (anonymized) + encrypted `T212.env.age`, `3b98f34`.
- **Portfolio-tracker plan revised** (`19a9dfd`) — IB API dropped entirely, T212 API optional v1, Claude digest + Gemini chat panel moved to v1. v0 stays CSV-only skeleton.

## Open threads
1. **PR #62 Phase 1 rename — STILL RED at close.** 4 failing checks: Lint+Build, Firebase Preview, 2× xfail-first. Viktor fixed initial Senna findings (stale lockfile, dead gitleaks allowlist) + a latent ccusage 0.8.0 → 18.0.11 version mismatch. More stale `apps/myapps` refs likely lurking. Re-dispatch Viktor next session to diagnose remaining reds.
2. **Portfolio v0 biggest gap: `importCsv` HTTPS callable not exported** from `apps/myapps/functions/src/index.ts` (soon `apps/darkstrawberry-apps/functions/src/index.ts` post-rename). Once Phase 1 lands, wire that up + force base-currency onboarding + deploy onSignIn blocking trigger.
3. **Portfolio Qs still open**: Q1 friend has T212 API beta activation? Q2 friend on Claude Max? Q7 real T212 CSV export needed (Duong's xlsx is a positions snapshot, not trades — API fixtures cover the shape).
4. **P6 migration purge** gated at T+7 (2026-04-26).
5. **Stale-green ADR** (`2026-04-19-stale-green-merge-gap.md`) still approved-not-implemented from S59.

## Watchouts
- **Senna Rule 6 near-miss** — ran `age -d` once during PR #61 review before switching to `tools/decrypt.sh`. Caught herself, logged, no plaintext in context. Worth monitoring.
- **Ekko reviewer-PAT drift** — pushed memory sweep using reviewer PAT because Duongntd got "repo not found" on private strawberry-agents. Identity-boundary concern; revisit.
- **Usage-cap kills background agents silently** — Viktor's first Phase 1 run + both reviewers on PR #61 got killed mid-flight when quota flipped. Re-dispatch is safe; SendMessage to killed agent is not.

## Dangling
- PR #62 `fe3d18c` (Phase 1) — 4 red checks, Senna CHANGES_REQUESTED outstanding.
- Nothing else unpushed post session close.

## Harness note
`end-session` skill was in SessionStart's available list but `Skill` tool returned "Unknown skill" mid-session. Slash-command `/end-session` surfaced the SKILL.md directly and worked. Session-level skill registry drifts — fallback is manual execution or slash-command.
