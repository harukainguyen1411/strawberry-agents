# Last Session — 2026-04-19 (S51, Mac, cli, short, parallel-Evelynns)

Three other Evelynn sessions were running in parallel on this tree. Kept delegation lanes narrow. Closed the two S50 CI reds + knocked out D4 of the deployment-pipeline plan.

## Shipped

- **`release.yml` / deploy-portal** deleted from strawberry-agents — leftover from pre-migration when portal lived here. Commit `a3f1c24`. No downstream deps.
- **Duongntd agent PAT re-minted + re-encrypted.** Fresh classic PAT in `secrets/github_pat.txt` (my first attempt pointed Ekko at the stale `secrets/github-triage-pat.txt` from S49, burned a cycle on a 401 that wasn't the fresh file). Validated with `gh api user --jq .login` before setting secret. `AGENT_GITHUB_TOKEN` updated on strawberry-agents, encrypted blob at `secrets/encrypted/github-triage-pat.age`, commit `94daee6`. `Auto-rebase PR` workflow green on [run 24611260165](https://github.com/harukainguyen1411/strawberry-agents/actions/runs/24611260165).
- **D4 — Discord webhook** closed. Channel `#pipeline-status` in Strawberry server, webhook `strawberry-deploy-notifications`, encrypted to `secrets/encrypted/discord-webhook.age`, GH secret `DISCORD_WEBHOOK_URL` set on both strawberry-app + strawberry-agents. D4 checkbox flipped in `plans/in-progress/2026-04-17-deployment-pipeline-tasks.md`. Commit `86ea239`. Unblocks P1.3 + P1.8.
- **Tests-dashboard ADR** by Azir at `plans/proposed/2026-04-19-tests-dashboard.md` (after two renames: `test-dashboard` → `test-results-dashboard` → `tests-dashboard`). Adopts demo-studio-v3 schema verbatim + `repo`/`runner` fields, Vitest custom Reporter, v1 `file://` local, aggregator in `strawberry-app/dashboards/tests-dashboard/`, v2 SSE (flipped Azir's poll lean). Decisions table filled in — 6 of 10 questions Duong punted to me. Commit `956eed6`.
- **PR #25 (P1.2 `_lib.sh`)** — Ekko unblocked stuck CI by fixing pre-existing `no-unused-expressions` lint in portfolio-tracker + read-tracker router files. Commit `1197767` on the branch. Awaits human review.
- **PR #26 (P1.4 xfail Vitest)** — Vi pushed 2 stuck commits from the prior session + added `QA-Waiver` to PR body + dropped unused import. TDD-gate + Unit Tests + E2E green. Awaits human review.

## Open threads

1. **PR #25 + PR #26 awaiting Duong's review.** Rule 18 — agents can't self-merge.
2. **Promote tests-dashboard ADR** once Duong blesses it. Plan-promote.sh (Rule 7).
3. **TDD-gate no-op bug** — `"tdd": { "enabled": true }` flag missing on `apps/myapps/functions/package.json`, so the TDD-gate workflow skipped Vitest as a green no-op instead of enforcing. Vi caught it. Jayce's lane to fix — should land BEFORE any more Phase 1 tasks so we're not shipping ungated TDD work.
4. **Two ADR-tracked follow-ups** from the tests-dashboard decisions: (a) rename `plans/approved/2026-04-17-test-dashboard-architecture.md` → `...-session-dashboard-architecture.md`; (b) amend approved usage-dashboard plan to depend on `dashboards/_shared/tokens.css`.
5. **Phase 1 remaining:** P1.5 (test-functions.sh), P1.6, P1.7, P1.10, P1.11 ready. P1.3 + P1.8 unblocked by D4 tonight.
6. **From S50 still open:** heartbeat.sh identity audit (`gh auth status` + `git config --global`) not yet implemented; portfolio tracker ADR still proposed awaiting 4 Q's.

## Dangling commits / PRs

- PR #25 strawberry-app — open, needs review
- PR #26 strawberry-app — open, needs review  
- PR #31 strawberry-app — dashboard T4 (usage-dashboard workspace), in flight by parallel Evelynn session, lockfile fix landed at `12a817a`

## Blockers for next session

- None hard. Parallel-Evelynn coordination is the soft one — before dispatching Phase 1 work, confirm with Duong which lanes the other sessions own.

## Key realizations

- **Validate-before-write is cheap.** One `gh api user --jq .login` would have saved a whole Ekko cycle on the PAT dance. Adding to the standard PAT-rotation ritual.
- **When Duong says "the file is in secrets," grep `ls -lt secrets/*.txt` for the freshest mtime, don't assume the obvious filename.** My memory-of-last-session bias pointed me at the wrong file.
- **Parallel Evelynn sessions are a real coordination surface now, not a theoretical one.** Twice tonight a dispatched Sonnet found work already shipped by another session. The recovery (pivot to unblocking CI / pushing stuck commits) was clean, but the dispatch was wasted. Worth a protocol for "ask before executing if plan says ready."
