# Last Session — 2026-04-18/19 (S52, Mac, cli, parallel-stream dashboard)

Parallel stream with another Evelynn running portfolio v0 simultaneously. I drove the Claude usage dashboard v1 execution from `~/Documents/Personal/strawberry-agents` (post-A4 canonical). Zero cross-talk — disjoint trees (mine: `dashboards/usage-dashboard/` + `scripts/usage-dashboard/` in strawberry-app; hers: `apps/portfolio/**`).

## Shipped

- **Dashboard T1-T8 merged** (PRs #21, #23, #24, #27, #30, #31 on harukainguyen1411/strawberry-app). Scaffold + roster generator, agent-scan, merge.mjs (with --roster), build.sh, index.html shell, app.js render.
- **T5/T6/T10 cleared by Jhin, queued for Duong's merge** (PRs #35, #37, #39). Refresh-server, sbu CLI, Playwright smoke.
- **TeamCreate → background pivot mid-session.** Used `dashboard-v1` team for T1-T4+T7+T8, Duong dissolved it, T5/T6/T10 shipped as background one-shots. Less ceremony, same result on serial flows.
- **Jhin caught nine real bugs across six PRs** — prefix-ordering path bug (T2), roster authoritative-source gap (T3), cross-filesystem mv atomicity (T4), /health CORS (T5), `open` rule-10 violation (T6), XSS via innerHTML (T8 — critical). Every one would have shipped without review.

## Open threads for next session

1. **T9 (Seraphine — Refresh button wiring)** — unblocks on PR #35 merge. Last dashboard task. Spawn as background one-shot with the `hidden-attr-vs-Tailwind-flex` follow-up on her plate.
2. **Fixture date horizon on T10** — tests break 2026-05-05 / 2026-05-08 / 2026-05-17 as calendar-anchored sessions exit the 30d window. Dynamic-fixture follow-up (generate `startedAt` relative to `new Date()` at fixture-install time).
3. **README stale on T6 (#37)** — still says "opens via `open` (macOS)" after cross-platform fix. Trivial chore.
4. **Portfolio v0** — the other Evelynn left 10 PRs open (#29/32/33/34/36/38/40/41/42/43/44). Her priority note: **merge #38 first** (Ekko lint fix), then sync main into the rest. Not mine to merge but noted for coordination.
5. **Pre-commit guard on archive path** — S50 open thread still unfilled. Commits to `~/Documents/Personal/strawberry` silently re-open the drift gap. Worth a guard script that refuses commits in the old path.
6. **From S49 backlog**: PR #19 (strawberry-app orphans) still needs harukainguyen1411 re-auth for rule-18 merge. Re-encrypt Duongntd PAT also still pending.

## Dangling commits / PRs

- PRs cleared for merge (dashboard): #35, #37, #39.
- PRs open on portfolio stream (other Evelynn's): #29, #32, #33, #34, #36, #38 (merge first), #40, #41, #42, #43, #44.
- PR #19 (strawberry-app orphans) still pending human merge.
- My close commit pending (transcript + handoff + memory shard + learnings).

## Blockers for next session

None hard. Dashboard stream can resume immediately from T9 once #35 lands. Portfolio coordination continues with the other Evelynn's handoff.

## Key realizations

- **Parallel Evelynns work when tree territory is disjoint.** No state races, no memory conflicts — both streams committed cleanly through the same main. Would not trust it for overlapping code.
- **Rule 18 is pragmatic today, not theoretical.** Every PR ran the same pattern: Jhin advisory LGTM via `gh pr comment` (same-account block on `gh pr review --approve`), Duong merges via browser as harukainguyen1411. Ten successful merges this stream, zero rule-18 workarounds needed. Document this as the working protocol.
- **TeamCreate earns its keep on coordination-heavy work; background spawns win on serial PR→review→merge loops.** Confirmed by dissolving the team mid-flight and shipping the rest faster. Pattern: start with TeamCreate only when agents need to peer-DM or share live task state; default to background one-shots otherwise.
- **"Branch from main, always"** is now a concrete rule from Seraphine's T7 mishap. Executors must not branch from a dependency's unmerged feature branch even if their work depends on it — when the dependency squash-merges, GitHub auto-closes the child PR.
