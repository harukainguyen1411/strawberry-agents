# Last Session — 2026-04-18 (S48, Mac, direct mode, Orianna build-out + TTS experiment)

Shipped Orianna v1 mostly end-to-end in one long session. ADR by Azir, 23 tasks via Kayn, executed by the team. Ended with Orianna's own tasks plan NOT clean (block=7), so final `implemented/` promote did not happen. ADR itself is clean and ready to promote.

## Shipped

- **Orianna agent + infrastructure.** Agent def at `.claude/agents/orianna.md` (both global and project-scoped), scaffolding (profile/memory/learnings/inbox), claim-contract, allowlist.
- **Fact-check gate wired into `plan-promote.sh`** — step 3.5 runs `scripts/orianna-fact-check.sh`. LLM path (claude CLI) with pure-bash fallback (`fact-check-plan.sh`). Blocks promotion on any `block` finding. Cross-repo aware (`apps/**` claims verified against `~/Documents/Personal/strawberry-app/` — cloned this session).
- **Weekly memory audit** — `scripts/orianna-memory-audit.sh` + audit prompt + reconciliation runbook. End-to-end verified by Vi.
- **Roster updates** — `agents/memory/agent-network.md`, `agents/evelynn/CLAUDE.md` delegation tree, repo-root `agents-table.md` and canonical `agents/memory/agents-table.md`.
- **Dashboard ADR** from Azir at `plans/proposed/2026-04-19-claude-usage-dashboard.md` (`a6cd887`). Key discovery: Strawberry agents run as top-level Claude sessions (not Task subagents) — attribution must come from first-message greeting regex. Unique wedge vs. `ccusage`.
- **Statusline wired + trimmed** — final fields: `git | model | ctx | cost | todos | idle`. Removed ci/prs/quota at Duong's request.
- **end-session skill flag flip** — `disable-model-invocation: true → false` (`1d4d929`). Evelynn can now auto-fire on explicit intent.
- **Bugs fixed through dogfood** — three wrong `claude` CLI flags, brace-expansion false positives, report-picker prefix collision, missing `<!-- orianna: ok -->` suppression syntax. All caught by Vi's smoke tests. Commits: `2aacda5`, `a08125b`, `803a7d5`, PR #183 (merged by Duong after billing block), `15f9b44`, `2f59359`.

## Session hiccups worth remembering

- **Session restart dropped Vi mid-run** — had to re-dispatch the final verify step. Background subagents don't survive resume.
- **GitHub Actions billing hard-stop** (same as S46) blocked PR #183 merge normally; Duong resolved manually.
- **Jayce went outside convention** by opening PR #183 for agent-infra scripts (all other Orianna work went direct-to-main per chore: policy). Added review detour but the review caught nothing that TDD didn't.
- **Parallel-agents-on-shared-tree race** — Yuumi's `git add -A` swept Ekko's in-progress `claim-contract.md` into Yuumi's commit. Matches the S44 learning about `git add -A` footgun.
- **Harness restriction** on `.claude/agents/*.md` writes from subagents; Jayce blocked on O1.1, Evelynn had to write orianna.md directly.

## Open threads for next session

1. **Finish Orianna v1 promotion** — tasks plan at block=7 after last dogfood run. Needs:
   - Three more `<!-- orianna: ok -->` markers on lines 550/554/672 (intentionally bogus test-seed paths in task descriptions)
   - Decision on forward-reference false positives (`plans/proposed/2026-04-19-orianna-smoke-bad-plan.md`, `assessments/memory-audits/2026-04-19-memory-audit.md` — described as future outputs)
   - LLM hallucination handling (Vi's last run inserted two block findings for paths that aren't in the file — non-deterministic, may not reappear)
   - Re-run → promote ADR + tasks to `plans/implemented/`
2. **Orianna v1.1 — Claude scheduled routine** for weekly memory audit via `CronCreate`/`schedule` skill. Replaces original v2 GitHub Actions cron. Queued as small follow-up after ship.
3. **Dashboard ADR** at `plans/proposed/2026-04-19-claude-usage-dashboard.md` has 7 open questions waiting for Duong's call before Kayn breaks it down. Most load-bearing: Q1 (local-file vs. Firebase Hosting), Q3 (work-repo scope), Q6 (Max-value yardstick).
4. **From S47 backlog (unchanged):**
   - Phase 4 — replay PRs #152 + #161 into strawberry-app
   - Strawberry-agents migration A2-A7 (filter tree still at `/tmp/strawberry-agents-migration`, not pushed)
   - Deployment pipeline Phase 1 — 8 tasks ready
   - Dependabot cleanup — 4 PRs parked pending CI billing
   - GitHub Actions billing still blocking CI across the repo
5. **Orianna design gap surfaced**: forward-reference outputs (plans that describe future files) need either a new suppression convention or a recognized pattern in the claim extractor. Worth a v1.1 ADR amendment.

## Dangling commits / PRs

- All Orianna commits on main. No dangling PRs.
- Tasks plan at `plans/in-progress/`, ADR at `plans/in-progress/`. Both need promotion once clean.

## Blockers for next session

- None hard — can resume immediately with Yuumi applying the three remaining suppression markers, Vi re-running, then promoting.
- Soft: LLM-hallucination blocks in dogfood are non-deterministic; may need 1-2 re-runs to get a clean result.

## Key realizations

- **Meta-tooling reveals its own need.** Building Orianna without Orianna in place meant every drift class she's meant to catch showed up in the build session itself. Expected in retrospect, painful in real time.
- **Session restart is load-bearing.** Background subagents don't survive — commit early, re-dispatch often.
- **Convention-drift from agents.** Jayce's PR #183 for agent-infra scripts wasn't wrong per Rule 18, but inconsistent with the chore-direct-to-main pattern. Worth documenting when to PR vs. direct in the delegation guidance.
- **The statusline + TTS detour was a net positive.** The statusline stuck (useful daily). TTS confirmed Gemini works but latency on long responses kills the UX; `say` + Siri voices is a macOS dead end for shell hooks. Killed at the right point.
