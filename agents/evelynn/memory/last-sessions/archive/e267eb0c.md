# Last Session — 2026-04-19 (S54, Mac, cli, late-night plan-heavy)

Plan-heavy session. Three ADRs promoted, one hook landed, PR chain unjammed end-to-end.

## Shipped

- **Tests-dashboard ADR** promoted to `plans/approved/2026-04-19-tests-dashboard.md` (commit `e97828d`) after Azir amended to make Playwright a first-class v1 writer (D4b). Kayn decomposed into 7 tasks + 2 follow-ups at `plans/proposed/2026-04-19-tests-dashboard-tasks.md` (`1007c8e`). Yuumi closed 5 DTDs inline (`1db3526`): Vitest `^4`, Playwright `^1`, pytest stub-only, paths confirmed, scope confirmed.
- **Orianna redesign ADR** promoted to `plans/approved/2026-04-19-orianna-role-redesign.md` (commit `a4dda94`). Shifts her from internal grep-anchor checker (now Phase 1, warn-only) to external freshness checker (Phase 2, block). Tools: WebSearch + WebFetch + Context7 + Firecrawl (Duong-added). 20-call budget/plan, 14-day SHA-keyed cache.
- **Subagent-task token attribution ADR** at `plans/proposed/2026-04-19-usage-dashboard-subagent-task-attribution.md` (`a4265f0`). Post-hoc scanner over `~/.claude/projects/<slug>/<session>/subagents/agent-<id>.jsonl` + `.meta.json` — harness already writes the data, no hook needed for capture. v1/v2 phase split: v1 = capture pipeline only (sentinel hook + scanner + aggregate), v2 = Panel 5 UI. All 7 open Qs resolved inline.
- **PostToolUse Agent→TaskCreate reminder hook** wired in `.claude/settings.json` (commit `3c7d3c4`). Every Agent tool call auto-injects "REMINDER: TaskCreate for subagent X — Y. Set owner=X." Verified working across the session.
- **PR #25 green + merged** by Duong. Ekko iterated to green — root cause was `firebase.json` `"public":"dist"` vs `composite-deploy.sh` assembling into `deploy/`. Fix: `sed` in `preview.yml` (commit `4871740`).
- **PR #46 opened** by Viktor — enabled `tdd.enabled:true` on `apps/myapps/package.json` + `apps/myapps/functions/package.json`. The two required CI checks (`tdd-gate.yml`, `unit-tests.yml`) had been silent no-ops for every PR in the functions scope. Walker stops at nearest ancestor so both packages needed the flag.
- **PR #47 opened** by Vi — root-caused the E2E heading bug that was blocking #25/#26/#28/#46. Two pre-existing `e2e.yml` bugs masked by TDD no-op: (a) missing `npm ci` → no Playwright binary, (b) missing `VITE_FIREBASE_*` env vars for the Vite `webServer` build → Firebase config throws on module load, Vue never mounts, every heading assertion fails.

## Open threads

1. **PRs in flight needing Duong's merge:** #26, #28, #46, #47. Merge order: #28 next (has `apps/myapps` build step Ekko added before composite-deploy), then #47 rebases and inherits, then #46 cascade-clears on E2E.
2. **Attribution ADR unpromoted** — Azir's ready at commit `a4265f0`. v1 execution plan needs Kayn/Aphelios breakdown next session.
3. **Tests-dashboard tasks plan unpromoted** — at `plans/proposed/2026-04-19-tests-dashboard-tasks.md`, DTDs all closed, ready for Duong's promotion.
4. **Orianna redesign has no implementation plan yet** — approved ADR but nobody has written the executable task breakdown. Needs Kayn or Lux-follow-up.
5. **Duplicate E2E coverage** (Vi flagged): `e2e.yml` and `myapps-test.yml` now both run Playwright for `apps/myapps`. Worth rationalizing — pick one canonical, scope the other.
6. **Follow-ups still open from S53:** typecheck never wired anywhere (Yuumi's topology audit), 3 `.bats` xfail files orphaned, slug-regression guard missing in agents repo, parallel-Evelynn branch isolation.

## Dangling commits / PRs

- PR #26, #28, #46, #47 — all open on `harukainguyen1411/strawberry-app`, merge chain above.
- Three ADRs promoted via manual bypass (self-referential forward-refs) — remains the working pattern until Orianna redesign is implemented.
- Session close commit pending.

## Blockers for next session

None hard. Open Qs are all execution-ready pending Duong's merge shepherding and plan promotions.

## Key realizations

- **Hooks cannot invoke in-session tools.** Verified via claude-code-guide agent. Closest automation is PostToolUse additionalContext — the hook nudges Claude with text, Claude chooses to call the tool next turn. Good enough for the TaskCreate-on-spawn use case.
- **The harness writes per-spawn attribution data for free.** `~/.claude/projects/<slug>/<session>/subagents/agent-<id>.jsonl` + `.meta.json` for every single subagent spawn, ever. No capture code needed; we just need a scanner. This unblocks the entire per-agent cost observability story.
- **Self-referential ADRs break the current Orianna gate.** Every ADR that describes new scripts/dirs triggers block-severity forward-ref findings. Manual bypass is the pattern until the redesign ships. Filed as the core motivator of the Orianna redesign.
- **Enabling TDD gate exposes latent bugs.** The moment `tdd.enabled:true` landed on myapps, two real E2E workflow bugs surfaced that had been hidden for weeks. Expect more of this as TDD spreads.
- **Parallel-Ekko still violates roster policy** even when disjoint. Dispatched two Ekkos this session (PR #25 loop + hook wiring). No git race materialised but the learning holds — chain serial tasks on one Ekko instead.
