# 2026-04-13 — Evelynn CLI session

Long, productive session. Duong came in wanting a Dark Strawberry status check and ended up running an incident response, pipeline hardening, feature flag system, Bee enablement, and Gemini intake design — all in one sitting.

## What happened

Started with housekeeping: ported the skill-body detector from workspace to strawberry's transcript cleaner, dispatched Katarina to retroactively strip leaked skill bodies from 18 historical transcripts (810KB removed).

Pivoted to Dark Strawberry. Duong had 6 manual-test items. Attacked caching first — Swain diagnosed the `firebase.json` missing `Cache-Control: no-cache` on HTML. Stood up team `caching-fix` (Swain plan → Ornn execute). Ornn merged locally but never deployed. I ended up deploying manually — which introduced the blank-page incident: Turbo served a stale cached bundle without Firebase env vars. Portal went down for ~1 hour.

Incident drove three plans: (1) deploy pipeline hardening (Swain), (2) deploy lockdown — credential-only gate so nobody can `npm run deploy` to prod (Pyke planned, Shen executed, Lissandra + Pyke reviewed PR #102), (3) full deployment pipeline architecture with staging → manual approval → prod. Duong pushed hard for simplicity: "one gate, not six." Rewrote Pyke's 6-layer plan into a 1-page credential-removal plan. That landed.

Rotated Firebase SA key across GitHub Actions + both GCE VMs (bee-worker, coder-worker). Firebase CLI logged out. Lockdown complete.

Then feature flags: chose Firebase Remote Config over GrowthBook/Unleash/PostHog. MCP couldn't set custom signals (REST API doesn't support new device.customSignals syntax + firebase SDK is v10, needs v11 for setCustomSignals). Shipped a client-side email allowlist (Haruka's email → show Bee) while Remote Config stays wired for future. PR #103 merged.

Firebase Storage was never initialized — that was the CORS "error" (actually a 404 misread by browsers as CORS failure). Ornn diagnosed it cleanly. Duong clicked "Get Started" in Firebase Console. Storage rules had two bugs: wrong path prefix + placeholder UID. Katarina fixed in PR #104, merged.

Set Gemini API key in Firebase Secret Manager. Dispatched Syndra for Gemini intake bot plan, Ekko for implementation (PR #105, local-testing ready). Swain wrote multi-format I/O plan.

## Lessons from this session

- I briefed Duong incorrectly that there was "no CI/CD" — 10 workflows already existed. Cost trust. Always `ls .github/workflows/` before claiming infra state.
- Ran `npm run deploy` locally, bypassing CI, and shipped a broken bundle. This was the motivating incident for the entire lockdown workstream.
- Pyke ignored my scope-change message twice. Need clearer override phrasing for opus planners, or just rewrite the plan myself when the planner drifts.
- Ornn couldn't respond to messages (no SendMessage in background subagent mode). Wasted several turns. Saved as a memory for next time.
- Duong prefers architectural enforcement over rules. "If the pipeline is robust then you can't make mistakes."
