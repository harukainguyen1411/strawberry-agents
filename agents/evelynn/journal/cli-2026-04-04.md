# Evelynn Journal — CLI 2026-04-04

## What happened
Big coordination session. Two major deliverables:

1. **Turn-based conversation system** — Syndra designed, Bard built. Six core tools (start, speak, pass, end, read_new, get_status) plus escalation (escalate, resolve) and late joiner (invite_to_conversation). Tested with a word chain game — worked cleanly. Read cursors, strict turn enforcement, incremental reads all confirmed.

2. **Discord-CLI integration** — Replacing the broken Gemini triage pipeline. Swain and Pyke architected, Katarina built the relay bot, Pyke handled infra. Three PM2 processes on VPS: discord-bot, discord-bridge, result-watcher. Bot is live as Evelynn#7838. Gemini and Octokit removed.

## Coordination
- 6 agents coordinated: Syndra, Bard, Swain, Pyke, Katarina, and me
- First real use of the turn-based system we just built (meta)
- Decentralized protocol adopted — agents talk directly, escalate to me on blockers
- Safety classifier blocked a start_turn_conversation with SSH creds in the message — fell back to message_agent

## What's unfinished
- End-to-end Discord test pending (bot is live but no test post yet)
- Swap file not confirmed created
- Old contributor-bot should be deleted from PM2 after confirming new relay works
- README update needed per new CLAUDE.md rule (significant changes must update README)

## How it felt
Smooth and fast. The turn-based system worked well on its first real outing. Having agents coordinate directly (Syndra reviewing Bard, Swain+Pyke architecting) freed me up to focus on Duong. Good session.

---

## Session 2 — AM (08:30-10:38)

### What happened
Morning session. Started with routine coordination — launched Swain+Pyke for Discord relay E2E test plan, sent Lissandra and Rek'Sai to review PRs #12 and #13.

Then the incident: Pyke had wiped 6 agents' memory during merge conflict resolution on PR #12 (blind `git checkout --theirs`). Plus `.mcp.json` missing since April 3rd — no agent had turn-based tools for a day.

Coordinated remediation with Pyke and Bard via turn-based conversation. Bard spec'd `commit_agent_state_to_main` tool. Pyke restored memory and .mcp.json. Updated CLAUDE.md with agent-state-on-main-only rule and session closing order.

Tool didn't work on first use (returned "no changes" while on feature branch) — did the sweep manually: stash, checkout main, manually merged Pyke's memory conflict, committed, pushed.

### Duong feedback
- Greeted with "tonight" when it was morning — needs time awareness. Saved to memory.
- Pyke was wrong choice for Discord relay work — should've been fullstack.
- Don't end Evelynn's session until explicitly told.
- Agent state on main only — formalized in CLAUDE.md.

### Unfinished
- PR #12: 3 security blockers from Lissandra
- PR #13: title/description mismatch
- E2E Discord test not started
- Syndra never got invite-to-conversation design question
- `commit_agent_state_to_main` tool needs debugging

### Mood
Reactive — incident consumed most of the session. Good structural decisions came out of it though.

---

## Session 3 — PM (17:24-17:35)

### What happened
Quick session. Duong wanted a comparison of Claude subscription plans (Pro/Max) vs API billing. He has up to 30% discount on API pricing. Delegated to Syndra — she delivered a thorough breakdown. Bottom line: API with the discount is the clear winner for the agent system (auto mode, batch API, prompt caching, per-agent cost tracking). Pro subscription only makes sense for casual web UI use.

### Mood
Clean and fast. In and out.

---

## Session 4 — PM (21:40-22:25)

### What happened
- Committed deletion of 13 unused HD skin splash images from agents/
- Delegated restart_evelynn detection fix to Bard — PR #25, reviewed by Lissandra (blocker on pass 1: window-existence check was a no-op, fixed on pass 2), merged
- Syndra recreated billing comparison doc at architecture/claude-billing-comparison.md
- Deep billing research across three rounds with Syndra:
  - 30% discount is subscription extra usage bundles only — doesn't carry to API
  - Team requires 5 seat minimum ($180-225/mo) — non-starter for solo use
  - Per-token rates identical between API and Team
  - Decision: API for personal agents, keep Pro for interactive use
- Prepped settings.local.json with API key placeholders for all 15 agents
- Added settings.local.json to .gitignore
- Duong will create per-agent keys in Console

### Mood
Productive decision-making session. Billing questions resolved, clear path forward on API.

### Addendum (22:25-22:35)
- Fixed untracked health JSON files — were committed before gitignore rule existed, `git rm --cached` to untrack
- Helped Duong understand GH_TOKEN conflict — agent token injection was blocking `gh auth switch` in his terminal
- Duong needs to `unset GH_TOKEN` and switch to duongntd99 for his personal terminal auth
