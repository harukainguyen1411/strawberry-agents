# 2026-04-21 (S64, cli, direct mode)

## Compact consolidation 00:00

This session was deep execution and a pivot.

Talon crossed the finish line on Orianna's external-claim verification step — Step E is now live. `ORIANNA_EXTERNAL_BUDGET=15` as the default, the test script green. That plan has been sitting in `in-progress` since the Karma quick-lane run yesterday; it's clean now and properly in `implemented/`. One whole class of Orianna fact-check drift — unverifiable external claims getting waved through on plausibility alone — is now blocked at gate time.

The inbox plan took a harder road. Channels was the right idea at research time; Lux's advisory held up. But the implementation reality didn't: the plugin approach Yuumi tested failed in the harness. Yuumi did the right thing — reverted cleanly, demoted the plan back to `proposed/`, and stopped. No lingering debris. Then Azir took the plan in a different direction entirely: Monitor-based autonomous watcher, 7-day archive TTL. The v3.1 amendment is committed. It's a fundamentally different architecture — no Channels dependency, no plugin layer, just the file system and a running `fsevents`-equivalent watch. Simpler. Ekko is mid-flight on the Orianna fact-check now; I'll have that result when the compact resolves.

The bigger strategic item is the memory-consolidation redesign. Lux's assessment landed and it's convincing: the current single-file `evelynn.md` is showing strain. Long sessions saturate the startup context budget. The proposal is a two-layer structure — an open-threads manifest for hot items, a lazy-loaded INDEX for structured history, individual session shards on disk. Duong answered all four gating questions (1a, 2a, 3a, 4b) — the path is clear. Swain is drafting the ADR now. It'll sit on disk until Duong commits it with the admin bypass (agent-identity is blocked from adding files to `proposed/` under §D12). Once it's committed and signed, I'll route Talon or Viktor at the implementation.

One process lesson emerged from Talon's work that I want to capture clearly: Orianna signatures bind at the moment of signing. Add a section to the plan body after that? The hash changes. All prior signatures become invalid. We got lucky that it was caught during the session rather than at a gate transition. Going forward: any plan that will grow implementation-gate sections needs those sections present as placeholders on the initial draft — before the first signing ceremony. Karma's quick-lane plans in particular need this discipline baked in.

The tree is not fully clean — Swain's ADR is uncommitted and Ekko's fact-check is in flight. But neither of those is something I can unblock from here. The compact is the right move now. Next session picks up from the handoff shard.

--- consolidated by Lissandra (pre-compact) ---

## Compact consolidation 2026-04-21 (second pass)

A lot moved since the first compact.

The plan-authoring freeze is gone. §D12 has been removed — Ekko shipped the change at `68525f1`. Agents can now write to `plans/proposed/` directly without requiring Duong's admin bypass on every addition. That unblocked four ADRs that Swain had been holding: the retrospection dashboard, the coordinator-decision-feedback loop, the daily-agent-repo-audit-routine, and the plan-prelint shift-left quick-lane plan. All four are now committed at `plans/proposed/personal/`. The pipeline is filling up.

The D1A ruling triggered more work than I anticipated. Requiring complex-track plans to stay single-file is architecturally correct — external breakdown and test-plan files create fragmentation and make signature verification ambiguous. But inlining them mid-lifecycle means the body hash changes, which means every prior Orianna signature becomes invalid. I watched the same recovery dance play out twice today — once for the inbox plan, once for the memory-consolidation ADR. Demote, re-sign, amend to fix newly-surfaced Orianna blocks in the inlined content, re-sign again, re-promote. It's not catastrophic but it has a real cost. The pattern is now documented. Anyone running a D1A inline should read the runbook first.

What D1A also exposed is the gap between what the commit-time pre-lint catches and what Orianna checks at sign time. The pre-lint runs on YAML schema and commit prefix. Orianna additionally checks bare filenames, cross-repo tool paths, forward self-references, and external claim verifiability. None of those are caught until promotion time. The result today was over ten amendment commits purely for Orianna block remediation. Karma's quick-lane plan to shift those checks left is the right response — get the cheap catches to author time, before the plan even touches `plan-promote.sh`.

Lux's WebFetch research surfaced something I want to sit with. An injected payload appeared in fetched content during routine research — not a security researcher's controlled experiment, just a page Lux pulled for reference. Lux caught it. But it's a reminder that WebFetch output is untrusted input by definition. The mitigation path is clear: llm-guard with ProtectAI's deberta-v3-base model as the pre-filter, Lakera as the fallback. The audit-routine ADR now includes this defense layer. What I'm noting here is the epistemics: I've treated WebFetch as "mostly safe" in practice. That assumption needs updating. Every fetched payload gets sanitized or I treat it as adversarial.

The inbox plan is at `approved/` now, ready for Viktor. The memory-consolidation ADR is approved too — Rakan has already landed the xfail tests on `feat/coordinator-memory-two-layer-boot`. Managed-agent-dashboard and S1-S2 service boundary are both approved. The plan queue is healthy. What's still sitting in `proposed/` are the four ADRs from this session and the managed-agent-lifecycle work plan. Those need Orianna sign and promotion before implementation can start.

One thing I want to flag for my next self: the audit-routine cost envelope is real. Twenty percent of my daily Pro quota per run is not a number to optimize around — it's a constraint to design for. The ADR captures the scheduling implications. Twice-daily is probably the ceiling before interactive sessions start to feel the pressure.

The tree is in better shape than it's been all session. The compact is right.

--- consolidated by Lissandra (pre-compact) ---

## Compact consolidation 2026-04-21 (third pass)

The backlog finally cleared. Three PRs merged in a single push: prelint shift-left (#12), memory two-layer boot (#13), and the 131-plan archive sweep (#14). The queue was clogged for two prior compacts; now it's clean. PR #15 — the rule-4 staged-diff scoping fix — is sitting with dual approval from Senna and Lucian, 39 tests green. I'm leaving it unmerged only because the session needs to compact; first action on restart is to pull the trigger on that merge.

The BSD awk story from PR #15 is worth capturing here because it's the kind of thing that would have taken me hours to diagnose cold. Talon's first implementation looked right — gawk 3-arg match extracting hunk line ranges. Senna flagged it: BSD awk on macOS doesn't support that syntax, fails silently at compile time, and falls through to the sed fallback. The sed fallback had a greedy-match bug that would return the wrong number whenever hunk headers contained trailing context with a `+` in the function name. Both bugs compounded: the primary path was unreachable on macOS, and the fallback was wrong. Talon fixed it with a simple anchored sed pattern and a regression test for the trailing-context case. That's the right outcome — no gawk dependency, one canonical path.

The more strategically significant thing today was the capacity hit. I watched Duong hit the weekly Opus cap mid-session — not the daily, the weekly. He purchased a second Max seat and we kept moving, but the cost of that interruption was real. Lux's advisory arrived with good timing: the bottleneck is the weekly cap, not session count. More seats don't extend the cap. The high-ROI response is prompt caching — cache_control: ephemeral on stable system prompts and shared includes, 90% discount on cached tokens. That audit hasn't happened yet. It's on the thread list now.

The five plans at proposed/ are the other thing I want to name clearly. Karma's pre-orianna-plan-archive plan is a retroactive case — PR #14 already implemented it, but the plan itself needs to travel from proposed/ to implemented/ via Orianna sign and promotion. That's a slightly unusual sequence. The other four are normal Orianna-gate candidates: agent-feedback-system, retrospection-dashboard, coordinator-decision-feedback, daily-agent-repo-audit-routine. None of those can move to implementation without Duong's approval. I'm not blocking on them; they can wait. But the queue shouldn't idle more than a day or two.

One small warning for my next self: Sona's workspace has a staged rename — the session-state-encapsulation plan file — that's already caused one learnings-write bounce for Senna. Before any cross-coordinator commit touches plans/approved/work/, verify Sona's staged state first. Easy to overlook; worth the ten-second check.

The tree is in better shape than it's been all session. Compact is right.

--- consolidated by Lissandra (pre-compact) ---
