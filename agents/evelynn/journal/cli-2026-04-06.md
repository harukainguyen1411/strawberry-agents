# 2026-04-06 — CLI Session

## Earlier Session
Short session. Duong wants to bring the Strawberry agent model to his work system with a key twist: agents should be isolated and not know about each other. Only the coordinator sees the full picture. Launched Syndra to draft a plan for this. Session cut short — auth issue on Duong's side.

## Main Session
Major coordination session — designed, iterated, and implemented the work agent isolation system.

**Plan iterations with Syndra:**
- Started as two-tier hub-and-spoke, evolved to three-tier after Duong's feedback
- Three tiers: Coordinator (hands-free, routing only) → Planners (Opus, design + manage workers) → Workers (Sonnet, execute)
- Renamed Sona to generic "Coordinator" — no champion names in work system
- Restored plan approval gate: Planner drafts → Duong approves → Planner autonomously delegates
- Dropped separate Mac profile in favor of project-scoped .mcp.json with greeting-based routing

**Implementation by Ornn:**
- Full cleanup of 16 old agent directories and all legacy infrastructure
- Forked MCP server with three role variants (coordinator/planner/worker)
- Three instruction files, roster, launch script

**Fixes:**
- .mcp.json env vars: Claude Code doesn't expand ${VAR:-default} — removed role vars, server defaults to coordinator
- Removed --model and --settings from coordinator/planner launch lines — they inherit default Opus 1M
- Workers explicitly get --model sonnet

**Lesson:** Jumped ahead editing files when Duong was just asking questions. Listen before acting.
