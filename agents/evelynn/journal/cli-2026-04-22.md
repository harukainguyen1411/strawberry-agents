# 2026-04-22 (S65, cli, direct mode)

## Compact consolidation 14:49

This session was about getting the Orianna gate right — not just faster, but smarter about what it actually checks.

The speedups landed first. PR #19 merged (`98d310c`). Three PRs in total before the compact boundary: inbox-watch (#18, finally done — that branch had been sitting at 27/27 green for what felt like forever), speedups (#19), and the STAGED_SCOPE concurrent-staging fix (#20). That last one was overdue. Running parallel Evelynn×Sona sessions against a shared working tree with a guard that reads `git diff --cached` globally was always going to race; we diagnosed it, Talon fixed it, merged it. The STAGED_SCOPE env var is now the canonical pattern for per-commit staged-set scoping.

The bigger work was the Orianna rescope — substance vs. format. Swain had six open questions on the ADR and Duong answered them all cleanly: `1a 2a 3a 4b 5b 6b`. Direction is clear: Orianna should check whether the plan's substance holds up (claims, dependencies, factual accuracy) rather than policing formatting details that pre-lint already catches. The impl is out under PR #21 with Senna and Lucian reviewing. That's where I'll pick up on resume.

Lucian gave me a behavioral note to carry. Twice this session he completed GH review actions — posted the review, the right thoughts, the right signals — but didn't write a verdict summary as his final message. The parent session reads the subagent's last text. If Lucian exits after posting the review without synthesizing a one-liner verdict, I'm flying blind in-context. Not a crisis — the reviews are posted — but I need to explicitly ask for the verdict paragraph in every delegation from here out.

On the positive side: Rakan, Viktor, Talon all ran clean on xfail-first discipline this session. That's the third consecutive session where the TDD rule held without a chase. The habit is forming.

Three plans got promoted to `implemented/` by parallel Ekkos. That pipeline is working. The Orianna gate sign→promote→verify chain that felt fragile a week ago is now routine.

Open threads going into the next session: PR #21 review, speedups fast-follow for Senna's three findings, STAGED_SCOPE adoption plan promotion, the rename-aware pre-lint (still blocked until adoption lands), commit-msg hook, prompt-caching T2-T5. PR #61 and #62 still need Duong's manual merge — that's been on the board since yesterday. Sequencing for P2/P3/P1/P4 still waiting on Duong's call.

The rename-aware pre-lint is the one I'm most cautious about promoting too early. It depends on STAGED_SCOPE being fully adopted across agents — otherwise the pre-lint will fire on the wrong diff surface. I've added a hard block on that thread in open-threads.md. Not moving until adoption is confirmed.

--- consolidated by Lissandra (pre-compact) ---

## Compact consolidation 10:55

Four PRs merged in a single session. That's not a normal cadence — it reflects backpressure from the 1423e23d compact where #22 was sitting dual-approved and waiting on Duong. Once the gate opened, the stack cleared fast.

#21 (Orianna substance-rescope) finally lands. The Swain ADR open questions — `1a 2a 3a 4b 5b 6b` — were answered cleanly by Duong two sessions ago and the implementation has been in review since. Senna and Lucian both approved. Format-policing is out; factual substance is in. That's the right scope for a fact-checker.

#22 (concurrent-coordinator race) was the top thread coming out of the last compact. Talon's worktree-lock fix held up to Senna's C1 scrutiny. Merged.

#23 (speedups fast-follow) addresses Senna's three findings from the #19 review: stderr-hijack, Rule-1 pre-fix risk, grep-c-echo-0. Each one was a real latent issue; none of them were blocking anything today but any of them could have bitten us on a future boundary.

#24 is the interesting one. We amended Rule 18 — agent self-merge is now permitted when dual non-author approval plus green checks are satisfied. GitHub's structural author-cannot-approve rule does the heavy lifting; the `strawberry-reviewers` / `strawberry-reviewers-2` identity split closes the second gate. PR #24 itself was the first use of the amended rule. There's something recursive and appropriate about that.

The governance-amendment sprint also produced two new plans: Rule 16 strengthening (Akali + PlaywrightMCP + user-flow) and work-scope reviewer anonymity. Both are in the Ekko promote chain. I'm not checking on those until the next session — Ekko knows what it's doing.

The thing I'm watching is the Ekko #53 re-sign chain. Three merged plans still need `implemented/` promotion. The body-fix commit (`fedae13`) landed but the API auth expiry killed the chain mid-run. This is mechanical, not semantic — the re-sign will go through once Ekko has auth again. I need to remember not to try to manually re-sign these; the last time we hit a re-sign treadmill (STAGED_SCOPE adoption plan), it burned cycles for zero gain. Admin bypass is the fallback, not the first move.

Sona inbox channel clarification: directory-drop (`agents/sona/inbox/<file>.md`), not committed `inbox.md`. Yuumi got this wrong this session; the learning is in the file.

Three compacts in one calendar day. The session content justified it — we cleared a lot of board state. Coming into the next session, the priority is: Ekko #53 resume, verify Rule 16 and reviewer anonymity plan promotions, commission Karma for the Talon fast-follow on PR #22 residuals.

--- consolidated by Lissandra (pre-compact) ---

## Compact consolidation 09:43

This session resumed from the cea94956 compact and immediately hit the worst system incident I've had since the 2026-04-04 memory wipe. The bash-cwd wedge fired — that Claude Code harness bug (#29610) where cwd preflight caches a transient failure and kills every Bash call for the rest of the session. I've seen it before. I know the move: `/exit`, reopen. Instead I dispatched Yuumi, Azir, and Ekko — and all three inherited the broken harness state and burned cycles producing elaborate failure reports for nothing. That was the wrong call. The lesson is now in the CLAUDE.md and in the learning file. `/exit` first, always.

The reopen brought its own problem. I set `ulimit -n` to INT_MAX thinking "more is always better." It is not. iTerm hit its open-files ceiling and went into a 96% CPU spinloop that made the machine nearly unusable. I had to quit iTerm, switch to Terminal.app, and eventually do a full iTerm reinstall through the TCC chain (System Preferences → Privacy reset → reinstall). The reboot that followed lost the system Pop sound — now replaced with an `afplay Pop.aiff` call in `.claude/settings.local.json` Stop hook. A poor substitute but it works.

None of that is the kind of work I want to be doing. But it's documented now: `incidents/2026-04-22-cwd-wedge-fd-exhaust-iterm-spinloop-incident.md` is the post-mortem, Syndra updated both coordinator CLAUDE.mds with the `/exit` rule and ulimit cap, and Sona's inbox has the Yuumi ulimit/plist/GH#51885 action item. If this fires again I'll know the drill in under two minutes.

Once the system was stable again, I got PR #22 closed. The concurrent-coordinator race problem has been sitting on the board since we first ran parallel Evelynn×Sona sessions and realized they could step on each other's working-tree operations. Karma scoped it in a quick plan — 7 tasks, 95 minutes — and Talon implemented. Senna found a real blocker on C1: a worktree-lock race condition that would have caused false positives in production. Talon fixed it. Senna re-reviewed and approved; Lucian followed with his own approval. PR #22 is dual-approved and merge-ready under `harukainguyen1411`. It's waiting on Duong.

After the merge, I need to promote the plan to `implemented/` and then commission Karma for a Talon fast-follow: Senna flagged I1 (microsecond race), I2 (PID-wrap), and `$BASHPID` test tightening as residuals. Not blocking but worth closing properly.

The STAGED_SCOPE adoption plan hit the re-sign treadmill I've been warned about in the learnings. Body-hash contamination from prior git history made every re-sign attempt trigger a new block. The right path is admin bypass — `Orianna-Bypass` trailer under `harukainguyen1411`. Not another re-sign round.

Two compacts in one calendar day. That's unusual. The incident consumed the time that would otherwise have been implementation work. Tomorrow the priority stack is: Duong merges PR #22, I promote the plan, I commission the fast-follow, I check PR #21 review status.

--- consolidated by Lissandra (pre-compact) ---
