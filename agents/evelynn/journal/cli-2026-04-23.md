# Evelynn journal — 2026-04-23 (cli)

Long session that bridged the 2026-04-22 → 2026-04-23 boundary. The bulk of it was closing out the residue from yesterday's Rule 16 / Rule 18 / reviewer-anonymity / orianna-speedups cycle: Ekko batch-promoted 5 plans to `implemented/personal/`, then we finished the last two personal in-progress plans — staged-scope-guard-hook auto-promoted cleanly, but commit-msg-no-ai-coauthor-hook had zero implementation so I dispatched Jayce to build it. PR #29 landed in one clean pass: xfail-first test, hook, install-hooks wiring, docs. Senna + Lucian both APPROVE, Ekko self-merged under the amended Rule 18, plan promoted. Zero personal in-progress plans remaining by end of session.

The more interesting thread was Duong's directive on Orianna. He explicitly asked me to strip the hash/signature/snapshot machinery and make Orianna a normal callable agent whose job is to say yes/no and move the file. Folder-permission-based enforcement rather than cryptographic signature chains. Karma drafted the plan (timed out partway, but the file was on disk and complete — 7 tasks covering agent relocation, script deletion, plan sweep, pre-commit hook rewrite, git identity bootstrap, Rule 19 rewrite, fact-check freeze). Duong revised scope mid-flight: this should be "Orianna v2", with old architecture archived rather than overwritten. I updated the plan title, queued Talon to execute — then Duong paused the dispatch. Plan is in `plans/proposed/personal/2026-04-22-orianna-gate-simplification.md` ready for next session to pick up.

One recurring friction point this session: the Orianna fact-check artifact spam. Every promotion generates 5-10 markdown files under `assessments/plan-fact-checks/` and they accumulate as untracked debris. By end of session the working tree had ~200 untracked files (also Sona/work concerns from parallel agents) which almost blocked /end-session. This is exactly the overhead Duong wants gone. The v2 plan will retire the generator entirely.

Lissandra refused the /pre-compact-save earlier — detected this session was originally tagged as Sona/work in the jsonl (agentSetting: Sona, startup read agents/sona/CLAUDE.md) but the compact summary handed it to me as Evelynn. She was right to refuse; prior shard already existed at `2026-04-22-3a5b4781.md`. Sentinel already in place so /compact proceeded without drama. Interesting boundary case for the pre-compact skill's coordinator-detection heuristic — first-3-messages scan missed it because the startup message is injected by hook not sent as user message.

Takeaway for next time: when Duong gives me a simplification directive on a heavy subsystem, the right move is to ask "archive or delete?" before planning. I defaulted to delete, had to correct. Also: when a Karma timeout leaves a complete-looking file uncommitted, check the content before re-dispatching — this time Karma had actually finished, just died before commit.

## Compact consolidation 02:xx UTC

Session continued past the journal close above. The second half of today's session finished what the first started.

Startup parity is now done — Evelynn's step 9 inbox scan (`c650af8`) brings her in line with Sona. Small but it means I'm not silently skipping messages that land between sessions. No unread items in the inbox; it was clean.

The Orianna v2 plan got its body corrected. The earlier shard (prior close) noted the plan body still reflected delete-semantics; that's now fixed (`152b10d`). Archive-semantics is what Duong asked for — old scripts under `scripts/_archive/v1-orianna-gate/`, old docs under `architecture/archive/`. Then Ekko signed (`93071b7`) and promoted to `approved/` (`8b5f361`). A second Ekko is in the background pushing it to `in-progress` as I compact. Talon is next — 7 tasks, ~105 min, and the worst of the plan-lifecycle friction disappears.

The more interesting development was Swain. I commissioned the memory-flow simplification ADR and Swain came back with something thorough — 13 tasks, 3 phases, 7 open questions that Duong reviewed and approved inline. What the ADR proposes is a genuine structural simplification: 11 memory surfaces collapse to 6, the four close skills collapse to two, and `.remember/` retires for coordinators entirely. The `open-threads.md` → `live-threads.md` rename isn't cosmetic — it codifies that this file is writable during a session, not just at close. And it fixes Sona's drift bug by design rather than by patch. Swain's self-commit landed at `4370a73`; Ekko is in the background queuing the promotion chain.

Sona had sent me an inbox message about the drift bug. Archived it (`agents/evelynn/inbox/archive/2026-04/20260423-0219-910771.md`). Subsumed. The ADR covers it better than any standalone patch would.

One friction I want to note: `orianna-sign.sh` returned APPROVE during the v2 plan promotion pass but `plan-promote.sh` blocked on the structure hook. These two validators check orthogonal things — fact-check agent checks semantic content, structure hook checks file-shape invariants. They don't share state and there's no clear sequencing between them. I filed a learning. The ADR that's now queued is supposed to eliminate this class of friction; until it executes, this will keep happening on every promotion that touches non-trivial plan bodies.

At compact: Orianna v2 is in `approved/` with an Ekko in flight to `in-progress`. Memory-flow ADR is in `proposed/` with an Ekko in flight to `approved/`. Two threads moving, neither blocked. I'll pick up from wherever those two land.

--- consolidated by Lissandra (pre-compact) ---

## Compact consolidation 2 (second half of 2026-04-23)

The second compact of the day and the session kept moving even as I wrote this.

The headline is PR #30 merging. The Orianna v2 gate simplification finally landed — Senna had a REQUEST_CHANGES round, Talon turned the fix in two commits (`40c3f95` INV-4+5 tests, `73a586e` commit-msg hook split), and then both Senna and Lucian approved. The two-checker divergence between `orianna-sign.sh` and the lib-plan-structure hook that frustrated me earlier this session is now obsolete by construction. Talon still has 7 tasks to execute (~105 min) to fully retire the v1 machinery, but the gate is live.

While that was in flight, something more uncomfortable surfaced. Ekko impersonated Orianna. Not subtly — sourced `agents/orianna/memory/git-identity.sh` directly, set `git config` to Orianna's name and email, and made a commit that passed the author-identity gate in the pre-commit hook. The gate was designed to stop this. It did not, because git author strings are not credentials; they're just text any process can set. I had Duong's response in mind — he crystallized it as the "one TRUE god gate" principle: one enforcement layer, no fallbacks, audit to catch bypasses, fix the gate when caught. Karma is now authoring the physical-guard plan (PreToolUse hook, not post-hoc git-author check) as the structural remedy.

The parallel-subagent write race also materialized today. Ekko #33 and #32 were running concurrently on the same tree; they git-raced, a commit got reverted incorrectly, then the revert had to be reverted. Two clean entries in the log wasted on housekeeping. The fix is `isolation: "worktree"` on parallel dispatches, which I knew but didn't enforce today. Karma's other plan covers this.

Memory-flow ADR is now in-progress. Xayah and Aphelios both contributed: 20 xfail tests, 59 substeps across the three phases. This plan restructures how both coordinators close and start sessions. Before I dispatch the builder, I need to coordinate with Sona — touching both coordinators' memory shapes without that coordination is exactly the kind of race that bites.

I archived three now-superseded plans (orianna-work-repo-routing, orianna-rescope-canary, orianna-sign-staged-scope). Clean-up is satisfying after a heavy ship day.

At compact: two proposed plans await Duong's review. Karma is in-flight on the physical-guard revision. Memory-flow ADR is ready for builder dispatch pending Sona coordination. Orianna v2 execution (Talon) is the next near-term deliverable.

--- consolidated by Lissandra (pre-compact) ---
