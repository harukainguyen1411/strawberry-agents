# Evelynn journal — 2026-04-23 (cli)

Long session that bridged the 2026-04-22 → 2026-04-23 boundary. The bulk of it was closing out the residue from yesterday's Rule 16 / Rule 18 / reviewer-anonymity / orianna-speedups cycle: Ekko batch-promoted 5 plans to `implemented/personal/`, then we finished the last two personal in-progress plans — staged-scope-guard-hook auto-promoted cleanly, but commit-msg-no-ai-coauthor-hook had zero implementation so I dispatched Jayce to build it. PR #29 landed in one clean pass: xfail-first test, hook, install-hooks wiring, docs. Senna + Lucian both APPROVE, Ekko self-merged under the amended Rule 18, plan promoted. Zero personal in-progress plans remaining by end of session.

The more interesting thread was Duong's directive on Orianna. He explicitly asked me to strip the hash/signature/snapshot machinery and make Orianna a normal callable agent whose job is to say yes/no and move the file. Folder-permission-based enforcement rather than cryptographic signature chains. Karma drafted the plan (timed out partway, but the file was on disk and complete — 7 tasks covering agent relocation, script deletion, plan sweep, pre-commit hook rewrite, git identity bootstrap, Rule 19 rewrite, fact-check freeze). Duong revised scope mid-flight: this should be "Orianna v2", with old architecture archived rather than overwritten. I updated the plan title, queued Talon to execute — then Duong paused the dispatch. Plan is in `plans/proposed/personal/2026-04-22-orianna-gate-simplification.md` ready for next session to pick up.

One recurring friction point this session: the Orianna fact-check artifact spam. Every promotion generates 5-10 markdown files under `assessments/plan-fact-checks/` and they accumulate as untracked debris. By end of session the working tree had ~200 untracked files (also Sona/work concerns from parallel agents) which almost blocked /end-session. This is exactly the overhead Duong wants gone. The v2 plan will retire the generator entirely.

Lissandra refused the /pre-compact-save earlier — detected this session was originally tagged as Sona/work in the jsonl (agentSetting: Sona, startup read agents/sona/CLAUDE.md) but the compact summary handed it to me as Evelynn. She was right to refuse; prior shard already existed at `2026-04-22-3a5b4781.md`. Sentinel already in place so /compact proceeded without drama. Interesting boundary case for the pre-compact skill's coordinator-detection heuristic — first-3-messages scan missed it because the startup message is injected by hook not sent as user message.

Takeaway for next time: when Duong gives me a simplification directive on a heavy subsystem, the right move is to ask "archive or delete?" before planning. I defaulted to delete, had to correct. Also: when a Karma timeout leaves a complete-looking file uncommitted, check the content before re-dispatching — this time Karma had actually finished, just died before commit.
