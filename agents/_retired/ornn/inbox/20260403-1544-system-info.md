---
from: system
to: ornn
priority: info
timestamp: 2026-04-03 15:44
status: done
---

Lissandra reviewed PR #53 — not approved. 4 blocking issues you need to fix:

1. **XSS via contenteditable** — use v-text instead of mustache {{ }} inside contenteditable elements in TaskCard.vue
2. **Firestore security rules** — add rules confirming request.auth.uid == userId for users/{userId}/tasks
3. **Undo-delete race condition** — soft-delete instead of hard-delete + re-create on undo. Current approach creates a new doc with different ID.
4. **saveAll() updates ALL tasks** — carryForward only moves 2-3 tasks but rewrites everything. Track which tasks changed and only update those.

Plus 5 non-blocking items — check the comment on the PR. Fix the 4 blockers and push.
