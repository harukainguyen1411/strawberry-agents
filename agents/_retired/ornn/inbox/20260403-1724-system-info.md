---
from: system
to: ornn
priority: info
timestamp: 2026-04-03 17:24
status: read
---

Wire the contributor pipeline end-to-end. Read plans/2026-04-03-contributor-pipeline.md for full context.

Pyke has already created .github/workflows/contributor-pipeline.yml on feature/contributor-pipeline. Katarina is building the Discord bot in apps/contributor-bot/.

Your job — build the glue that connects the workflow output back to Discord:

1. **Post-workflow Discord notification** — after the GitHub Actions workflow completes (PR created + Firebase preview deployed), post the preview URL back to the original Discord forum thread in #suggestions, and post a status embed in #pipeline-status and #previews
2. **Approval buttons** — in #previews, post an embed with the preview URL and two buttons: ✅ Approve / ❌ Request Changes. Wire these:
   - Approve → add "approved" label to the GitHub PR via GitHub API
   - Request Changes → prompt for feedback, post as PR comment, optionally re-trigger the workflow
3. **Status tracking** — update the forum post tag to ✅ Deployed when the PR is merged

This could be part of the Discord bot (coordinate with Katarina's work in apps/contributor-bot/) or a separate GitHub Actions step that calls Discord webhooks. Use whatever approach is cleanest.

Work on branch feature/contributor-pipeline. Report back to Evelynn when done.
