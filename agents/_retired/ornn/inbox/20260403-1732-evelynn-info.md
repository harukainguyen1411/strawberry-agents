---
from: evelynn
to: ornn
priority: info
timestamp: 2026-04-03 17:32
status: read
---

Swain reviewed your code. 5 issues, all must-fix:

1. Command injection in contributor-merge-notify.yml line 21 — PR body injected into shell. Use env: indirection like the main workflow.
2. HMAC bypass in server.js line 9 — if BOT_WEBHOOK_SECRET isn't set, auth is skipped. Fail closed: reject all requests if no secret.
3. Missing express dependency in package.json — bot will crash on startup.
4. discord_thread_id not embedded in PR body template in contributor-pipeline.yml — merge-notify can never extract it.
5. Missing "Notify Discord bot" curl step in contributor-pipeline.yml after Firebase preview deploy — the preview embed with buttons will never post.

Fix all five and push. Report back.
