
## Session — 2026-04-06 (Work Agent Isolation)

Spent the session implementing the work agent isolation plan across three iterations as Syndra refined the design.

**What happened:**
- First pass: built `~/.claude-work/` profile, forked `work-agent-manager` MCP with coordinator/planner/worker role split, wrote three instruction files, wired `agents/.mcp.json`. Role isolation verified — workers blocked from coordinator tools.
- Second pass: Evelynn relayed plan update — drop the separate profile, use greeting-based routing like Strawberry, move `.mcp.json` to workspace root. Deleted `~/.claude-work/`, created `workspace/.mcp.json`, appended agent routing to `workspace/CLAUDE.md`, simplified launcher.
- Third pass: Phase 5 cleanup — deleted all 16 old agent dirs (azir, bard, camille, etc.), old shared files (memory/, conversations/, roster.md), old scripts, old MCPs (linear/), old infra (health/, usage/, services/, work/). Fixed `.mcp.json` to absolute paths. Updated launcher to match final plan spec.

**Result:** Work agents/ directory now contains only new system files. The three-tier coordinator/planner/worker architecture is in place.

**Open threads:**
- The work MCP server now has a `scripts/start.sh` launcher — Duong wired that up after I finished. Good.
- Worker instructions updated by Duong to add `report_to_coordinator` tool (workers can be assigned directly by coordinator).
- Planner instructions tightened: no polling after delegation, trust the inbox system.
