# Journal — CLI — 2026-04-04

## Session Summary

Tasked by Evelynn to implement the shared task board from Swain's architecture plan.

## What happened

**Phase 1 — Firebase MCP tools:** Added 5 tools to the evelynn MCP server (task_list, task_create, task_update, task_delete, task_changes). Lazy Firebase Admin SDK init, service account auth, SERVER_TIMESTAMP for writes. Blocked briefly on credentials — Duong added the service account JSON and UID, then I tested the full create/list/delete cycle successfully.

**Phase 2 — Vue app changes:** Extended the Task interface with updatedBy/source/notes/category fields. Updated all store mutations to set `updatedBy: 'duong'`, addTask sets `source: 'app'`. Added notes display to TaskCard (italic violet text).

**PR #21** was created. Lissandra reviewed — 3 blockers: no input validation, missing composite Firestore index, no existence check before update/delete. Fixed all three and pushed. PR is ready for re-review.

## Complications

- Accidentally committed to main instead of the feature branch. Had to use stash + checkout + merge to get the commit onto the feature branch.
- The stash approach caused the review fixes to land on main first, requiring careful handling to keep the feature branch in sync.
- Going forward: use `git worktree` for feature branch work when already on a branch. The shared working directory makes this tricky.

## Open threads

- PR #21 waiting on Lissandra re-review
- End-to-end test (Evelynn creates task → Vue sees it) not yet done — requires Evelynn to actually call the tools after merge
