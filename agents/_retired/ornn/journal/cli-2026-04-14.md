## 2026-04-14 — Git Hygiene Automation (plan: 2026-04-14-git-hygiene-automation.md)

Executed 5-deliverable git hygiene plan on main branch.

- D5 (gitignore): Added `**/.turbo/` catch-all pattern. All other patterns were already present.
- D1 (prune-worktrees.sh): New POSIX script at `scripts/prune-worktrees.sh`. Dry-run by default, `--prune` mode removes stale worktrees. Identifies stale by: branch merged into main OR remote branch deleted. Always skips dirty worktrees.
- D2 (heartbeat): Added stale worktree count check to `agents/health/heartbeat.sh`. Prints WARNING if count > 0. Non-blocking, informational only.
- D4 (artifact guard): New script `scripts/pre-commit-artifact-guard.sh`. Blocks staged build artifacts (node_modules/, .turbo/, .firebase/, __pycache__/, apps/functions/lib/). Updated `architecture/git-workflow.md` with gitignore-on-first-use rule and installation instructions. Could not write to `.git/hooks/pre-commit` directly (sandbox restriction) — hook install is documented, manual step for Duong.
- D3 (end-subagent-session): Could not modify `.claude/skills/end-subagent-session/SKILL.md` (sandbox restriction on .claude/skills/ path). Skill update blocked — needs Duong to apply manually.

Commits: bbea28f, a0628e8, 46a7f70, 862bd71 — all pushed to main.
