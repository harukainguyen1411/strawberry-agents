# 2026-04-04 — CLI Session

## What happened
- Launched by Evelynn to review PR #13 (chore: remove myapps, contributor-bot, and legacy infra files)
- Pulled the diff — only 4 agent memory files changed. Zero deletions.
- Title/description promises major file removals that aren't in the diff. Mismatch.
- Posted review as GitHub comment. Reported to Evelynn via conversation.
- Re-read updated agent-network.md — now uses turn-based conversations as primary comms.
- Duong corrected: always use `gh pr comment`, not `gh pr review`. Saved to memory.

## Observations
- The memory edits themselves are clean — just compaction and session log updates for Bard, Lissandra, Pyke, Syndra.
- Protocol has shifted to turn-based conversations. Need to use `start_turn_conversation` / `speak_in_turn` going forward.

---

# 2026-04-04 — CLI Session (continued, later)

## What happened

**PR #16 — Telegram relay bridge (first + second pass)**
- First pass: 5 findings. Critical — bot token committed in plan file. High — first-run flush was a no-op (saved offset "0" after `?offset=-1`, so all old messages would flood through on next poll). Also pipe-subshell masked errors, error handler logged empty string, no signal trap.
- Second pass: all 5 fixed. Bard used here-string instead of pipe, fixed flush to extract and save `last_id + 1`, used `exit_code=$?` pattern, added signal trap.

**PR #23 — Agent GitHub token injection (first + second pass)**
- First pass: 4 findings. Critical — token raw-interpolated into AppleScript `write text "..."` string (shell + AppleScript injection vector). High — export command printed literal token to terminal scrollback. Medium — no file permission check. Low — blast radius to child processes undocumented.
- Second pass: all 4 fixed. Token now read via `$(cat 'path')` at runtime, path shell-quoted, AppleScript escaping added, `stat` permission check added, inline comment documents intentional propagation.

## Feedback received
- Evelynn reminded: always message back when a task is complete (protocol rule #7). First PR #16 review was posted without reporting. Corrected from then on.
