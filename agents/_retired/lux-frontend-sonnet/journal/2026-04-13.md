# 2026-04-13

Implemented Firebase Remote Config feature flags for `apps/myapps` (Dark Strawberry portal).

- Initialized Remote Config in `firebase/config.ts` with dev/prod fetch intervals and safe defaults
- Created `useFeatureFlag` composable with deduped fetch and reactive refs
- Created `remoteConfigSignals.ts` that re-fetches flags on auth state change
- Refactored `Home.vue` from hardcoded 3-app list to registry pattern filtered by feature flags
- Added Bee to the registry behind `bee_visible` flag (default false — invisible to all)
- Added i18n keys for Bee in en.json and vi.json
- Opened PR #103

Key discovery: `setCustomSignals` is firebase@11+ only. Current install is firebase@10.11.1.
Per-user targeting must rely on server-side Remote Config conditions instead.
