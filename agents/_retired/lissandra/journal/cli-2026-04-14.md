# Lissandra — 2026-04-14

## Session summary

Reviewed PR #105 (feat-bee-gemini-intake — Bee Gemini conversational intake bot).

Findings:
- M1 (MEDIUM): `fileRef` from client passed to `bucket.file()` without prefix validation — path traversal on Cloud Storage.
- M2 (MEDIUM): `beeIntakeSubmit` has no idempotency guard — double-call creates duplicate GitHub Issues.
- L1 (LOW): Token budget force path appends synthetic message to Firestore before user message, creating consecutive user turns in history.
- L2 (LOW): INTRO string duplicated in backend and frontend; intro filter in `beeIntakeTurn` is fragile (index-based).
- L3 (LOW): GCS paths written to GitHub issue bodies — minor path disclosure.
- L4 (LOW): `fileRef` can be URL-injected client-side; server-side validation (M1) is the only guard.

Verdict: changes requested. Review posted as `gh pr comment 105`.

## Re-review session (s24)

Re-reviewed PR #105 commit a8d8a7d (Fiora's fixes). Verified all 6 findings:
- M1: confirmed pre-existing guard (`startsWith(\`bee-temp/\${uid}/\`)`).
- M2: confirmed pre-existing guard (`session.issueNumber` early-return).
- L1: forceMsg removed from Firestore writes; injected in-memory only after user message. Noted residual: two consecutive user turns still sent to Gemini when budget hit — pre-existing design question, not worsened.
- L2: `BEE_INTRO_MESSAGE` extracted to module-level constant; filter updated to content-match.
- L3: gs:// path redacted from issue body; session ID reference used instead.
- L4: client-side regex `/^bee-temp\/[^/]+\/.+$/` validates fileRef before use.

Verdict: all findings addressed. Approved. Comment posted to PR #105.
