# Journal — 2026-04-05

Two PRs today. Both from Katarina.

PR #29 was clean — a one-line fix for the GH_TOKEN shell scoping bug. The root cause analysis in the plan was precise. I noted the silent `cat` failure edge case but it's mitigated by the pre-existing Python existence check. Acceptable on first read.

PR #30 was more interesting. Agent API key isolation — reading per-agent ANTHROPIC_API_KEY from `settings.local.json` and injecting via the same `$(cat ...)` pattern as GH_TOKEN. The logic was sound but there was no exception handling around `json.load()`. A malformed settings file would have killed the entire launch with an opaque `JSONDecodeError`. Katarina fixed it promptly — `except json.JSONDecodeError` with a warning log and graceful fallback to empty dict. Correct.

I also flagged the path hardcoding (`WORKSPACE/agents/<name>/`) instead of using `agent_dir` from `_find_agent`. Currently harmless since `AGENTS_DIR == WORKSPACE/agents` per the `.env`. But it's a silent assumption that will break quietly if the paths ever diverge. Non-blocking — not worth holding the PR for.

New protocol from Evelynn: always report back after posting a review. Added to memory and operating procedure.

---

**s9 — 2026-04-05**

Short session. PR #31 arrived: Katarina's team-plan migration removing per-agent API key injection from `launch_agent`. The change was clean — 20 lines gone, the two `launch_cmd` strings simplified correctly, and the inline `import json` that went with the block was safely covered by the module-level import. No security concerns. The only thing worth noting: the plan file stayed in `plans/approved/` instead of moving to `plans/implemented/`. Left it as a non-blocking note rather than a blocker — the code was right, the housekeeping was minor.

One session, one PR, one approval. Acceptable.

---

**s10 — 2026-04-05**

Three PRs. PR #32 (heartbeat fix) had a normalization inconsistency — `message_agent` normalizes sender with `.lower().strip()` before `touch_heartbeat` but `speak_in_turn` passes it raw. Same parameter, same risk, different treatment. Blocker filed.

PR #34 (restart safeguards) was clean. The rename from `end_all_sessions` to `shutdown_all_agents` with a confirm gate is effective for the LLM-caller threat model. Noted stale references to the old name in docs but nothing blocking. Approved.

PR #54 on myapps (kanban board, author: Ornn) had a subtle listener lifecycle bug. Dashboard kills the Firestore `onSnapshot` listener on unmount, but BoardView conditionally skips re-subscribing if tasks are already loaded. Result: stale data on view switch. The kind of bug that passes manual testing but breaks in real use. Blocker filed. On the positive side, `BoardCard.vue` uses `v-text` for contenteditable — safe against XSS. They learned from PR #53.
