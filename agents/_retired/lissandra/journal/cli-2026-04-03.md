# 2026-04-03 — CLI Session

## What happened
First session. Launched by Evelynn to review PR #5 (feature/ops-separation).

## Review: PR #5
Initial review found 6 issues:
1. heartbeat.sh copied without idempotency check
2. heartbeat.sh shouldn't be in ops (it's a tool, not state)
3. chmod 700 only on root dir, subdirs got umask defaults
4. Redundant `ops/` gitignore entry (ops is outside repo)
5. No cleanup path for migrated originals
6. CLAUDE.md still references old heartbeat path

Pyke fixed all 6. Re-reviewed — clean. Approved via comment (can't formally approve own PR).

## Observations
- Pyke also made the agent list dynamic instead of hardcoded. Good instinct.
- Used `umask 077` instead of recursive chmod — cleaner solution than what I suggested.
- The separation of durable vs ephemeral state is the right architectural call.

## Session 2 — PR #8 Review (migrate-ops-improvements)
Launched by system inbox. Single commit extending migrate-ops.sh to cover journal/ and last-session.md.

Clean PR. Mirrors existing patterns. No bugs, no security issues. Noted three non-blocking observations: empty dir creation for agents without journals, .md-only glob for journal files, and post-migration path divergence for last-session.md reads. Approved first-pass.

Reported findings to Evelynn via conversation.

## Session 3 — PR #53 Review (myapps tasklist app)
Big PR — full Task List app: Vue 3 + TypeScript + Tailwind + Firestore. Weekly calendar grid, drag-drop, undo, dark mode, i18n.

### Pass 1 (Ornn's original code)
Found 9 issues — 4 blocking, 5 non-blocking:
- **Blocking:** XSS via contenteditable, missing Firestore security rules, undo-delete race condition (new doc ID on restore), saveAll batching every task on carry-forward.
- **Non-blocking:** isMobile not reactive, today not updating past midnight, undo toast key collision, no touch drag visual feedback, no Firestore error handling.

Could not post to GitHub — permission denied for agent-initiated external writes. Reported via conversation.

### Pass 2 (Katarina's UI fixes — 5790f96)
4 changes: whole-card drag, done tasks inline, dropdown opens above, tablet breakpoint. Found 1 blocking regression: touch/mobile drag-and-drop completely removed (touch handlers deleted, HTML5 drag doesn't work on touch). Dead guard code with non-existent CSS classes noted.

### Pass 3 (Katarina's touch fix — 09e5445)
Touch drag restored with 15px threshold, clone ghost, drag-over highlighting. Clean fix. Approved the touch portion.

### Pass 4 (Ornn's blocker fixes — 99493ec + 91ab144)
All 4 blockers resolved: v-text for XSS, firestore.rules added, soft-delete with _deleted flag preserving doc IDs, saveTasks only updates carried-forward tasks. Verified touch handlers present at branch HEAD.

Two non-blocking notes: soft-deleted docs accumulate (need cleanup-on-load), and tab close during undo window leaves _deleted docs forever.

**Final verdict: Clear to merge.**

## Reflections
- GitHub comment permission is a recurring issue. Need Duong to grant `gh pr comment` permission or find another workflow.
- This was a thorough multi-pass review across two authors' commits. The back-and-forth worked well — each pass caught real issues and each fix was clean.
- Katarina's touch regression was a good catch. Removing touch handlers without replacement is exactly the kind of thing that slips through when you're focused on desktop UX.
- Ornn's soft-delete approach is solid but has a cleanup gap. Not blocking but will bite eventually.

## Session 4 — PR #11 Review + Rek'Sai Fix

Two tasks from system inbox.

### PR #11 (contributor-pipeline)
GitHub Actions workflow + VPS runbook for contributor pipeline. Discord → GitHub dispatch → self-hosted runner → Claude Code → Firebase preview.

3 blockers, 9 non-blocking. All blockers are security:
1. Command injection via `${{ }}` expression interpolation of unsanitized Discord inputs in shell context
2. Persistent self-hosted runner with full credentials (Firebase, GitHub, SSH) — one compromised run poisons all future runs
3. Unrestricted `Bash` in Claude Code's `--allowedTools` on that same credentialed machine

Posted as PR comment (can't request-changes on own repo).

### Rek'Sai iTerm Fix
Dynamic profile had name `RekSai` but `server.py` uses `.capitalize()` → `Reksai`. Changed profile name in dynamic profiles JSON and plist to match.
