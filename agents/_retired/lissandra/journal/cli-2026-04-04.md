# 2026-04-04 — CLI Session

## What happened
- Launched by Evelynn to review PR #12 (feat: Discord relay with two-pass triage and delegation system)
- Read the full diff (~2700 lines): discord-relay bot, discord-bridge.sh, result-watcher.sh, ecosystem.config.js, turn-based conversation system in agent-manager, plans, README rewrite, agent memory updates
- Posted review as GitHub comment with 3 blockers and 7 non-blocking items
- Reported back to Evelynn via message_agent

## Key findings
- B1: `--dangerously-skip-permissions` in delegation pass — same class as PR #11 finding. User-controlled Discord content gets full system access after only a single LLM triage pass.
- B2: Heredoc delimiter collision in discord-bridge.sh — shell injection vector
- B3: TOCTOU race in PID-based delegation lock

## Notes
- Duong explicitly instructed never to auto-close sessions. Saved as feedback memory.
- The turn-based conversation system is well-designed but the frontmatter parser has edge cases (N7, N9).

---

## Session 2 — Afternoon

Heavy day. Eight PRs reviewed across two sessions, all part of a large system build-out by Evelynn/Bard/Ornn.

### PRs reviewed

**#15** (evelynn-mcp-and-flexible-conversations) — 3 blockers: sender enforcement theater, stash pop failures silent, massive code duplication. All fixed by Bard. Approved second pass.

**#17** (restart_evelynn tool) — 3 blockers: misleading break in find_agent_session, reliance on Claude CLI internal JSONL format, race condition on 4s sleep. Fixed. Approved.

**#18** (restart notification) — No blockers. Timing note (notification before session alive), redundant import, silent exception.

**#19** (Telegram bridge v2) — 2 blockers: minute-level filename collision (messages lost), no backoff on rapid polls. Fixed with seconds + random hex, sleep 1. Also session name vs window name fix. Approved.

**#20** (context health monitoring) — No blockers. Clean. Noted stale session_start after restart will cause false restart-now recommendations.

**#21** (Firebase task board) — 3 blockers: no input validation, missing composite Firestore index, no existence check before update/delete. All fixed. Approved.

**#22** (task delegation tracking) — 1 blocker: delegation ID race via glob-count. Fixed with seconds + random hex. Approved.

**#24** (restart notification timing) — No blockers. Poll loop added for session readiness detection. Approved after second pass.

### Patterns observed today
- The glob-count ID generation anti-pattern appeared twice (#19, #22) — flagged both times, both fixed the same way.
- Bard is fast and responsive to feedback — typically turns around fixes before the next inbox message arrives.
- Silent exception swallowing (`except Exception: pass`) is a recurring style in this codebase. Worth watching.

---

## Session 3 — Evening

### PR reviewed
**#25** (restart-detection-fix) — Bard's fix for false negative detection in restart_evelynn. 1 blocker: window-existence check was a no-op (iTerm window never disappears during restart, so `session_detected` always True, "uncertain" path was dead code). 2 non-blocking: inconsistent exit-wait still using unreliable iTerm name check, silent exception swallow persisting. Bard fixed all three in one push — removed detection loop entirely, replaced exit-wait with fixed delay, logged exceptions. Approved pass 2.
