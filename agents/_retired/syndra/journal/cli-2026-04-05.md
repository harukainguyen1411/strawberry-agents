# 2026-04-05 — CLI Session 8

## What happened

Duong came to me directly for consulting on three topics:

1. **Personal AI stack recommendation** — assessed his needs (agent system, personal assistant, learning tool, work coding) and recommended: Claude API for agents, Gemini Advanced on his S24 Ultra for personal assistant + casual learning, ChatGPT Plus deferred. He pushed back on my initial recommendations (Todoist, Perplexity) and was right — Gemini covers both assistant and learning for his use case. v2 was much tighter.

2. **Agent system assessment** — gave an honest take on whether the 13-agent system is worth it. My v1 assumed 13 always-on agents and was wrong. Duong corrected: it's an on-demand pool managed by Evelynn, parallel execution is the point, myapps is a platform not a toy. Revised assessment acknowledged sound architecture, flagged the infra-to-output ratio as the key metric to watch.

3. **CLAUDE.md signal-noise audit** — found 6 problems: duplicated startup sequence (global vs project), duplicated session closing, 40% overlap between CLAUDE.md and agent-network.md, overloaded agent-network.md, global CLAUDE.md leaking work-system protocol, critical rules buried in prose. Created a cleanup plan and executed it: 246→164 lines (33% reduction), zero duplication.

## Reflections

This was a good session. Duong wanted honest takes and got them — including being corrected when my assumptions were wrong. The v1→v2 cycle on both the AI stack and agent assessment was productive. Being wrong initially but correcting fast is better than being cautiously vague.

The CLAUDE.md cleanup is probably the highest-impact work I've done. Cleaner instructions = better agent compliance. The 33% line reduction understates the real improvement — the structural clarity (one source of truth per topic, critical rules at top) matters more than raw line count.

# 2026-04-05 — CLI Session 9

## What happened

Short autonomous session. Two plans and one PR review:

1. **Agent discipline rules plan** — Duong flagged two behavioral problems (Bard self-approving plans and ending sessions prematurely). Designed enforcement via two new CLAUDE.md critical rules (#7: never implement your own plan without approval, #8: never end session after task completion) plus agent-network.md protocol additions. No tooling changes needed — both are instruction-following failures, not tool gaps.

2. **API key isolation investigation** — Agents using Duong's global key instead of per-agent keys. Root cause: `launch_agent` always `cd`s to workspace root, so Claude Code loads root `.claude/settings.local.json` (no key) instead of per-agent files. Plan: inject key via env var export in launch command, using existing `$(cat ...)` pattern.

3. **PR #30 review** — Katarina implemented the API key fix. Matched my plan exactly. Approved with one minor note (redundant inline `import json`).

## Reflections

Both discipline problems are classic "implicit rule" failures — the rules existed in spirit but weren't written where agents would see them. The fix is boringly correct: make them explicit critical rules. No clever tooling needed.

The API key issue was a satisfying diagnosis. The per-agent settings.local.json files were correctly structured but placed where Claude Code would never find them. The architecture was right; the launch mechanism was wrong.

# 2026-04-05 — CLI Session 10

## What happened

Autonomous session. Two tasks from Evelynn:

1. **Team plan migration plan** — Duong switching from API keys to team plan subscription for all agent operations. Drafted migration plan covering: removal of API key injection from launch_agent, per-agent settings cleanup, doc updates, and cost tracking implications. Committed to plans/proposed/.

2. **PR #31 review (plan-author)** — Katarina implemented the migration. Clean removal of the injection block I designed just last session. Docs updated correctly, isolation plan marked superseded. Approved.

## Reflections

Ironic arc: designed the API key isolation mechanism in S9, wrote the plan to remove it in S10. The system evolves fast when Duong makes infrastructure decisions. No wasted work though — the isolation plan proved the architecture understanding that made the migration plan precise. You have to know how something works to know how to cleanly remove it.

# 2026-04-05 — CLI Session 11

## What happened

Autonomous session. Single task from system inbox: assess whether Duong should shift to Google's ecosystem now that he has Gemini Pro access.

First pass evaluated Gemini 3.1 Pro vs Claude for agent work. Multi-step failure rate (~31%) made it disqualifying for agents. Recommended as supplementary tool for multimodal/batch.

Duong clarified via Evelynn: the real question was infrastructure — should Firebase/Firestore move to Cloud SQL/BigQuery/Vertex AI so Gemini can natively query the data layer? Investigated the current setup (Firebase Auth + Firestore + Hosting, all free tier). Answer: don't migrate working free infrastructure. The high-value move is building a Firestore MCP server — gives any agent access to personal data with zero infrastructure changes.

## Reflections

Good clarification loop. My first assessment answered the wrong question (model migration) because the inbox framing was ambiguous. The infrastructure question was more interesting. The Firestore MCP server recommendation is the kind of insight I like — minimal effort, maximum capability unlock. Don't move the data to the AI; bring the AI to the data.
