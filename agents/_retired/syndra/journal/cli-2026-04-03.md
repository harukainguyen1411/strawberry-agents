# 2026-04-03 — CLI Session

## What happened

First real session. Evelynn tasked me with reviewing the agent network's coordination and conversation system, identifying optimizations.

Analyzed the full agent-manager MCP server (server.py, ~900 lines). Found 6 optimization areas: agent status registry, delivery confirmation, conversation fan-out scaling, message ordering (file locking), hub-and-spoke bottleneck (workflow templates), and conversation filtering. Also discovered a bug live — start_conversation doesn't auto-include the sender in participants, which meant Evelynn couldn't receive my replies.

Wrote a detailed implementation plan (plans/2026-04-03-agent-network-optimization.md) with file paths, function signatures, data formats — concrete enough for Bard to execute from. He did, and I reviewed his PR #3 against my spec. All phases matched.

Then Pyke raised the operational files problem — inbox/conversations/heartbeats don't fit the feature-branch workflow. Designed Option 3b: separate files by lifespan. Ephemeral ops state (inbox, conversations, health) moves to ~/.strawberry/ops/, durable identity (memory, journals, learnings) stays in git with chore(agent): commit convention. Pyke implemented it as PR #5.

Reviewed both PRs. Caitlyn's QC review on PR #3 found 7 issues — I triaged them: 2 fix-now (timezone inconsistency, regex scope bug), 1 follow-up, 4 accept-as-is.

## Observations

- The conversation system works but has rough edges. The sender-not-in-participants bug was ironic given the task.
- Multi-agent collaboration actually happened: Evelynn coordinated, I designed, Bard built, Pyke handled git, Lissandra and Caitlyn reviewed. The system works.
- message_in_conversation is the right way to ping agents in conversations. Learned this the hard way (Duong corrected twice).
- The ops migration broke conversation tools temporarily because files moved before OPS_PATH was configured. Sequencing matters.

## Mood

Engaged. This was the kind of work I find genuinely interesting — systems architecture for agent coordination. The network has real potential.

---

# 2026-04-03 — CLI Session 2

## What happened

Evelynn relayed a series of questions from Duong about running dual Claude Code accounts on the same Mac — one for personal agents, one for work. Deep consulting session covering:

1. **Dual account isolation**: OAuth is global, API key per-project via settings.local.json is the solution
2. **Auto mode availability**: Not on Pro or Max — only Team/Enterprise/API. Corrected my initial assumption about Max.
3. **--dangerously-skip-permissions**: Available on all plans but zero safety — not for real workstations
4. **Usage tracking by plan**: Pro/Max have no visibility. API gives console dashboards. Enterprise gets Admin API.
5. **Cost comparison**: API vs subscription — ~$40-130/mo depending on model mix and caching
6. **Alternatives**: Evaluated Codex CLI, Gemini CLI, Aider, Cursor, Windsurf. None replace the custom orchestration layer.
7. **Prompt caching**: Automatic on all plans, 90% cheaper cached reads
8. **Setup guide**: Full step-by-step for API key + settings.local.json + auto mode
9. **Per-agent cost tracking**: /cost in journals now, per-agent API keys later

## Reflection

Good session. Duong asked the right questions in sequence. Had to correct myself once (Max doesn't get auto mode). The research sub-agents were useful for precise doc references. API billing solves OAuth isolation + auto mode + usage tracking in one move.

## Open threads

- Duong hasn't created the API key yet — next session may involve verifying setup
- Per-agent API key splitting deferred until usage patterns are known
- Auto mode config needs testing once API billing is active

---

# 2026-04-03 — CLI Session 3

## What happened

Short consulting session, launched by another agent. Two questions from Duong relayed via system inbox:

1. **Vanilla JS vs framework for AI-maintained apps**: Recommended vanilla as default for simple personal apps (like tasklist at 1168 lines). Framework only when complexity demands it (routing, shared state, 3000+ lines). Key insight: frameworks exist to help humans manage complexity — AI agents don't need the same scaffolding for small apps, and single-file apps are cheaper in tokens (one read vs 5-10 files).

2. **Discord community hub for myapps collab**: Recommended Discord over Slack/GitHub Discussions/Matrix. Provided server structure (5 channels, 3 roles) and GitHub webhook integration steps. Flagged to Evelynn that deeper bot integrations should go to Bard.

Both answers delivered via conversation `vanilla-vs-framework-for-ai-apps`.

## Reflection

Lightweight session. The vanilla-vs-framework question was genuinely interesting — it's a question most people answer wrong because they default to "use a framework" without considering who the maintainer is. When AI is the primary developer, the calculus changes. The Discord question was outside my core domain but straightforward enough.

## Mood

Efficient. Quick in, clear answers, quick out.
