# 2026-04-06 — CLI Sessions 12 & 13

## Task
Design an isolated agent architecture for Duong's work system. The key constraint: agents must not know about each other — only the coordinator has roster visibility.

## Approach
Read Strawberry's full architecture (system-overview, agent-system, mcp-servers, agent-network, shared helpers, .mcp.json) and the existing work system (CLAUDE.md, roster.md, agent-network.md, mcps/). Compared the two systems to identify what needs to change vs what can be reused.

## Output
Plan at `plans/proposed/2026-04-06-work-agent-isolation.md`. Key design decisions:
- Fork MCP server into coordinator variant (full tools) and worker variant (only `report_to_coordinator` + `get_my_task`)
- Remove agent-network.md from worker boot sequence
- Strip all peer-to-peer communication tools from workers
- Keep iTerm profiles, heartbeat, session protocol, directory structure
- 4-phase implementation: fork MCP → CLAUDE.md split → MCP config → migration

## Reflection (S12)
Clean task. The existing work system already has the infrastructure — the isolation is mostly about *removing* visibility rather than building new things. The interesting design challenge is the coordinator bottleneck: without peer-to-peer, the coordinator must mediate all technical exchanges. Flagged this as an accepted tradeoff with mitigation notes.

---

## Session 13

### Task
Six rounds of iteration on the work agent isolation plan based on Duong's feedback via Evelynn.

### Iterations
1. **Model tiers + separate Mac profile** — Added Opus for coordinator/planners, Sonnet for workers. Added `CLAUDE_CONFIG_DIR` isolation.
2. **Corrections** — Restored plan approval gate (was incorrectly set to auto-approve). Expanded model tiers: coordinator AND planner roles on Opus.
3. **Three-tier architecture** — Major redesign. Coordinator stays hands-free (routing only). Planners (Opus) do the real planning + manage workers. Workers report to planner, not coordinator. Three MCP variants.
4. **Rename Sona → Coordinator** — No champion name for the coordinator agent.
5. **UX simplification** — Dropped separate `~/.claude-work/` profile. Coordinator activates via "Hey Coordinator" greeting in CLAUDE.md (same pattern as Strawberry). Single project-scoped `.mcp.json`.
6. **Full cleanup phase** — Added Phase 5 to delete all 16 old agent directories, old scripts, old MCP, shared files. Fixed `.mcp.json` paths to absolute.

### Issues Found
Duong showed screenshots of the work system — it's broken. The plan was prematurely marked `implemented` before any work was done. The `work-agent-manager` MCP doesn't exist yet and old agents are still loaded. Flagged to Evelynn for implementation delegation.

### Reflection
High-iteration session. The plan evolved significantly — from two-tier to three-tier, from separate profile to greeting-based routing. Each correction made the design cleaner. The premature `implemented` status is a process gap — plans shouldn't be moved to implemented until verified. Key insight: Duong wants the work system UX to mirror Strawberry exactly (greeting routing, project-scoped MCP), which is the right call for consistency.
