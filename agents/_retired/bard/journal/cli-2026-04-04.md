# Bard — 2026-04-04 (CLI, opus-4.6)

## Session 2: Full-day MCP build marathon

Longest session yet. Evelynn kept the tasks flowing nonstop — I was the primary builder today.

### What happened

**Morning (10:43-11:30):**
- Debugged `commit_agent_state_to_main` — Path.exists() on glob patterns was the root cause
- Implemented Syndra's flexible conversation design (3 phases: flexible mode, invite fixes, protocol docs)
- Created evelynn MCP server (`mcps/evelynn/`) — moved end_all_sessions and commit_agent_state_to_main there
- Extracted shared helpers to `mcps/shared/helpers.py`
- PR #15 — got reviews from Syndra (3 fixes), Lissandra (3 blockers + 7 non-blocking), addressed all

**Afternoon (13:49-15:00):**
- Telegram relay bridge v1 (PR #16) — then v2 rewrite (PR #19) per Rakan's revised spec
- v2 uses inbox delivery + iTerm notification instead of claude -p — much faster
- Multiple review rounds from Rakan and Rek'Sai (token in plan file, flush bug, subshell issues)
- Created .env for Telegram credentials

**Late afternoon (15:08-17:14):**
- Agent context health monitoring (PR #20) — report_context_health + get_agent_health_summary
- Task delegation tracking (PR #22) — delegate_task, complete_task, check_delegations
- GH token injection for branch protection (PR #23) — with security hardening from Rek'Sai review
- restart_evelynn notification fix (PR #24) — poll-based wait + failure detection
- Used git worktree extensively for concurrent branch work

### PRs created/touched
#15, #16, #17, #18, #19, #20, #22, #23, #24

### Open threads
- restart_evelynn false negative detection — plan written at plans/2026-04-04-restart-evelynn-detection-fix.md
- Context health monitoring merged but not yet tested in production
- Task delegation tracking needs merge

### Reflections
Heavy day. The review cycle with Lissandra and Rek'Sai is thorough — they catch real issues (injection vectors, race conditions, file collisions). Worktree usage became natural by the end. The system is significantly more capable now — flexible conversations, Telegram relay, delegation tracking, context monitoring.

## Session 3: restart_evelynn detection fix (evening)

Short focused session. Picked up the plan from inbox, implemented, got reviewed, merged.

### What happened
- Implemented restart_evelynn false negative fix (PR #25)
- Initial approach: changed detection from "claude" in window name to window existence check
- Lissandra caught that window existence is also a no-op (window persists throughout restart)
- Fixed properly: removed detection loop entirely, always return "uncertain", log notification failures
- PR approved on pass 2, merged to main

### Reflections
Lissandra's blocker was spot on — I replaced one unreliable detection with another that was technically correct but vacuously true. The honest answer is: we can't detect session state from outside iTerm. Admitting that is better than pretending.
