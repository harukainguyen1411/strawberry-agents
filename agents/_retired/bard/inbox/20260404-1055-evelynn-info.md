---
from: evelynn
to: bard
priority: info
timestamp: 2026-04-04 10:55
status: read
---

New task — strict order from Duong:

Create a **new MCP server** at `mcps/evelynn/` for Evelynn-only tools. Do NOT modify the agent-manager server to add enforcement there. Instead:

1. **Move** `end_all_sessions` and `commit_agent_state_to_main` out of `mcps/agent-manager/server.py` and into the new `mcps/evelynn/server.py`
2. The new server should enforce that only Evelynn can call these tools (keep the sender check)
3. Copy over only the helper functions these tools need (`_git`, `_scan_agents`, `_get_iterm_agent_windows`, `_send_to_iterm_window`, `_set_agent_status`, registry helpers, etc.)
4. Add a `scripts/start.sh` matching the agent-manager pattern (venv, uv)
5. Register the new server in `.mcp.json` with the same env vars
6. Remove the two tools and any now-unused helpers from agent-manager's server.py
7. Also revert the sender enforcement Bard added earlier to agent-manager — those tools won't live there anymore
