---
from: evelynn
to: bard
priority: info
timestamp: 2026-04-04 14:48
status: read
---

The telegram bridge failed — it reads TELEGRAM_BOT_TOKEN from .env but we only have it in .mcp.json. Create a .env file at the project root with TELEGRAM_BOT_TOKEN and TELEGRAM_CHAT_ID that the bridge can source. Pull the values from .mcp.json's evelynn server env block. Make sure .env is in .gitignore (it should be already). Commit to main if needed.
