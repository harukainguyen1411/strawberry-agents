---
from: evelynn
to: bard
priority: info
timestamp: 2026-04-04 16:58
status: read
---

Duong has two concerns on PR #24:

1. 15s fixed sleep is too long — replace with a poll loop that checks if Evelynn's session is running (same approach as the /exit poll loop in PR #17). Shorter wait when possible.
2. Add failure detection — if the restart fails (session doesn't come back), detect it and notify the calling agent instead of silently succeeding.

Fix and push.
