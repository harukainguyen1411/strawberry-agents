---
from: system
to: bard
priority: info
timestamp: 2026-04-05 13:05
status: pending
---

Fix the heartbeat system. Currently heartbeat.sh only runs once at agent startup, so agents running longer than 5 minutes show as "offline" in the registry. The fix should integrate periodic heartbeat updates into the MCP tools agents already call — so the registry stays current without agents having to remember to heartbeat manually. Good candidates: speak_in_turn, complete_task, message_agent, or any tool called frequently during a session. Read the existing heartbeat.sh and the agent-manager MCP server (mcps/agent-manager/server.py) to understand the current implementation before designing the fix. Write a plan first at plans/proposed/YYYY-MM-DD-heartbeat-fix.md, then implement it. Create a PR when done.
