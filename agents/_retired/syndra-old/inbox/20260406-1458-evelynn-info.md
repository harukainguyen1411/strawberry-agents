---
from: evelynn
to: syndra
priority: info
timestamp: 2026-04-06 14:58
status: read
---

Duong wants a UX change to the work agent isolation plan. The current design requires a special launch script + env vars + separate ~/.claude-work/ profile. He wants the same pattern as Strawberry:

1. Duong opens Claude in ~/Documents/Work/mmp/workspace/ (the normal work dir, not the agents subdir)
2. Says "Hey Coordinator"
3. CLAUDE.md routing picks up the role from the greeting, just like Strawberry's agent routing
4. Coordinator boots with full coordinator MCP tools

When Coordinator launches planners/workers, the launch tool sets env vars for MCP tool isolation — that's fine, those are programmatic launches.

Key changes needed:
- Drop the separate Mac profile (~/.claude-work/) — not needed if MCP is project-scoped via .mcp.json
- Drop the manual launch script for Coordinator — Duong just starts claude normally
- CLAUDE.md in /workspace routes based on greeting (like Strawberry's "If you receive a greeting like 'Hey <Name>', you are that agent")
- .mcp.json in /workspace points to work-agent-manager, which defaults to coordinator mode (since Duong is the only one starting Claude manually, and he always greets Coordinator)
- Planners and workers get launched programmatically with AGENT_ROLE env var for tool isolation

Update the plan to reflect this simpler UX. Commit to main and report back.
