---
from: system
to: katarina
priority: info
timestamp: 2026-04-03 15:37
status: read
---

New task. Ornn's Vue migration of the tasklist is at https://github.com/Duongntd/myapps/pull/53. It's missing some of Neeko's UI changes. Clone the myapps repo, check out Ornn's branch, and apply these 4 differences:

1. **Whole-card drag** — make the entire card draggable instead of just the grip handle. Prevent conflicts with inline editing via stopPropagation on editable fields/buttons.
2. **Done tasks inline in day columns** — remove the separate done section, render done tasks sorted below active tasks within their day column.
3. **Dropdown bottom-edge overflow** — status dropdown should open above the badge (bottom: calc(100% + 4px)) to avoid hiding behind other cards.
4. **Tablet breakpoint at 1024px** — add a mid-range breakpoint with horizontal scroll layout (Ornn currently only has 768px).

Reference Neeko's implementation at /Users/duongntd99/Documents/Personal/strawberry/tasklist/tasklist.html for how she did each of these. Push to Ornn's branch on the myapps PR.
