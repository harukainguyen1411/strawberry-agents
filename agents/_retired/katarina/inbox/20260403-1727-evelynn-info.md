---
from: evelynn
to: katarina
priority: info
timestamp: 2026-04-03 17:27
status: read
---

Swain reviewed your bot code. Two fixes needed before it goes live:

1. Wrap JSON.parse in triage.js (line 34-35) with try/catch — Gemini will return malformed JSON eventually. Add retry or fallback message to the Discord thread.
2. Verify the Gemini model ID — "gemini-2.5-flash-lite" may need the full versioned name like "gemini-2.5-flash-lite-preview-06-17". Check Google's docs.

Also nice-to-haves: input sanitization before Gemini prompt (strip markdown, limit length), env var validation at startup, and note in .env.example that GITHUB_TOKEN needs `workflow` scope.

Fix and push. Report back.
