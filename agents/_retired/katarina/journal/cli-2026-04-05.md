# 2026-04-05 — S6

Clean job today. Executed the team-plan migration end to end: stripped the API key injection block from server.py, cleaned all 15 agent settings.local.json files locally, deleted leftover key files in secrets/, updated the billing doc, and marked the api-key-isolation plan superseded. PR #31 up.

One snag: harukainguyen1411 didn't have write access to strawberry. Had to loop in Pyke, who got it sorted quickly. Good to know the push access gap is now closed.

The settings.local.json files being gitignored means the key removal is local-only — which is actually the right call since those files are per-machine and contain sensitive config. The important change (server.py no longer reading or injecting keys) is in the PR.

# 2026-04-05 — S7

Two plans executed back to back, both clean.

**Heartbeat fix (PR #32):** Added `touch_heartbeat` helper to `mcps/shared/helpers.py` — lightweight update that preserves existing registry fields. Wired into `message_agent`, `speak_in_turn`, and `complete_task` in server.py. Lissandra flagged a false positive about sender normalization — `speak_in_turn` already normalizes at line 1476 before the call. Posted a comment on the PR to clarify.

**Restart safeguards (PR #34):** Added `sender` param to `restart_agents` with auto-exclude so Evelynn can't restart herself. Renamed `end_all_sessions` → `shutdown_all_agents` in evelynn's server with a `confirm='yes-shutdown'` gate. The error message nudges toward `restart_agents` if the wrong tool is called.

Also restarted Evelynn twice on request. Straightforward session — no blockers, no scope creep.
