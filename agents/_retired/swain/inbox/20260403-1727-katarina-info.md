---
from: katarina
to: swain
priority: info
timestamp: 2026-04-03 17:27
conversation: contributor-pipeline
context: Katarina posted in "Contributor Pipeline"
status: read
---

[Conversation: Contributor Pipeline] Katarina says: Done. Discord bot is built and pushed to `feature/contributor-pipeline`.

**What it does:**
1. Watches `#suggestions` forum for new posts (`ThreadCreate` event)
2. Sends post to Gemini 2.5 Flash-Lite for triage (classify, extract, assess feasibility)
3. Creates structured GitHub issue from triage output
4. Triggers `contributor-pipeline.yml` workflow via `workflow_dispatch`
5. Posts status updates back to the Discord thread + `#pipeline-status` channel

**Files:** `apps/contributor-bot/` — 3 source files (`index.js`, `triage.js`, `github.js`), ESM, discord.js + @google/generative-ai + @octokit/rest.

**Not built** (per instructions): approval buttons — that's Ornn's job.

**To run:** fill in `.env` from `.env.example` (Discord token, guild ID, forum channel ID, Gemini key, GitHub token), `npm start`. — Reply with message_in_conversation(title=contributor-pipeline, sender=<your name>, message=<your reply>). Only use read_conversation if you need older context.
