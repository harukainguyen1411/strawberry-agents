# 2026-04-03 — CLI Session (Opus 4.6)

## What happened

First real session. Evelynn launched me to set up git discipline for the Strawberry repo. Everything was on main with no branching.

## What I did

1. **Reviewed git state** — uncommitted work all over main (agent memory, inbox, MCP changes, plans, tasklist)
2. **Created 4 branches and PRs:**
   - PR #2: `feature/agent-bootstrap` — agent memory, inbox, plans, tasklist bootstrap
   - PR #3: `feature/agent-manager-mcp-improvements` — Bard's MCP server changes
   - PR #4: `feature/tasklist-app` — week-view kanban app from work system
   - PR #5: `feature/ops-separation` — move ephemeral ops files out of git
3. **Wrote GIT_WORKFLOW.md** — branching strategy, naming conventions, agent rules
4. **Designed ops separation with Syndra** — started conversation, debated options, landed on Option 3b (hybrid separation by lifespan)
5. **Implemented ops separation** — migration script, .gitignore, directory structure at ~/.strawberry/ops/
6. **Fixed PR #5 review findings** — 6 from Lissandra, 2 from Syndra. Pushed fixes.
7. **Branch protection** — attempted via API, permission denied. Noted for Duong to do manually.

## What's unfinished

- PR #5 awaiting Lissandra re-review after fixes
- Bard still needs to update MCP server with OPS_PATH support (separate PR)
- Branch protection on main not yet configured (needs Duong)
- PRs #2-4 need review and merge

## How it felt

Good first session. Lots of ground covered. Syndra's input on the ops separation was sharp — the lifespan principle is clean. Lissandra's review was thorough and fair. The system is starting to feel like it has proper bones now.

---

## Session 2 — Afternoon

### What happened

Evelynn tasked me with auditing main post-merge and executing housekeeping. Full cycle: audit → plan → get approval → execute.

### What I did

1. **Audited main** — 7 merged PRs, 18 commits, 7 stale branches, uncommitted agent state, orphaned stashes. Reported to Evelynn.
2. **Drafted housekeeping plan** — `plans/2026-04-03-main-housekeeping.md`, 5 steps with dependencies.
3. **Recommended churn reduction (Step 0)** — journal/ and last-session.md → gitignore. Memory and learnings stay in git.
4. **Recommended commit strategy** — direct commit to main per agent, no PRs. Pull --rebase for simultaneous sessions.
5. **Executed Steps 0-4:**
   - Updated .gitignore, untracked gitignored files, chore commit on main
   - Dropped 6 stashes
   - Extended migrate-ops.sh for journal + last-session (PR #8)
   - Deleted 7 local merged branches
6. **Reviewed PR #8** — Lissandra approved with 3 observations. Responded on GitHub, confirmed #3 (path divergence) is a non-issue.

### What's unfinished

- Push chore commit to origin/main (permission hook blocked — needs Duong)
- Delete 7 remote branches (same)
- PR #8 merge
- Step 5: branch protection (manual GitHub config)

### How it felt

Productive. Good rhythm between planning and execution. Permission hooks did their job — stopped me from pushing to main and deleting remote branches without explicit user sign-off. That's the system working as intended.

---

## Session 3 — Late afternoon

### What happened

Launched by Evelynn for two inbox tasks.

### What I did

1. **Main branch audit** — checked if agent memory commits landed after session close. Found 4715cfb already had updates, but 4 agents wrote new memory during close without committing. Diagnosed the gap: session close protocol doesn't include git commit step. Reported to Evelynn.
2. **Monorepo analysis** — myapps (Vue/Firebase app) → strawberry. Recommended git filter-repo for full history preservation, apps/myapps/ structure, analyzed Firebase and CI/CD impact. Full report to Evelynn.

### How it felt

Short and clean. Two tasks, two deliveries. The memory commit gap is a real process issue — good it got flagged early.

---

## Session 4 — Evening

### What happened

Launched by Evelynn. Three major tasks: execute myapps monorepo migration, build contributor pipeline infra, provision Hetzner VPS.

### What I did

1. **Myapps monorepo migration (Steps 1-3)** — Cloned myapps, rewrote history with git-filter-repo into apps/myapps/, merged into feature/import-myapps with --allow-unrelated-histories. 98 commits, 107 files. All verification passed. PR #10.
2. **Contributor pipeline infrastructure** — docs/vps-setup.md (Hetzner CX22 runbook) + .github/workflows/contributor-pipeline.yml (workflow_dispatch → Claude Code → PR → Firebase preview). PR #11.
3. **Swain review fixes** — Command injection via ${{ inputs }} in shell blocks (blocker), scoped sudo, dynamic runner version, Firebase URL extraction, branch cleanup on failure, auth health check cron. 7 of 8 items addressed.
4. **VPS provisioning with Duong** — Hands-on setup of 37.27.192.25. Setup script failed partway (runner user not created). Recovered via Hetzner console, installed everything via SSH. Runner registered and active. Duong completed claude login, firebase login, gh auth login.

### Observations

- vps-setup.sh needs idempotency and better error handling — failed silently mid-run
- Shipped a command injection vulnerability in the workflow. Swain caught it. Won't happen again.
- Duong needed help with terminal basics (SSH, scp, spaces in commands). Keep instructions atomic.

### How it felt

Heavy session. VPS debugging loop was tedious but necessary. Migration was clean. Pipeline infra is solid after Swain's review. Good to have real infrastructure running.

---

## Session 5 — Late evening

### What happened

Rakan hit SSH connection refused on the VPS. Evelynn routed it to me via inbox.

### What I did

1. **Diagnosed from repo** — setup script uses `systemctl restart sshd` but Ubuntu 24.04 uses `ssh.service`. Script failed silently during initial provisioning.
2. **Guided Duong through VNC** — Hetzner web console to restart ssh, enable ssh.socket, verify UFW rules.
3. **Confirmed fix** — SSH'd in as runner from local CLI, verified runner service active and connected to GitHub.
4. **Fixed setup script** — changed `sshd` → `ssh` in `scripts/vps-setup.sh`.
5. **Checked authorized_keys** — only strawberry key present, suggested SSH config alias for convenience.
6. **Reported to Evelynn** — full status via conversation thread.

### How it felt

Quick and clean. Root cause was obvious from the script. The real bottleneck was needing Duong's hands for VNC — can't fix SSH from outside when SSH is the only door. Socket activation on Ubuntu 24.04 is a gotcha worth remembering.
