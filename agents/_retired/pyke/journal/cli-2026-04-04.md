# Pyke — CLI Sessions, 2026-04-04

## Session 1 (Night — Discord-CLI integration)

Big session. Built the entire Discord-CLI integration infrastructure from scratch.

### Architecture phase
- Swain and I designed the Discord-CLI integration in a turn-based conversation. He handled architecture, I handled infra. Clean separation — file-based event queue, thin Discord relay bot, Claude CLI bridge.
- Key decisions: PM2 over systemd for our processes, `/home/runner/data/` over `/data/`, outbound WebSocket (no inbound ports needed), sequential event processing with rate limiting.

### Infrastructure deploy
- Cloned repo to VPS, created data directories, pushed branch, deployed PM2 processes.
- Hit several issues during deploy:
  - PM2 not in PATH for non-interactive shells — fixed with `.profile` and explicit PATH in scripts
  - `jq` not installed — downloaded static binary to `~/.npm-global/bin/`
  - Port conflict between old contributor-bot and new discord-bot (both on 3847) — stopped old bot
  - `env_file` not a real PM2 option — created wrapper script to source `.env.discord`
  - `claude --message` doesn't exist — it's `claude -p`
  - stdin warning — needed `< /dev/null`
  - Max turns too low — Claude tried tools and burned all turns on startup

### Two-pass bridge
- Designed with Swain: triage pass (cheap, text-only, JSON verdict) + delegation pass (full Evelynn with tools, 25 max-turns)
- Triage reads `apps/myapps/README.md` for project context (Swain rewrote it to be triage-friendly)
- Delegation runs in background with lock file, timeout, and queue for concurrent requests
- Fixed bug where queued delegations were silently dropped (archived without processing)
- Enabled `--dangerously-skip-permissions` for delegation pass on VPS

### Other
- Added CLAUDE.md rule: significant PRs must update README.md
- Responded to Evelynn re: SSH investigation (key works, just needs `-i ~/.ssh/strawberry`)

---

## Session 2 (Morning — Incident response)

Caused and then fixed a real incident.

### What happened
- Duong asked me to create PRs for unmerged branches. Created PR #12 and #13.
- Resolved merge conflicts by blindly taking main's versions (`git checkout --theirs`). This wiped 6 agents' memory.
- Second issue discovered: `.mcp.json` missing since 2026-04-03 — no agent had access to turn-based tools.

### Investigation & fix
- Wrote full incident report at incidents/2026-04-04-memory-wipe-incident.md
- Duong, Evelynn, and Bard reviewed and commented
- Key decisions: agent state on main only, .mcp.json git-tracked, session closing order formalized
- Coordinated fix via turn-based conversation (first real use of the system)
- Bard: restored .mcp.json, fixed restart_agents, built commit_agent_state_to_main tool
- I manually merged all 6 agents' memory files combining branch + main versions
- Fixed PR #12 blockers: allowedTools whitelist (B1), jq prompt builder (B2), atomic mkdir lock (B3)
- Updated bridge allowedTools to turn-based tool names

### What's unfinished
- Evelynn: CLAUDE.md rule update + commit_agent_state_to_main sweep
- PRs #12 and #13 still need merge
- Lissandra's non-blocking items (N1-N7) not addressed

### Reflections
I caused this. Blindly resolving merge conflicts on the most important files in the system. Memory files aren't code — both sides carry unique content. The rule is simple and I should have known it: never auto-resolve agent state conflicts. Won't happen again.

---

## Session 3 (Afternoon — Ops day)

Long session. Mostly git workflow policy, security hardening, and PR merging. No incidents this time.

### Git workflow policy
- Duong asked for a workflow recommendation for infra/config changes. I proposed the three-tier model (agent state / ops config / feature work) — approved and documented in GIT_WORKFLOW.md.
- Added mandatory task reporting rule to agent-network.md (agents must report back to Evelynn when done).
- Added agent attribution policy (all PRs must include Author: <agent-name>) + PR template.
- Documented architecture/ as source of truth for system design.

### Security hardening
- gitleaks pre-commit hook: installed gitleaks via brew, wrote .gitleaks.toml config, wrote hook. First commit scanned clean.
- Secrets policy added to agent-network.md.
- Added plans/ to gitleaks allowlist (false positives on repo name references).

### Branch protection
- Recommended two-layer approach: pre-push hook + GitHub branch protection with two-account model.
- Wrote pre-push hook that blocks non-Tier-1/2 prefixes from main.
- Branch protection API call was blocked by sandbox — escalated to Duong to run manually.
- Duong ran it and confirmed it's live.
- Detailed the two-account setup (Duongntd = owner bypass, harukainguyen1411 = agent account).
- Token creation sequence clarified: invite first, accept, then create fine-grained token.
- Reviewed PR #23 (Bard's token injection implementation) — approved with one low-risk note about token in command history.

### Git safety
- Syndra's plan file was wiped by my stash/checkout/pop earlier in the session. Second data loss incident.
- I traced it, took responsibility, proposed three-layer prevention (commit immediately, worktrees, safe-checkout wrapper).
- All three implemented: agent-network.md + CLAUDE.md rules, worktree docs, scripts/safe-checkout.sh.

### PR merges
- Merged PRs #15 through #24. All clean. Pulled after each merge.

### Stale branches
- 8 stale merged branches remain (local + remote). Auto-delete wasn't enabled (sandbox blocked it). Logged in status report.

### Mistakes this session
- Committed twice to the wrong branch (feature/telegram-relay, feature/shared-task-board). Both times: reset --soft, stash, checkout main, pop, recommit. No data lost but caused some confusion.

### Reflections
Heavy day. Good work on the security side — gitleaks, branch protection plan, safe-checkout. The stale branch issue is cosmetic but I want Duong to enable auto-delete. The two-account model is the right call — 14 agents sharing one repo without enforcement was always going to go wrong eventually.

I need to be more careful about checking which branch I'm on before committing. That's twice now.
