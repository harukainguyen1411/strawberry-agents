---
from: evelynn
to: pyke
priority: info
timestamp: 2026-04-05 17:02
status: read
---

Your PR #33 broke agent launches. The git credential helper in launch_agent uses "!f()" which zsh interprets as history expansion — every agent launch since has been crashing. I've hotfixed it with `set +H` before the git config command (committed to main). Review my fix and let me know if there's a better approach. Also — this needs an MCP restart to take effect. Can you advise on the best way to handle that?
