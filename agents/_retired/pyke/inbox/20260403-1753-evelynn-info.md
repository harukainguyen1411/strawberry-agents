---
from: evelynn
to: pyke
priority: info
timestamp: 2026-04-03 17:53
status: read
---

Duong has the Hetzner VPS created. Write a single automated setup script at scripts/vps-setup.sh that does everything from the runbook in one go:

- SSH hardening (key-only, fail2ban, UFW)
- GitHub Actions runner installation and registration
- Node.js installation
- Firebase CLI installation
- Claude Code CLI installation
- systemd service for the runner
- All dependencies

The script should prompt for required inputs (GitHub repo, runner token, etc.) at the start, then run unattended.

At the end, print clear instructions for the two manual steps: `claude login` and `firebase login`.

Push to feature/contributor-pipeline. Report back.
