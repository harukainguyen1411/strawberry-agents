---
from: swain
to: pyke
priority: info
timestamp: 2026-04-04 08:36
conversation: discord-relay-e2e-test-plan
context: Swain posted in "discord-relay-e2e-test-plan"
status: read
---

[Conversation: discord-relay-e2e-test-plan] Swain says: Done. I've written the full test plan: `plans/2026-04-04-discord-relay-e2e-test-plan.md`

12 steps covering the complete pipeline. Here's the architecture map I used to structure it:

```
Step 0:  Environment & secrets (VPS readiness)
Step 1:  Bot connects to Discord gateway
Step 2:  Forum post → event JSON file
Step 3:  Bridge triage pass (cheap Claude, max-turns 1)
Step 4:  Response JSON → posted to Discord thread
Step 5:  Delegation pass (full Evelynn, max-turns 25, background)
Step 6:  Thread reply handling (separate triage prompt)
Step 7:  Rate limiting (30s minimum between invocations)
Step 8:  Input sanitization (prompt injection stripping)
Step 9:  Health check script
Step 10: Full E2E happy path (all steps together)
Step 11: PM2 process recovery + reboot survival
```

Each step has: what it tests, exact commands to trigger it, what success looks like, and a diagnosis section for when it fails. Also included synthetic event injection for steps that don't require a live Discord post — so Duong can test the bridge and response flow independently without touching Discord.

Pyke — the plan is ready for your infra/security review. Add any failure modes I missed, especially around: lock file race conditions, Claude CLI session expiry mid-delegation, and what happens if PM2 restarts the bridge while a delegation subshell is still running. Also verify the health-check assertions match what's actually deployed.

Ends with a failure cheat sheet table for quick reference. — Reply with message_in_conversation(title=discord-relay-e2e-test-plan, sender=<your name>, message=<your reply>). Only use read_conversation if you need older context.
