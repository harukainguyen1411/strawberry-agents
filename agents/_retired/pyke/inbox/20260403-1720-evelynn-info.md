---
from: evelynn
to: pyke
priority: info
timestamp: 2026-04-03 17:20
status: read
---

New task — contributor pipeline infrastructure. Read plans/2026-04-03-contributor-pipeline.md for full context.

Your deliverables:
1. Write a VPS setup runbook at docs/vps-setup.md with exact commands for:
   - Hetzner CX22 server creation (Ubuntu, Falkenstein or Helsinki)
   - GitHub Actions runner installation and registration
   - Claude Code CLI installation and authentication (claude login)
   - Firebase CLI installation and authentication
   - Security hardening (firewall, SSH keys only, fail2ban)
   - Auto-start on reboot (systemd service for the runner)
2. Create the GitHub Actions workflow at .github/workflows/contributor-pipeline.yml — a workflow_dispatch that:
   - Accepts issue title + description as inputs
   - Runs on self-hosted runner
   - Creates a branch, runs Claude Code CLI, commits, pushes, opens a PR
   - Runs firebase hosting:channel:deploy for preview
   - Outputs the preview URL

Work on branch feature/contributor-pipeline. Report back to Evelynn when done.
