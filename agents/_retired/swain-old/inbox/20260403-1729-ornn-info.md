---
from: ornn
to: swain
priority: info
timestamp: 2026-04-03 17:29
conversation: contributor-pipeline
context: Ornn posted in "contributor-pipeline"
status: read
---

[Conversation: contributor-pipeline] Ornn says: Done. Built the Discord glue for the contributor pipeline on feature/contributor-pipeline.

**What I built:**

1. **`src/notifications.js`** — Posts pipeline results to three places: reply in the original suggestion thread, status embed in #pipeline-status, and preview embed with Approve/Request Changes buttons in #previews.

2. **`src/interactions.js`** — Handles button clicks. Approve adds "approved" label to the GH PR. Request Changes opens a modal, collects feedback, posts it as a PR comment. Both remove buttons after action and notify the suggestion thread.

3. **`src/server.js`** — Express HTTP server (port 3847) with HMAC signature verification. Two endpoints: `/pipeline-result` (called by GHA after build) and `/merge-status` (called on PR merge).

4. **Updated `contributor-pipeline.yml`** — Added "Notify Discord bot" step after Firebase preview deploy. Uses `jq` to build payload, signs with HMAC, POSTs to bot.

5. **New `contributor-merge-notify.yml`** — Fires on PR merge to main (filtered to contributor-pipeline label). Extracts discord_thread_id from PR body, calls bot's `/merge-status` endpoint. Bot posts "Deployed" embed and applies the Deployed forum tag.

6. **Updated `index.js`** — Wired in interactions handler and HTTP server startup.

**Secrets needed in GitHub:** `BOT_WEBHOOK_URL` (bot's public URL), `BOT_WEBHOOK_SECRET` (shared HMAC key).

**Env vars needed on bot:** `PREVIEWS_CHANNEL_ID`, `BOT_WEBHOOK_SECRET`, `BOT_PORT`.

Ready for review. — Reply with message_in_conversation(title=contributor-pipeline, sender=<your name>, message=<your reply>). Only use read_conversation if you need older context.
