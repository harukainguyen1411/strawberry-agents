# Journal — 2026-04-05

## Session: B3 Task List Plan

Single task this session: write the implementation plan for B3 (myapps task list) from the approved roadmap.

Started by auditing the existing codebase before writing a word of the plan. That discipline paid off — the feature was already ~90% built. WeekGrid, DayColumn, TaskCard, the full Pinia store with Firestore CRUD, undo, carry-forward, local mode support, router registration, Home page card. Nearly complete.

The three real gaps I found:
1. `getDocs` (one-time fetch) instead of `onSnapshot` — Evelynn's writes won't appear in Duong's browser without refresh. This is the most important gap given the whole point is Evelynn + Duong sharing the same task list.
2. Missing Firestore index for the `createdAt` query
3. No E2E test

Also caught two new CLAUDE.md rules mid-session: plans go directly to main (not via PR), and `chore:` prefix for all commits. I'd already opened a PR before these were noted — the PR is harmless but the plan commit should have gone straight to main. Will apply the new rules correctly next session.

The plan itself is tight and actionable. A Sonnet agent should be able to implement all three gaps in one sitting.

## Session 2: Telegram bridge, Plan Viewer, PR review

Three tasks this session, all research/design — no implementation.

**Telegram Bridge Investigation** — Bridge exits immediately when run as background Bash task in Claude Code. Root cause: Claude Code's Bash tool has a hard 2-minute timeout (10 min max with background flag). The bridge is an infinite-loop daemon; it can't live inside a Claude Code session. Recommended launchd as the proper solution.

**Plan Viewer Design** — Designed a GitHub Contents API-backed markdown viewer inside myapps for mobile plan browsing. Status-tabbed browser, markdown reader, one-tap approve action. PAT stored in Firestore for auth (private repo). No new dependencies — `marked` already installed. Committed to `plans/proposed/`.

**PR #54 Architecture Review** — Reviewed Ornn's task list implementation against my B3 plan. All three required gaps addressed (onSnapshot, indexes, E2E tests). Bonus kanban board view added. Found one bug (missing cleanup on BoardView unmount), two nits. Posted review as `gh pr comment`.

Good signal that the plan-then-delegate model works when plans are precise — Ornn's implementation matched the spec closely.
