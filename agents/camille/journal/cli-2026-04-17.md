# 2026-04-17 — Phase 3 addendum

Drafted `plans/proposed/2026-04-17-dependabot-phase3.md` covering the 25 residual Dependabot alerts after Phase 1-2 resolved 79. Four batches: B3a bee-worker vite/vitest coupled upgrade, B3b root @tootallnate/once, B3c myapps minimatch per-major overrides, B3d myapps residuals (ajv / picomatch / brace-expansion / possibly vite #66 if not auto-closed by B8 propagation).

Critical design calls:
- Cross-referenced Viktor's lockfile patterns and Vi's verification protocol into an explicit pattern table (Appendix A) so implementers don't re-derive.
- Marked pre-flight re-query of open alerts as mandatory — auto-closure from Phase 2 merges may shrink scope further.
- Forced serialization of B3c → B3d because both touch `apps/myapps/package-lock.json`. B3a and B3b parallelize safely.
- Called out that bee-worker has no test files (Vi's finding), so vitest 2→3 API drift has zero blast radius inside bee-worker. The green gate is `tsc --noEmit` + build, not vitest.
- Specified minimatch overrides must be per-major (3.1.2, 5.1.7, 6.0.1, 10.0.1) — a flat unconditional override breaks parent-declared ranges.

Committed as `chore: propose dependabot phase 3 addendum for residual 25 alerts` (e3745d1). No implementers assigned per `rule-plan-writers-no-assignment`.
