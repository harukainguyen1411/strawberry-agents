# Step 2 ADR — gotchas

## Canonical schema.yaml does not exist
Step 1 ADR repeatedly references `tools/demo-studio-schema/schema.yaml` ("PR #37") — that file is not in the repo. Only `demo-studio-mcp/tests/fixtures/schema.yaml` (120 lines) exists. Step 2 must materialise a real one. Future ADRs: never reference an "external PR" without verifying the file is on disk.

## Service ownership boundary slipped
context.md says services 2–5 are owned by other teammates, but Step 2 must implement Service 2 because no other owner is doing it. Flagged in ADR open questions for Duong; do not silently absorb.

## Stub TODOs are well-marked in main.py
Every stub handler in `demo-config-mgmt/main.py` has explicit `# TODO: implement` comments — perfect map for Step 2 task decomposition.

## Reuse Step 1 ADR template
The "Test Strategy" + "Handoff Notes for Executors" sections from Step 1 ADR worked well for Caitlyn → Vi → Jayce chain. Kept the same structure for Step 2.
